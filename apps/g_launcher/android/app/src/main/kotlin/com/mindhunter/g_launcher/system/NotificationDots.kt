package com.mindhunter.g_launcher.system

/**
 * OPTIONAL - and it carries a cost.
 *
 * Notification dots require NotificationListenerService, which triggers a manual
 * Play policy review and asks the user for a scary-sounding permission.
 *
 * Recommendation: ship v1 WITHOUT dots. Add them later, as an opt-in toggle,
 * once the launcher has traction and the review is worth the wait.
 */
