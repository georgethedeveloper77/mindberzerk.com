// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_launcher.keys

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object KeysApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * Why a keystore operation failed, in a form the caller can act on.
 *
 * An enum rather than a message, because the three cases need three different
 * responses and only one of them is an error worth showing as one.
 */
enum class KeyFailure(val raw: Int) {
  /** The person dismissed the biometric prompt. Not an error: a cancel. */
  CANCELLED(0),
  /**
   * A new fingerprint or face was enrolled, so the key was destroyed by the
   * platform. `setInvalidatedByBiometricEnrollment(true)` is what causes this
   * and it is the right default; what it needs is copy that explains it and an
   * offer to regenerate.
   */
  INVALIDATED_BY_ENROLLMENT(1),
  /**
   * No biometric or device credential is set up at all, so a key that requires
   * one cannot be created.
   */
  NO_AUTHENTICATION_CONFIGURED(2),
  /**
   * Below API 28, where there is no BiometricPrompt to carry a CryptoObject.
   * The honest answer on such a device is that it cannot hold this kind of
   * key, not a silently weaker one.
   */
  UNSUPPORTED_PLATFORM(3),
  /** Anything else. [KeyResult.message] carries the detail. */
  UNKNOWN(4);

  companion object {
    fun ofRaw(raw: Int): KeyFailure? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * A key that exists in the keystore.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class KeyInfo (
  /**
   * The name the user gave it. One key per device, so this is usually the
   * device: revoking a lost phone is then deleting one line from
   * `authorized_keys` rather than rotating everything.
   */
  val alias: String,
  /**
   * The affine coordinates of the public point.
   *
   * RAW, exactly as the platform produced them, INCLUDING a BigInteger sign
   * byte or missing leading zeros. Normalising them here would put that logic
   * in Kotlin where it cannot be tested against fixed vectors; Dart does it in
   * `ssh_key_encoding.dart`, which has them.
   */
  val x: ByteArray,
  val y: ByteArray,
  /**
   * Whether the private key is held in hardware rather than in software.
   *
   * Surfaced because it is the difference between two SECURITY CLAIMS, and the
   * key manager says which one applies. Claiming hardware backing on a device
   * that does not have it is worse than not claiming it.
   */
  val hardwareBacked: Boolean,
  /**
   * Whether it landed in StrongBox specifically, which is a dedicated secure
   * chip rather than the TEE. Most phones have a TEE, far fewer have this.
   */
  val strongBoxBacked: Boolean,
  val createdAtMillis: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): KeyInfo {
      val alias = pigeonVar_list[0] as String
      val x = pigeonVar_list[1] as ByteArray
      val y = pigeonVar_list[2] as ByteArray
      val hardwareBacked = pigeonVar_list[3] as Boolean
      val strongBoxBacked = pigeonVar_list[4] as Boolean
      val createdAtMillis = pigeonVar_list[5] as Long
      return KeyInfo(alias, x, y, hardwareBacked, strongBoxBacked, createdAtMillis)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      alias,
      x,
      y,
      hardwareBacked,
      strongBoxBacked,
      createdAtMillis,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as KeyInfo
    return KeysApiPigeonUtils.deepEquals(this.alias, other.alias) && KeysApiPigeonUtils.deepEquals(this.x, other.x) && KeysApiPigeonUtils.deepEquals(this.y, other.y) && KeysApiPigeonUtils.deepEquals(this.hardwareBacked, other.hardwareBacked) && KeysApiPigeonUtils.deepEquals(this.strongBoxBacked, other.strongBoxBacked) && KeysApiPigeonUtils.deepEquals(this.createdAtMillis, other.createdAtMillis)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.alias)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.x)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.y)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.hardwareBacked)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.strongBoxBacked)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.createdAtMillis)
    return result
  }
  override fun toString(): String {
    return "KeyInfo(alias=$alias, x=${x.contentToString()}, y=${y.contentToString()}, hardwareBacked=$hardwareBacked, strongBoxBacked=$strongBoxBacked, createdAtMillis=$createdAtMillis)"
  }
}

/**
 * The outcome of an operation that can fail in a way worth naming.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class KeyResult (
  val info: KeyInfo? = null,
  /**
   * An ASN.1 DER ECDSA signature, `SEQUENCE { INTEGER r, INTEGER s }`.
   *
   * Converted to the SSH wire form in Dart, by `ecdsaDerToSshSignature`. Doing
   * it here would mean the mpint rules living in Kotlin, untested; there they
   * are pinned to fixed vectors.
   */
  val signature: ByteArray? = null,
  /** Null on success. */
  val failure: KeyFailure? = null,
  val message: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): KeyResult {
      val info = pigeonVar_list[0] as KeyInfo?
      val signature = pigeonVar_list[1] as ByteArray?
      val failure = pigeonVar_list[2] as KeyFailure?
      val message = pigeonVar_list[3] as String?
      return KeyResult(info, signature, failure, message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      info,
      signature,
      failure,
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as KeyResult
    return KeysApiPigeonUtils.deepEquals(this.info, other.info) && KeysApiPigeonUtils.deepEquals(this.signature, other.signature) && KeysApiPigeonUtils.deepEquals(this.failure, other.failure) && KeysApiPigeonUtils.deepEquals(this.message, other.message)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.info)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.signature)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.failure)
    result = 31 * result + KeysApiPigeonUtils.deepHash(this.message)
    return result
  }
  override fun toString(): String {
    return "KeyResult(info=$info, signature=${signature?.contentToString()}, failure=$failure, message=$message)"
  }
}
private open class KeysApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          KeyFailure.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          KeyInfo.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          KeyResult.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is KeyFailure -> {
        stream.write(129)
        writeValue(stream, value.raw.toLong())
      }
      is KeyInfo -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is KeyResult -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface KeysHostApi {
  /** Aliases of every key this app holds. */
  fun listKeys(): List<String>
  /** The public part of one key, or null when the alias is unknown. */
  fun publicKey(alias: String): KeyInfo?
  /**
   * Generate a P-256 key, non-extractable, requiring authentication per use.
   *
   * P-256 rather than Ed25519 because AndroidKeyStore does not support
   * Ed25519 at all. OpenSSH accepts `ecdsa-sha2-nistp256`, so this works
   * against any server that has not stripped ECDSA.
   *
   * Not async: generation itself needs no prompt. Only signing does.
   */
  fun generateKey(alias: String): KeyResult
  /**
   * Forget a key. Returns false when the alias was already unknown.
   *
   * The key is destroyed in the keystore and cannot be recovered, which is why
   * the caller should say so before calling this and remind the person to
   * remove the line from `authorized_keys` afterwards.
   */
  fun deleteKey(alias: String): Boolean
  /**
   * Sign a challenge, behind a biometric prompt.
   *
   * ASYNC, and that single word is the reason dartssh2 had to be forked: a
   * prompt takes as long as a person takes, and `SSHKeyPair.sign` was declared
   * to return synchronously.
   */
  fun sign(alias: String, challenge: ByteArray, callback: (Result<KeyResult>) -> Unit)

  companion object {
    /** The codec used by KeysHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      KeysApiPigeonCodec()
    }
    /** Sets up an instance of `KeysHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: KeysHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_launcher.KeysHostApi.listKeys$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.listKeys())
            } catch (exception: Throwable) {
              KeysApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_launcher.KeysHostApi.publicKey$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val aliasArg = args[0] as String
            val wrapped: List<Any?> = try {
              listOf(api.publicKey(aliasArg))
            } catch (exception: Throwable) {
              KeysApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_launcher.KeysHostApi.generateKey$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val aliasArg = args[0] as String
            val wrapped: List<Any?> = try {
              listOf(api.generateKey(aliasArg))
            } catch (exception: Throwable) {
              KeysApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_launcher.KeysHostApi.deleteKey$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val aliasArg = args[0] as String
            val wrapped: List<Any?> = try {
              listOf(api.deleteKey(aliasArg))
            } catch (exception: Throwable) {
              KeysApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_launcher.KeysHostApi.sign$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val aliasArg = args[0] as String
            val challengeArg = args[1] as ByteArray
            api.sign(aliasArg, challengeArg) { result: Result<KeyResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(KeysApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(KeysApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
