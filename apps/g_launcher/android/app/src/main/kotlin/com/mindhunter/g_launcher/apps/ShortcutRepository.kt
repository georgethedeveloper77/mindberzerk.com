package com.mindhunter.g_launcher.apps

/**
 * PHASE 4 (late) - long-press shortcuts.
 *
 * LauncherApps.getShortcuts() exposes an app's deep shortcuts (the ones you see
 * on long-press in Pixel Launcher). Requires the HOME role to be held.
 * Also handles pinned shortcuts via LauncherApps.pinShortcuts().
 *
 * Nice-to-have, not a launch blocker. Skip until the core shell feels right.
 */
