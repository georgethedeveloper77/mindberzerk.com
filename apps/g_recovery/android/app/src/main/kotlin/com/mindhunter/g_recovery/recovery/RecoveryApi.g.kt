// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.recovery

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object RecoveryApiPigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * What this app is currently allowed to see. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RecoveryAccess (
  /** `Environment.isExternalStorageManager()`. */
  val allFilesAccess: Boolean,
  /**
   * THE FACT THE WHOLE FEATURE RESTS ON.
   *
   * On Android 11 and above a MediaStore query with MATCH_INCLUDE returns only
   * the items THIS app trashed. Another app's trashed photo is invisible
   * without All Files Access. A freshly installed G Recovery has trashed
   * nothing, so without the grant the ledger is empty on every device forever.
   *
   * Currently identical to [allFilesAccess]. It is a separate field because it
   * is a separate claim: if a future Android adds a narrower permission that
   * buys the same visibility, this flips and the UI needs no change.
   */
  val canSeeOtherAppsTrash: Boolean,
  /**
   * Whether the settings screen for the grant can be opened at all. False on
   * the rare build with the activity stripped, where the UI must stop offering
   * a button that goes nowhere.
   */
  val storageManagerAvailable: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RecoveryAccess {
      val allFilesAccess = pigeonVar_list[0] as Boolean
      val canSeeOtherAppsTrash = pigeonVar_list[1] as Boolean
      val storageManagerAvailable = pigeonVar_list[2] as Boolean
      return RecoveryAccess(allFilesAccess, canSeeOtherAppsTrash, storageManagerAvailable)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      allFilesAccess,
      canSeeOtherAppsTrash,
      storageManagerAvailable,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RecoveryAccess
    return RecoveryApiPigeonUtils.deepEquals(this.allFilesAccess, other.allFilesAccess) && RecoveryApiPigeonUtils.deepEquals(this.canSeeOtherAppsTrash, other.canSeeOtherAppsTrash) && RecoveryApiPigeonUtils.deepEquals(this.storageManagerAvailable, other.storageManagerAvailable)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.allFilesAccess)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.canSeeOtherAppsTrash)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.storageManagerAvailable)
    return result
  }
  override fun toString(): String {
    return "RecoveryAccess(allFilesAccess=$allFilesAccess, canSeeOtherAppsTrash=$canSeeOtherAppsTrash, storageManagerAvailable=$storageManagerAvailable)"
  }
}

/**
 * One place deleted data can still be hiding. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RecoverySource (
  /** Stable id: "media_trash", "app_trash", "thumbnails". */
  val sourceId: String,
  val label: String,
  /**
   * "full" | "preview" | "none".
   *
   * THE HONESTY STAMP, and it is data rather than a UI decision. A thumbnail
   * scan can only ever return a 512 px preview, and a card that does not say so
   * is the lie this app exists not to tell. Carried on the item too, because a
   * single source can mix fidelities once the trashmap grows.
   */
  val fidelity: String,
  /**
   * False when the source cannot be read at all on this device, for example
   * app trash without All Files Access. The row is still shown, with [detail]
   * explaining why, rather than being silently dropped.
   */
  val available: Boolean,
  val itemCount: Long,
  val totalBytes: Long,
  /**
   * Human readable reason when [available] is false, or a qualifier when it is
   * true. Never the thing the UI branches on.
   */
  val detail: String? = null,
  /**
   * Days the OS keeps items here before permanent deletion. Null when the
   * source has no expiry, which is true of thumbnails and of most app trash
   * folders.
   */
  val retentionDays: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RecoverySource {
      val sourceId = pigeonVar_list[0] as String
      val label = pigeonVar_list[1] as String
      val fidelity = pigeonVar_list[2] as String
      val available = pigeonVar_list[3] as Boolean
      val itemCount = pigeonVar_list[4] as Long
      val totalBytes = pigeonVar_list[5] as Long
      val detail = pigeonVar_list[6] as String?
      val retentionDays = pigeonVar_list[7] as Long?
      return RecoverySource(sourceId, label, fidelity, available, itemCount, totalBytes, detail, retentionDays)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      sourceId,
      label,
      fidelity,
      available,
      itemCount,
      totalBytes,
      detail,
      retentionDays,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RecoverySource
    return RecoveryApiPigeonUtils.deepEquals(this.sourceId, other.sourceId) && RecoveryApiPigeonUtils.deepEquals(this.label, other.label) && RecoveryApiPigeonUtils.deepEquals(this.fidelity, other.fidelity) && RecoveryApiPigeonUtils.deepEquals(this.available, other.available) && RecoveryApiPigeonUtils.deepEquals(this.itemCount, other.itemCount) && RecoveryApiPigeonUtils.deepEquals(this.totalBytes, other.totalBytes) && RecoveryApiPigeonUtils.deepEquals(this.detail, other.detail) && RecoveryApiPigeonUtils.deepEquals(this.retentionDays, other.retentionDays)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.sourceId)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.label)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.fidelity)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.available)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.itemCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.totalBytes)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.detail)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.retentionDays)
    return result
  }
  override fun toString(): String {
    return "RecoverySource(sourceId=$sourceId, label=$label, fidelity=$fidelity, available=$available, itemCount=$itemCount, totalBytes=$totalBytes, detail=$detail, retentionDays=$retentionDays)"
  }
}

/**
 * One recoverable thing. Codec 131.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RecoverableItem (
  /**
   * Opaque handle minted natively. Dart must never parse it.
   *
   * A MediaStore item and a loose file need completely different restore paths,
   * and encoding that difference in a string the UI can read invites a caller
   * to branch on it. Native holds the index; Dart hands the id back.
   */
  val itemId: String,
  val sourceId: String,
  val name: String,
  /** "image" | "video" | "audio" | "document" | "other". */
  val kind: String,
  /** "full" or "preview". Per item, not just per source. */
  val fidelity: String,
  val sizeBytes: Long,
  /**
   * Where it was, when that is knowable. Null for a loose file in a trash
   * folder, because the original location is genuinely not recorded anywhere
   * and guessing it would put files back in the wrong place.
   */
  val relativePath: String? = null,
  val mimeType: String? = null,
  val dateDeletedMillis: Long? = null,
  val dateAddedMillis: Long? = null,
  /**
   * Days until the OS deletes this permanently. Null means no expiry is known,
   * which is not the same as no expiry.
   */
  val expiresInDays: Long? = null,
  /**
   * A URI the UI can draw a thumbnail from. Display bytes only, through
   * Flutter's image cache.
   */
  val previewUri: String? = null,
  val width: Long? = null,
  val height: Long? = null,
  val durationMillis: Long? = null,
  /**
   * Which app or folder this came from, for display. "WhatsApp Status", "MIUI
   * Gallery". Null for MediaStore trash, where the folder path already says it.
   *
   * APPENDED IN PHASE 6b, at the end of the class, which is the only safe
   * position: field order inside a Pigeon class is the encoding order.
   */
  val origin: String? = null,
  /**
   * What KIND of find this is. "trash" | "status" | "cache".
   *
   * The distinction that makes Status work. A WhatsApp status was never
   * deleted: it is a file with a 24 hour life sitting in a visible folder, and
   * offering to "restore" it is nonsense because it is not lost. It is SAVED
   * instead, into the recovery folder, before it expires on its own.
   *
   * Getting this wrong in either direction is the same failure. Calling a
   * status a recovered file overclaims; hiding it entirely misses what is
   * probably the single most searched for case in this whole category.
   */
  val role: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RecoverableItem {
      val itemId = pigeonVar_list[0] as String
      val sourceId = pigeonVar_list[1] as String
      val name = pigeonVar_list[2] as String
      val kind = pigeonVar_list[3] as String
      val fidelity = pigeonVar_list[4] as String
      val sizeBytes = pigeonVar_list[5] as Long
      val relativePath = pigeonVar_list[6] as String?
      val mimeType = pigeonVar_list[7] as String?
      val dateDeletedMillis = pigeonVar_list[8] as Long?
      val dateAddedMillis = pigeonVar_list[9] as Long?
      val expiresInDays = pigeonVar_list[10] as Long?
      val previewUri = pigeonVar_list[11] as String?
      val width = pigeonVar_list[12] as Long?
      val height = pigeonVar_list[13] as Long?
      val durationMillis = pigeonVar_list[14] as Long?
      val origin = pigeonVar_list[15] as String?
      val role = pigeonVar_list[16] as String?
      return RecoverableItem(itemId, sourceId, name, kind, fidelity, sizeBytes, relativePath, mimeType, dateDeletedMillis, dateAddedMillis, expiresInDays, previewUri, width, height, durationMillis, origin, role)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      itemId,
      sourceId,
      name,
      kind,
      fidelity,
      sizeBytes,
      relativePath,
      mimeType,
      dateDeletedMillis,
      dateAddedMillis,
      expiresInDays,
      previewUri,
      width,
      height,
      durationMillis,
      origin,
      role,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RecoverableItem
    return RecoveryApiPigeonUtils.deepEquals(this.itemId, other.itemId) && RecoveryApiPigeonUtils.deepEquals(this.sourceId, other.sourceId) && RecoveryApiPigeonUtils.deepEquals(this.name, other.name) && RecoveryApiPigeonUtils.deepEquals(this.kind, other.kind) && RecoveryApiPigeonUtils.deepEquals(this.fidelity, other.fidelity) && RecoveryApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes) && RecoveryApiPigeonUtils.deepEquals(this.relativePath, other.relativePath) && RecoveryApiPigeonUtils.deepEquals(this.mimeType, other.mimeType) && RecoveryApiPigeonUtils.deepEquals(this.dateDeletedMillis, other.dateDeletedMillis) && RecoveryApiPigeonUtils.deepEquals(this.dateAddedMillis, other.dateAddedMillis) && RecoveryApiPigeonUtils.deepEquals(this.expiresInDays, other.expiresInDays) && RecoveryApiPigeonUtils.deepEquals(this.previewUri, other.previewUri) && RecoveryApiPigeonUtils.deepEquals(this.width, other.width) && RecoveryApiPigeonUtils.deepEquals(this.height, other.height) && RecoveryApiPigeonUtils.deepEquals(this.durationMillis, other.durationMillis) && RecoveryApiPigeonUtils.deepEquals(this.origin, other.origin) && RecoveryApiPigeonUtils.deepEquals(this.role, other.role)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.itemId)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.sourceId)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.name)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.kind)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.fidelity)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.sizeBytes)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.relativePath)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.mimeType)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.dateDeletedMillis)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.dateAddedMillis)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.expiresInDays)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.previewUri)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.width)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.height)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.durationMillis)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.origin)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.role)
    return result
  }
  override fun toString(): String {
    return "RecoverableItem(itemId=$itemId, sourceId=$sourceId, name=$name, kind=$kind, fidelity=$fidelity, sizeBytes=$sizeBytes, relativePath=$relativePath, mimeType=$mimeType, dateDeletedMillis=$dateDeletedMillis, dateAddedMillis=$dateAddedMillis, expiresInDays=$expiresInDays, previewUri=$previewUri, width=$width, height=$height, durationMillis=$durationMillis, origin=$origin, role=$role)"
  }
}

/**
 * Progress during a scan. Codec 132.
 *
 * Results stream because the one genuinely good idea in this category is
 * DiskDigger's: items become restorable before the scan finishes. A user who
 * spots the photo they came for should not have to wait out the remaining
 * thirty thousand entries.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ScanProgress (
  val sourceId: String,
  /** Entries examined so far. */
  val scanned: Long,
  /**
   * Entries to examine. A REAL COUNT, taken before the walk begins, not a
   * timer dressed up as a progress bar.
   */
  val total: Long,
  val found: Long,
  val foundBytes: Long,
  val done: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ScanProgress {
      val sourceId = pigeonVar_list[0] as String
      val scanned = pigeonVar_list[1] as Long
      val total = pigeonVar_list[2] as Long
      val found = pigeonVar_list[3] as Long
      val foundBytes = pigeonVar_list[4] as Long
      val done = pigeonVar_list[5] as Boolean
      return ScanProgress(sourceId, scanned, total, found, foundBytes, done)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      sourceId,
      scanned,
      total,
      found,
      foundBytes,
      done,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ScanProgress
    return RecoveryApiPigeonUtils.deepEquals(this.sourceId, other.sourceId) && RecoveryApiPigeonUtils.deepEquals(this.scanned, other.scanned) && RecoveryApiPigeonUtils.deepEquals(this.total, other.total) && RecoveryApiPigeonUtils.deepEquals(this.found, other.found) && RecoveryApiPigeonUtils.deepEquals(this.foundBytes, other.foundBytes) && RecoveryApiPigeonUtils.deepEquals(this.done, other.done)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.sourceId)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.scanned)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.total)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.found)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.foundBytes)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.done)
    return result
  }
  override fun toString(): String {
    return "ScanProgress(sourceId=$sourceId, scanned=$scanned, total=$total, found=$found, foundBytes=$foundBytes, done=$done)"
  }
}

/**
 * The result of one restore or purge attempt. Codec 133.
 *
 * A RESULT OBJECT RATHER THAN A THROWN ERROR, because most outcomes are not
 * errors. `expired` means the OS already removed it and the list was stale;
 * `needsConsent` means the system wants the user to confirm and the UI must ask
 * rather than retry; `noSpace` is user-actionable. Collapsing these into an
 * exception produces the "Restore failed" dialog that tells nobody anything.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RestoreOutcome (
  val itemId: String,
  /**
   * "restored" | "expired" | "needsConsent" | "noSpace" | "denied" |
   * "notFound" | "failed"
   */
  val status: String,
  val detail: String,
  /** Where it ended up. Null unless [status] is "restored". */
  val restoredPath: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RestoreOutcome {
      val itemId = pigeonVar_list[0] as String
      val status = pigeonVar_list[1] as String
      val detail = pigeonVar_list[2] as String
      val restoredPath = pigeonVar_list[3] as String?
      return RestoreOutcome(itemId, status, detail, restoredPath)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      itemId,
      status,
      detail,
      restoredPath,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RestoreOutcome
    return RecoveryApiPigeonUtils.deepEquals(this.itemId, other.itemId) && RecoveryApiPigeonUtils.deepEquals(this.status, other.status) && RecoveryApiPigeonUtils.deepEquals(this.detail, other.detail) && RecoveryApiPigeonUtils.deepEquals(this.restoredPath, other.restoredPath)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.itemId)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.status)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.detail)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.restoredPath)
    return result
  }
  override fun toString(): String {
    return "RestoreOutcome(itemId=$itemId, status=$status, detail=$detail, restoredPath=$restoredPath)"
  }
}

/**
 * Everything the ledger needs to draw itself. Codec 134.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RecoverySummary (
  val sources: List<RecoverySource>,
  val totalItems: Long,
  val totalBytes: Long,
  /**
   * Items with 48 hours or less left. The one number worth putting a deadline
   * on, because after it passes nothing can bring them back.
   */
  val expiringSoonItems: Long,
  /**
   * Per kind, so home can label six category tiles without a second call.
   *
   * APPENDED IN PHASE 4, at the end of the class. Field order inside a Pigeon
   * class is the encoding order: appending is safe, inserting is not. Same rule
   * that governs IconStyle in the launcher schema.
   *
   * Computed in the same cursor pass as the totals, so the pre-scan stays a
   * counting operation and never walks a directory tree.
   */
  val imageCount: Long,
  val videoCount: Long,
  val audioCount: Long,
  val documentCount: Long,
  val otherCount: Long,
  /**
   * True when this summary was produced WITHOUT full access, so the numbers are
   * a floor rather than a total.
   *
   * This is what stops the pre-scan from lying. Before the grant the app can
   * only count its own trashed items and the thumbnail cache, and a screen
   * showing that as a total would be the fake count every competitor opens
   * with.
   */
  val partial: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RecoverySummary {
      val sources = pigeonVar_list[0] as List<RecoverySource>
      val totalItems = pigeonVar_list[1] as Long
      val totalBytes = pigeonVar_list[2] as Long
      val expiringSoonItems = pigeonVar_list[3] as Long
      val imageCount = pigeonVar_list[4] as Long
      val videoCount = pigeonVar_list[5] as Long
      val audioCount = pigeonVar_list[6] as Long
      val documentCount = pigeonVar_list[7] as Long
      val otherCount = pigeonVar_list[8] as Long
      val partial = pigeonVar_list[9] as Boolean
      return RecoverySummary(sources, totalItems, totalBytes, expiringSoonItems, imageCount, videoCount, audioCount, documentCount, otherCount, partial)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      sources,
      totalItems,
      totalBytes,
      expiringSoonItems,
      imageCount,
      videoCount,
      audioCount,
      documentCount,
      otherCount,
      partial,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RecoverySummary
    return RecoveryApiPigeonUtils.deepEquals(this.sources, other.sources) && RecoveryApiPigeonUtils.deepEquals(this.totalItems, other.totalItems) && RecoveryApiPigeonUtils.deepEquals(this.totalBytes, other.totalBytes) && RecoveryApiPigeonUtils.deepEquals(this.expiringSoonItems, other.expiringSoonItems) && RecoveryApiPigeonUtils.deepEquals(this.imageCount, other.imageCount) && RecoveryApiPigeonUtils.deepEquals(this.videoCount, other.videoCount) && RecoveryApiPigeonUtils.deepEquals(this.audioCount, other.audioCount) && RecoveryApiPigeonUtils.deepEquals(this.documentCount, other.documentCount) && RecoveryApiPigeonUtils.deepEquals(this.otherCount, other.otherCount) && RecoveryApiPigeonUtils.deepEquals(this.partial, other.partial)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.sources)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.totalItems)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.totalBytes)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.expiringSoonItems)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.imageCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.videoCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.audioCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.documentCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.otherCount)
    result = 31 * result + RecoveryApiPigeonUtils.deepHash(this.partial)
    return result
  }
  override fun toString(): String {
    return "RecoverySummary(sources=$sources, totalItems=$totalItems, totalBytes=$totalBytes, expiringSoonItems=$expiringSoonItems, imageCount=$imageCount, videoCount=$videoCount, audioCount=$audioCount, documentCount=$documentCount, otherCount=$otherCount, partial=$partial)"
  }
}
private open class RecoveryApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RecoveryAccess.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RecoverySource.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RecoverableItem.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ScanProgress.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RestoreOutcome.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RecoverySummary.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is RecoveryAccess -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is RecoverySource -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is RecoverableItem -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is ScanProgress -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is RestoreOutcome -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is RecoverySummary -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface RecoveryHostApi {
  /**
   * Push the trash path registry. Called once at startup from Dart.
   *
   * PUSHED FROM DART rather than read from assets natively, and that is the
   * whole point: Dart decides where the JSON came from. Today it is a bundled
   * asset. In Phase 7 it is a signed CDN pack, and not one line of Kotlin
   * changes. Recovery coverage is data, not code.
   */
  fun setTrashMap(json: String, callback: (Result<Unit>) -> Unit)
  fun access(callback: (Result<RecoveryAccess>) -> Unit)
  /**
   * Opens the All Files Access settings screen. Returns false when no activity
   * on this device can handle the intent.
   */
  fun requestAllFilesAccess(callback: (Result<Boolean>) -> Unit)
  /**
   * Fast counts for the home screen, from MediaStore aggregates and a shallow
   * look at the thumbnail cache. Never walks a directory tree.
   *
   * Runs before the permission grant, which is why [RecoverySummary.partial]
   * exists. This is what makes home open populated instead of on a spinner.
   */
  fun prescan(callback: (Result<RecoverySummary>) -> Unit)
  /**
   * The full walk. Progress arrives on [RecoveryFlutterApi]; items become
   * readable through [items] while it runs.
   */
  fun scan(sourceIds: List<String>, callback: (Result<RecoverySummary>) -> Unit)
  /** Stop an in-flight scan. Whatever was found so far stays available. */
  fun cancelScan(callback: (Result<Unit>) -> Unit)
  /**
   * A page of findings. Sorted newest deleted first, then largest first for
   * items with no deletion date.
   */
  fun items(sourceId: String, offset: Long, limit: Long, callback: (Result<List<RecoverableItem>>) -> Unit)
  /**
   * Put items back. MediaStore items return to their original folder; loose
   * files go to a recovery folder, because their original location was never
   * recorded anywhere and inventing one puts files in the wrong place.
   */
  fun restore(itemIds: List<String>, callback: (Result<List<RestoreOutcome>>) -> Unit)
  /** Delete permanently, right now. Used by the review session's bin. */
  fun purge(itemIds: List<String>, callback: (Result<List<RestoreOutcome>>) -> Unit)
  /**
   * JPEG bytes for one item, at most [maxPixels] on the long edge.
   *
   * APPENDED IN PHASE 5. A method, so no type ids move.
   *
   * BYTES RATHER THAN A URI, and this is the one place that decision is not
   * obvious. Flutter cannot load a `content://` URI: Image.network wants http
   * and Image.file wants a path, and a trashed MediaStore row has neither. The
   * alternatives were a FileProvider that re-exports other apps' deleted files
   * over a content provider of our own, which is a security surface for a
   * thumbnail, or this.
   *
   * Downscaled NATIVELY, before the bytes cross the channel. A review session
   * showing a hundred 12 megapixel photos would otherwise push about 600 MB
   * through the platform channel and decode all of it in the Dart heap. At 512
   * px the same session is a few megabytes.
   *
   * Null when the item has no renderable preview, which is the honest answer
   * for an audio file or a document, not an error.
   */
  fun thumbnail(itemId: String, maxPixels: Long, callback: (Result<ByteArray?>) -> Unit)
  /**
   * Files that are still on the device, matched by name.
   *
   * APPENDED IN PHASE 4. A HostApi method is not codec-numbered, so adding one
   * moves no type ids. That is why sensor streaming and this could both be left
   * out of the first cut without painting the schema into a corner.
   *
   * RETURNS RecoverableItem, WHICH IS NOT A TYPE LIE, though it looks like one.
   * These files were never deleted, and [RecoverableItem.sourceId] is
   * "live_files" so the UI can tell them apart and refuse to offer Restore on
   * something that was never lost. A parallel class carrying the same eleven
   * fields would have been a second thing to keep in step with the first, and
   * search results have to render in one homogeneous list either way.
   *
   * Backed by MediaStore's own index rather than a directory walk, so it stays
   * fast enough to run on every keystroke.
   */
  fun search(query: String, limit: Long, callback: (Result<List<RecoverableItem>>) -> Unit)

  companion object {
    /** The codec used by RecoveryHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      RecoveryApiPigeonCodec()
    }
    /** Sets up an instance of `RecoveryHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: RecoveryHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.setTrashMap$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val jsonArg = args[0] as String
            api.setTrashMap(jsonArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(RecoveryApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.access$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.access{ result: Result<RecoveryAccess> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.requestAllFilesAccess$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestAllFilesAccess{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.prescan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.prescan{ result: Result<RecoverySummary> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.scan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val sourceIdsArg = args[0] as List<String>
            api.scan(sourceIdsArg) { result: Result<RecoverySummary> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.cancelScan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cancelScan{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(RecoveryApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.items$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val sourceIdArg = args[0] as String
            val offsetArg = args[1] as Long
            val limitArg = args[2] as Long
            api.items(sourceIdArg, offsetArg, limitArg) { result: Result<List<RecoverableItem>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.restore$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val itemIdsArg = args[0] as List<String>
            api.restore(itemIdsArg) { result: Result<List<RestoreOutcome>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.purge$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val itemIdsArg = args[0] as List<String>
            api.purge(itemIdsArg) { result: Result<List<RestoreOutcome>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.thumbnail$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val itemIdArg = args[0] as String
            val maxPixelsArg = args[1] as Long
            api.thumbnail(itemIdArg, maxPixelsArg) { result: Result<ByteArray?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.RecoveryHostApi.search$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val queryArg = args[0] as String
            val limitArg = args[1] as Long
            api.search(queryArg, limitArg) { result: Result<List<RecoverableItem>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(RecoveryApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(RecoveryApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/** Generated class from Pigeon that represents Flutter messages that can be called from Kotlin. */
class RecoveryFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by RecoveryFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      RecoveryApiPigeonCodec()
    }
  }
  /**
   * Fires on the platform thread during a scan, throttled natively so a fast
   * source cannot flood the channel.
   */
  fun onScanProgress(progressArg: ScanProgress, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.g_recovery.RecoveryFlutterApi.onScanProgress$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(progressArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(RecoveryApiPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
