// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.compare

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object CompareApiPigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * A set of files that belong together. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompareGroup (
  val groupId: String,
  /**
   * "exact" for byte identical copies, "similar" for near duplicates.
   *
   * A String rather than an enum, matching every other schema in this app: an
   * enum numbers before classes and adding a third kind would renumber
   * everything after it.
   */
  val kind: String,
  /**
   * Every member, largest first, so the head of the list is the one worth
   * keeping on size alone.
   */
  val fileIds: List<String>,
  val totalBytes: Long,
  /**
   * What removing everything except the keeper would free.
   *
   * Reported separately from [totalBytes] because they are wildly different
   * numbers and confusing them is how a cleaner app claims to free forty
   * gigabytes and frees four.
   */
  val wastedBytes: Long,
  /**
   * The suggestion, never the decision.
   *
   * Largest file in the group, since for two encodings of one photo the bigger
   * one carries more detail. The UI preselects the others and lets the user
   * change it; nothing is ever removed on this recommendation alone.
   */
  val keepFileId: String,
  /**
   * Size per file, in the same order as [fileIds].
   *
   * APPENDED LAST, so nothing before it renumbers.
   *
   * Without it the UI divided the group total evenly, which is exact for byte
   * identical copies and wrong for near duplicates, where a shared copy can be
   * a fifth the size of the original. Once a user can keep more than one file
   * in a set, that approximation stops being cosmetic: the figure on the button
   * is what they are deciding on.
   */
  val sizes: List<Long>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompareGroup {
      val groupId = pigeonVar_list[0] as String
      val kind = pigeonVar_list[1] as String
      val fileIds = pigeonVar_list[2] as List<String>
      val totalBytes = pigeonVar_list[3] as Long
      val wastedBytes = pigeonVar_list[4] as Long
      val keepFileId = pigeonVar_list[5] as String
      val sizes = pigeonVar_list[6] as List<Long>
      return CompareGroup(groupId, kind, fileIds, totalBytes, wastedBytes, keepFileId, sizes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      groupId,
      kind,
      fileIds,
      totalBytes,
      wastedBytes,
      keepFileId,
      sizes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompareGroup
    return CompareApiPigeonUtils.deepEquals(this.groupId, other.groupId) && CompareApiPigeonUtils.deepEquals(this.kind, other.kind) && CompareApiPigeonUtils.deepEquals(this.fileIds, other.fileIds) && CompareApiPigeonUtils.deepEquals(this.totalBytes, other.totalBytes) && CompareApiPigeonUtils.deepEquals(this.wastedBytes, other.wastedBytes) && CompareApiPigeonUtils.deepEquals(this.keepFileId, other.keepFileId) && CompareApiPigeonUtils.deepEquals(this.sizes, other.sizes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.groupId)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.kind)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.fileIds)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.totalBytes)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.wastedBytes)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.keepFileId)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.sizes)
    return result
  }
  override fun toString(): String {
    return "CompareGroup(groupId=$groupId, kind=$kind, fileIds=$fileIds, totalBytes=$totalBytes, wastedBytes=$wastedBytes, keepFileId=$keepFileId, sizes=$sizes)"
  }
}

/**
 * One image judged soft. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class BlurredImage (
  val fileId: String,
  /**
   * Variance of the Laplacian. Higher is sharper.
   *
   * Reported raw rather than as a verdict so the UI can sort by it and a
   * threshold can move without a native change. It is scale dependent, which
   * is why every image is measured at the same working size.
   */
  val sharpness: Double,
  val sizeBytes: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): BlurredImage {
      val fileId = pigeonVar_list[0] as String
      val sharpness = pigeonVar_list[1] as Double
      val sizeBytes = pigeonVar_list[2] as Long
      return BlurredImage(fileId, sharpness, sizeBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      sharpness,
      sizeBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as BlurredImage
    return CompareApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompareApiPigeonUtils.deepEquals(this.sharpness, other.sharpness) && CompareApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.sharpness)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.sizeBytes)
    return result
  }
  override fun toString(): String {
    return "BlurredImage(fileId=$fileId, sharpness=$sharpness, sizeBytes=$sizeBytes)"
  }
}

/**
 * What a scan found. Codec 131.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompareResult (
  val groups: List<CompareGroup>,
  val blurred: List<BlurredImage>,
  val scanned: Long,
  /**
   * True when the user stopped it. The findings so far are real and are kept;
   * what is not true is that they are complete, and the UI says so.
   */
  val cancelled: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompareResult {
      val groups = pigeonVar_list[0] as List<CompareGroup>
      val blurred = pigeonVar_list[1] as List<BlurredImage>
      val scanned = pigeonVar_list[2] as Long
      val cancelled = pigeonVar_list[3] as Boolean
      return CompareResult(groups, blurred, scanned, cancelled)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      groups,
      blurred,
      scanned,
      cancelled,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompareResult
    return CompareApiPigeonUtils.deepEquals(this.groups, other.groups) && CompareApiPigeonUtils.deepEquals(this.blurred, other.blurred) && CompareApiPigeonUtils.deepEquals(this.scanned, other.scanned) && CompareApiPigeonUtils.deepEquals(this.cancelled, other.cancelled)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.groups)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.blurred)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.scanned)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.cancelled)
    return result
  }
  override fun toString(): String {
    return "CompareResult(groups=$groups, blurred=$blurred, scanned=$scanned, cancelled=$cancelled)"
  }
}

/**
 * How far along. Codec 132.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompareProgress (
  val scanned: Long,
  val total: Long,
  val found: Long,
  val done: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompareProgress {
      val scanned = pigeonVar_list[0] as Long
      val total = pigeonVar_list[1] as Long
      val found = pigeonVar_list[2] as Long
      val done = pigeonVar_list[3] as Boolean
      return CompareProgress(scanned, total, found, done)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      scanned,
      total,
      found,
      done,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompareProgress
    return CompareApiPigeonUtils.deepEquals(this.scanned, other.scanned) && CompareApiPigeonUtils.deepEquals(this.total, other.total) && CompareApiPigeonUtils.deepEquals(this.found, other.found) && CompareApiPigeonUtils.deepEquals(this.done, other.done)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.scanned)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.total)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.found)
    result = 31 * result + CompareApiPigeonUtils.deepHash(this.done)
    return result
  }
  override fun toString(): String {
    return "CompareProgress(scanned=$scanned, total=$total, found=$found, done=$done)"
  }
}
private open class CompareApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompareGroup.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          BlurredImage.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompareResult.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompareProgress.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is CompareGroup -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is BlurredImage -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is CompareResult -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is CompareProgress -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface CompareHostApi {
  /**
   * Decodes every image and computes all three answers in one pass.
   *
   * [blurThreshold] is passed in so the line between soft and sharp can move
   * without a native change. Around 100 is a reasonable start on a 256 pixel
   * working size; below 50 catches only the truly ruined.
   */
  fun scan(maxImages: Long, blurThreshold: Double, callback: (Result<CompareResult>) -> Unit)
  /**
   * Stops the scan. Whatever has been compared so far is returned by the call
   * still in flight, with cancelled true.
   */
  fun cancel(callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by CompareHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      CompareApiPigeonCodec()
    }
    /** Sets up an instance of `CompareHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: CompareHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompareHostApi.scan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val maxImagesArg = args[0] as Long
            val blurThresholdArg = args[1] as Double
            api.scan(maxImagesArg, blurThresholdArg) { result: Result<CompareResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompareHostApi.cancel$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cancel{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompareApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(CompareApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Progress, pushed while a scan runs.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class CompareFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by CompareFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      CompareApiPigeonCodec()
    }
  }
  fun onCompareProgress(progressArg: CompareProgress, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.g_recovery.CompareFlutterApi.onCompareProgress$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(progressArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(CompareApiPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
