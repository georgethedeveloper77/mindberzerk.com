// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.messages

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object MessagesApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * Whether capture is possible and running. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MessageCapture (
  /**
   * Whether notification access has been granted in system settings.
   *
   * The only thing standing between off and on. There is no runtime dialog for
   * this permission: it is a settings screen the user has to visit, which is
   * why the app has to check it on resume rather than await a result.
   */
  val listenerEnabled: Boolean,
  /**
   * Whether the user has also switched capture on inside this app.
   *
   * Separate from [listenerEnabled] on purpose. Granting the system permission
   * and choosing to archive are two decisions, and collapsing them would mean
   * a user who granted access for one reason silently starts recording every
   * message they receive.
   */
  val capturing: Boolean,
  val messageCount: Long,
  val conversationCount: Long,
  /**
   * When capture began, epoch milliseconds, or null if it never has.
   *
   * The honest edge of the archive. Everything older than this is not missing,
   * it never existed as far as this app is concerned, and the UI says so
   * instead of showing an empty list.
   */
  val since: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MessageCapture {
      val listenerEnabled = pigeonVar_list[0] as Boolean
      val capturing = pigeonVar_list[1] as Boolean
      val messageCount = pigeonVar_list[2] as Long
      val conversationCount = pigeonVar_list[3] as Long
      val since = pigeonVar_list[4] as Long?
      return MessageCapture(listenerEnabled, capturing, messageCount, conversationCount, since)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      listenerEnabled,
      capturing,
      messageCount,
      conversationCount,
      since,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MessageCapture
    return MessagesApiPigeonUtils.deepEquals(this.listenerEnabled, other.listenerEnabled) && MessagesApiPigeonUtils.deepEquals(this.capturing, other.capturing) && MessagesApiPigeonUtils.deepEquals(this.messageCount, other.messageCount) && MessagesApiPigeonUtils.deepEquals(this.conversationCount, other.conversationCount) && MessagesApiPigeonUtils.deepEquals(this.since, other.since)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.listenerEnabled)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.capturing)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.messageCount)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.conversationCount)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.since)
    return result
  }
  override fun toString(): String {
    return "MessageCapture(listenerEnabled=$listenerEnabled, capturing=$capturing, messageCount=$messageCount, conversationCount=$conversationCount, since=$since)"
  }
}

/**
 * One captured message. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ArchivedMessage (
  val messageId: String,
  /**
   * The app that posted it, such as com.whatsapp. Kept raw so a future filter
   * can group by app without re-deriving it from a display name.
   */
  val packageName: String,
  val appLabel: String,
  /**
   * The chat or group title as the notification carried it. Null when the
   * posting app gave no title, which happens on a first message from an
   * unsaved number.
   */
  val conversation: String? = null,
  val sender: String? = null,
  val text: String,
  val postedAtMillis: Long,
  /**
   * When the notification was taken away, epoch milliseconds, or null while it
   * is still showing.
   *
   * NOT proof of deletion. A notification is removed when the user opens the
   * chat, swipes it away, or when the sender deletes the message, and the
   * system does not say which. The UI must not label this "deleted".
   */
  val removedAtMillis: Long? = null,
  /**
   * True when a later notification for the same conversation replaced this
   * text with different text.
   *
   * The closest thing to real evidence of a deletion this approach can offer:
   * WhatsApp replaces the message with "This message was deleted", which
   * arrives as an update to the same notification, so the ORIGINAL text is
   * still here and the replacement is what everyone else sees.
   */
  val edited: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ArchivedMessage {
      val messageId = pigeonVar_list[0] as String
      val packageName = pigeonVar_list[1] as String
      val appLabel = pigeonVar_list[2] as String
      val conversation = pigeonVar_list[3] as String?
      val sender = pigeonVar_list[4] as String?
      val text = pigeonVar_list[5] as String
      val postedAtMillis = pigeonVar_list[6] as Long
      val removedAtMillis = pigeonVar_list[7] as Long?
      val edited = pigeonVar_list[8] as Boolean
      return ArchivedMessage(messageId, packageName, appLabel, conversation, sender, text, postedAtMillis, removedAtMillis, edited)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      messageId,
      packageName,
      appLabel,
      conversation,
      sender,
      text,
      postedAtMillis,
      removedAtMillis,
      edited,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ArchivedMessage
    return MessagesApiPigeonUtils.deepEquals(this.messageId, other.messageId) && MessagesApiPigeonUtils.deepEquals(this.packageName, other.packageName) && MessagesApiPigeonUtils.deepEquals(this.appLabel, other.appLabel) && MessagesApiPigeonUtils.deepEquals(this.conversation, other.conversation) && MessagesApiPigeonUtils.deepEquals(this.sender, other.sender) && MessagesApiPigeonUtils.deepEquals(this.text, other.text) && MessagesApiPigeonUtils.deepEquals(this.postedAtMillis, other.postedAtMillis) && MessagesApiPigeonUtils.deepEquals(this.removedAtMillis, other.removedAtMillis) && MessagesApiPigeonUtils.deepEquals(this.edited, other.edited)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.messageId)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.packageName)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.appLabel)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.conversation)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.sender)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.text)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.postedAtMillis)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.removedAtMillis)
    result = 31 * result + MessagesApiPigeonUtils.deepHash(this.edited)
    return result
  }
  override fun toString(): String {
    return "ArchivedMessage(messageId=$messageId, packageName=$packageName, appLabel=$appLabel, conversation=$conversation, sender=$sender, text=$text, postedAtMillis=$postedAtMillis, removedAtMillis=$removedAtMillis, edited=$edited)"
  }
}
private open class MessagesApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MessageCapture.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ArchivedMessage.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is MessageCapture -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is ArchivedMessage -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface MessagesHostApi {
  /** Whether capture is possible, and how much has been kept. */
  fun captureState(callback: (Result<MessageCapture>) -> Unit)
  /**
   * Opens the system notification access screen.
   *
   * Returns whether a screen could be opened at all. Some heavily modified ROMs
   * hide it, and an app that silently does nothing on those is worse than one
   * that says it could not get there.
   */
  fun openListenerSettings(callback: (Result<Boolean>) -> Unit)
  /**
   * Turns archiving on or off inside this app.
   *
   * Off does not delete what has been kept. Clearing is [clear], deliberately a
   * separate and explicit act.
   */
  fun setCapturing(value: Boolean, callback: (Result<Unit>) -> Unit)
  /** Newest first. [conversation] null means every chat. */
  fun messages(conversation: String?, limit: Long, callback: (Result<List<ArchivedMessage>>) -> Unit)
  /** Every conversation seen, newest activity first. */
  fun conversations(callback: (Result<List<String>>) -> Unit)
  /** Deletes the archive. */
  fun clear(callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by MessagesHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesApiPigeonCodec()
    }
    /** Sets up an instance of `MessagesHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: MessagesHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.captureState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.captureState{ result: Result<MessageCapture> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.openListenerSettings$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.openListenerSettings{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.setCapturing$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val valueArg = args[0] as Boolean
            api.setCapturing(valueArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.messages$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val conversationArg = args[0] as String?
            val limitArg = args[1] as Long
            api.messages(conversationArg, limitArg) { result: Result<List<ArchivedMessage>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.conversations$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.conversations{ result: Result<List<String>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.MessagesHostApi.clear$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.clear{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
