// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.compress

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object CompressApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * A file worth looking at. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressCandidate (
  val fileId: String,
  val name: String,
  val sizeBytes: Long,
  /**
   * Zero when MediaStore does not know, which happens on files written by
   * apps that never told it. The picture still compresses; only the row on
   * screen is thinner.
   */
  val widthPx: Long,
  val heightPx: Long,
  val mimeType: String,
  /**
   * "screenshot" | "photo". Null from an older native layer.
   *
   * Separate categories because they are separate operations, not a filter on
   * one. A photo is re-encoded as JPEG and loses a little quality for a lot of
   * space. A screenshot is re-encoded as lossless WebP and loses nothing at
   * all, because it is text and flat colour, which is exactly the content JPEG
   * is worst at.
   */
  val kind: String? = null,
  /**
   * When the picture was taken, for the sort and the details row.
   *
   * Without it the shared sort control offers newest and oldest against a
   * field that does not exist, which is a list ordered by nothing wearing the
   * label of a list ordered by date.
   */
  val dateMillis: Long? = null,
  /** The folder it lives in, for details mode. "DCIM/Camera" and the like. */
  val folder: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressCandidate {
      val fileId = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val sizeBytes = pigeonVar_list[2] as Long
      val widthPx = pigeonVar_list[3] as Long
      val heightPx = pigeonVar_list[4] as Long
      val mimeType = pigeonVar_list[5] as String
      val kind = pigeonVar_list[6] as String?
      val dateMillis = pigeonVar_list[7] as Long?
      val folder = pigeonVar_list[8] as String?
      return CompressCandidate(fileId, name, sizeBytes, widthPx, heightPx, mimeType, kind, dateMillis, folder)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      name,
      sizeBytes,
      widthPx,
      heightPx,
      mimeType,
      kind,
      dateMillis,
      folder,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressCandidate
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.name, other.name) && CompressApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes) && CompressApiPigeonUtils.deepEquals(this.widthPx, other.widthPx) && CompressApiPigeonUtils.deepEquals(this.heightPx, other.heightPx) && CompressApiPigeonUtils.deepEquals(this.mimeType, other.mimeType) && CompressApiPigeonUtils.deepEquals(this.kind, other.kind) && CompressApiPigeonUtils.deepEquals(this.dateMillis, other.dateMillis) && CompressApiPigeonUtils.deepEquals(this.folder, other.folder)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.name)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.sizeBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.widthPx)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.heightPx)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.mimeType)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.kind)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.dateMillis)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.folder)
    return result
  }
  override fun toString(): String {
    return "CompressCandidate(fileId=$fileId, name=$name, sizeBytes=$sizeBytes, widthPx=$widthPx, heightPx=$heightPx, mimeType=$mimeType, kind=$kind, dateMillis=$dateMillis, folder=$folder)"
  }
}

/**
 * What a re-encode would actually produce. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressPreview (
  val fileId: String,
  val originalBytes: Long,
  /**
   * MEASURED, not predicted. The encode really happened, into memory.
   *
   * Can be LARGER than the original, and often is for a photo already saved at
   * a lower quality than the one being applied. The UI drops those rather than
   * offering to make a file bigger.
   */
  val newBytes: Long,
  val quality: Long,
  /**
   * What the re-encode produced, which is NOT always the source type.
   *
   * A PNG becomes WebP. The screen has to be able to say so, because "your
   * screenshots will become .webp files" is a thing a person may object to and
   * must not discover afterwards.
   */
  val outputMime: String? = null,
  /**
   * Whether a single pixel changed.
   *
   * Load bearing on screen. A lossless saving can be offered without any
   * warning at all, and it also justifies a far lower worth-it threshold: the
   * 20 percent floor exists to stop trading permanent quality for a trivial
   * gain, and with nothing being traded there is nothing to protect against.
   */
  val lossless: Boolean? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressPreview {
      val fileId = pigeonVar_list[0] as String
      val originalBytes = pigeonVar_list[1] as Long
      val newBytes = pigeonVar_list[2] as Long
      val quality = pigeonVar_list[3] as Long
      val outputMime = pigeonVar_list[4] as String?
      val lossless = pigeonVar_list[5] as Boolean?
      return CompressPreview(fileId, originalBytes, newBytes, quality, outputMime, lossless)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      originalBytes,
      newBytes,
      quality,
      outputMime,
      lossless,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressPreview
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.originalBytes, other.originalBytes) && CompressApiPigeonUtils.deepEquals(this.newBytes, other.newBytes) && CompressApiPigeonUtils.deepEquals(this.quality, other.quality) && CompressApiPigeonUtils.deepEquals(this.outputMime, other.outputMime) && CompressApiPigeonUtils.deepEquals(this.lossless, other.lossless)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.originalBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.newBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.quality)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.outputMime)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.lossless)
    return result
  }
  override fun toString(): String {
    return "CompressPreview(fileId=$fileId, originalBytes=$originalBytes, newBytes=$newBytes, quality=$quality, outputMime=$outputMime, lossless=$lossless)"
  }
}

/**
 * What happened to one file. Codec 131.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressOutcome (
  val fileId: String,
  /**
   * "replaced" | "skipped" | "failed"
   *
   * Skipped means the new file was not smaller enough to be worth it, which is
   * a success for the user and a non event for the app.
   */
  val status: String,
  val savedBytes: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressOutcome {
      val fileId = pigeonVar_list[0] as String
      val status = pigeonVar_list[1] as String
      val savedBytes = pigeonVar_list[2] as Long
      return CompressOutcome(fileId, status, savedBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      status,
      savedBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressOutcome
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.status, other.status) && CompressApiPigeonUtils.deepEquals(this.savedBytes, other.savedBytes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.status)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.savedBytes)
    return result
  }
  override fun toString(): String {
    return "CompressOutcome(fileId=$fileId, status=$status, savedBytes=$savedBytes)"
  }
}

/**
 * How a run is going. Codec 132.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressProgress (
  val running: Boolean,
  val done: Long,
  val total: Long,
  val savedBytes: Long,
  val currentName: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressProgress {
      val running = pigeonVar_list[0] as Boolean
      val done = pigeonVar_list[1] as Long
      val total = pigeonVar_list[2] as Long
      val savedBytes = pigeonVar_list[3] as Long
      val currentName = pigeonVar_list[4] as String?
      return CompressProgress(running, done, total, savedBytes, currentName)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      running,
      done,
      total,
      savedBytes,
      currentName,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressProgress
    return CompressApiPigeonUtils.deepEquals(this.running, other.running) && CompressApiPigeonUtils.deepEquals(this.done, other.done) && CompressApiPigeonUtils.deepEquals(this.total, other.total) && CompressApiPigeonUtils.deepEquals(this.savedBytes, other.savedBytes) && CompressApiPigeonUtils.deepEquals(this.currentName, other.currentName)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.running)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.done)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.total)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.savedBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.currentName)
    return result
  }
  override fun toString(): String {
    return "CompressProgress(running=$running, done=$done, total=$total, savedBytes=$savedBytes, currentName=$currentName)"
  }
}

/**
 * Counts and bytes, with nothing encoded. Codec 133.
 *
 * ─── THE ONE READING IN THIS API THAT IS FREE ────────────────────────────────
 *
 * Everything else here measures by re-encoding, which is the whole point and
 * is also why none of it can run unasked. But counting the screenshots and
 * adding up their sizes is a single MediaStore query, instant and cheap.
 *
 * That is enough for the storage tab to say "1,204 screenshots, 8.7 GB" before
 * anything has been scanned, which is a real fact rather than an empty
 * invitation, and it still promises no saving it has not measured.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressSummary (
  val screenshotCount: Long,
  val screenshotBytes: Long,
  val photoCount: Long,
  val photoBytes: Long,
  /**
   * Clips whose codec makes them worth re-encoding. Null from older native.
   *
   * Counted the same way as the others and just as cheaply: MediaStore knows
   * the size, and the codec test is a header read rather than a decode.
   */
  val videoCount: Long? = null,
  val videoBytes: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressSummary {
      val screenshotCount = pigeonVar_list[0] as Long
      val screenshotBytes = pigeonVar_list[1] as Long
      val photoCount = pigeonVar_list[2] as Long
      val photoBytes = pigeonVar_list[3] as Long
      val videoCount = pigeonVar_list[4] as Long?
      val videoBytes = pigeonVar_list[5] as Long?
      return CompressSummary(screenshotCount, screenshotBytes, photoCount, photoBytes, videoCount, videoBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      screenshotCount,
      screenshotBytes,
      photoCount,
      photoBytes,
      videoCount,
      videoBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressSummary
    return CompressApiPigeonUtils.deepEquals(this.screenshotCount, other.screenshotCount) && CompressApiPigeonUtils.deepEquals(this.screenshotBytes, other.screenshotBytes) && CompressApiPigeonUtils.deepEquals(this.photoCount, other.photoCount) && CompressApiPigeonUtils.deepEquals(this.photoBytes, other.photoBytes) && CompressApiPigeonUtils.deepEquals(this.videoCount, other.videoCount) && CompressApiPigeonUtils.deepEquals(this.videoBytes, other.videoBytes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.screenshotCount)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.screenshotBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.photoCount)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.photoBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.videoCount)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.videoBytes)
    return result
  }
  override fun toString(): String {
    return "CompressSummary(screenshotCount=$screenshotCount, screenshotBytes=$screenshotBytes, photoCount=$photoCount, photoBytes=$photoBytes, videoCount=$videoCount, videoBytes=$videoBytes)"
  }
}

/**
 * Both versions of one picture, for looking at. Codec 134.
 *
 * ─── WHY BOTH BUFFERS AND NOT JUST THE NEW ONE ───────────────────────────────
 *
 * The screen has to show the original too, and the obvious shortcut is to pull
 * it from the thumbnailer. That would be dishonest: the thumbnailer returns a
 * scaled decode, so the original would arrive softer than the full resolution
 * re-encode sitting beside it, and the compressed version would look BETTER
 * than the file it came from.
 *
 * A comparison that flatters the thing being sold is worse than no comparison.
 * Both sides come back at their true size, from one decode, and the difference
 * on screen is only ever the difference the encoder made.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressComparison (
  val fileId: String,
  /** The file exactly as it is on disk. */
  val original: ByteArray,
  /**
   * What the re-encode produced. Identical bytes are still returned when
   * [lossless] is true, so the screen never has to special case a null.
   */
  val encoded: ByteArray,
  val originalBytes: Long,
  val newBytes: Long,
  /**
   * True for a screenshot. The viewer shows no comparison at all in that case,
   * because a control that swaps an image for the same image is a control that
   * teaches the user their eyes are wrong.
   */
  val lossless: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressComparison {
      val fileId = pigeonVar_list[0] as String
      val original = pigeonVar_list[1] as ByteArray
      val encoded = pigeonVar_list[2] as ByteArray
      val originalBytes = pigeonVar_list[3] as Long
      val newBytes = pigeonVar_list[4] as Long
      val lossless = pigeonVar_list[5] as Boolean
      return CompressComparison(fileId, original, encoded, originalBytes, newBytes, lossless)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      original,
      encoded,
      originalBytes,
      newBytes,
      lossless,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressComparison
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.original, other.original) && CompressApiPigeonUtils.deepEquals(this.encoded, other.encoded) && CompressApiPigeonUtils.deepEquals(this.originalBytes, other.originalBytes) && CompressApiPigeonUtils.deepEquals(this.newBytes, other.newBytes) && CompressApiPigeonUtils.deepEquals(this.lossless, other.lossless)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.original)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.encoded)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.originalBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.newBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.lossless)
    return result
  }
  override fun toString(): String {
    return "CompressComparison(fileId=$fileId, original=${original.contentToString()}, encoded=${encoded.contentToString()}, originalBytes=$originalBytes, newBytes=$newBytes, lossless=$lossless)"
  }
}

/**
 * One file this app has already made smaller. Codec 135.
 *
 * ─── A RECORD, NOT A FOLDER ──────────────────────────────────────────────────
 *
 * Nothing moves on disk. The picture stays exactly where it was, in the
 * gallery, next to everything else. Moving people's photographs into an app
 * directory is what cleaner apps do and it is how photographs get lost, along
 * with every other app's ability to find them.
 *
 * ─── AND IT IS WHAT STOPS THE LOOP ───────────────────────────────────────────
 *
 * The output of a compression is itself a large JPEG, so without this it comes
 * straight back as a candidate and the app offers to compress the thing it
 * just made. That is why a freshly compressed library fills with rows reading
 * "no gain": they were all correct, and all pointless.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CompressedEntry (
  /**
   * The id of the file that was WRITTEN, not the one that was trashed.
   *
   * It is the one that still exists, so it is the one the candidate query has
   * to exclude and the one a thumbnail can be drawn from.
   */
  val fileId: String,
  val name: String,
  val originalBytes: Long,
  val newBytes: Long,
  val whenMillis: Long,
  /**
   * Whether a pixel changed. "lossless" and "quality 85" are different
   * promises, and a week later nobody remembers which a given file got.
   */
  val lossless: Boolean,
  /**
   * Meaningless when [lossless]. Recorded anyway, because a file that turns
   * out badly is evidence about a setting.
   */
  val quality: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CompressedEntry {
      val fileId = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val originalBytes = pigeonVar_list[2] as Long
      val newBytes = pigeonVar_list[3] as Long
      val whenMillis = pigeonVar_list[4] as Long
      val lossless = pigeonVar_list[5] as Boolean
      val quality = pigeonVar_list[6] as Long
      return CompressedEntry(fileId, name, originalBytes, newBytes, whenMillis, lossless, quality)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      name,
      originalBytes,
      newBytes,
      whenMillis,
      lossless,
      quality,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CompressedEntry
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.name, other.name) && CompressApiPigeonUtils.deepEquals(this.originalBytes, other.originalBytes) && CompressApiPigeonUtils.deepEquals(this.newBytes, other.newBytes) && CompressApiPigeonUtils.deepEquals(this.whenMillis, other.whenMillis) && CompressApiPigeonUtils.deepEquals(this.lossless, other.lossless) && CompressApiPigeonUtils.deepEquals(this.quality, other.quality)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.name)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.originalBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.newBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.whenMillis)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.lossless)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.quality)
    return result
  }
  override fun toString(): String {
    return "CompressedEntry(fileId=$fileId, name=$name, originalBytes=$originalBytes, newBytes=$newBytes, whenMillis=$whenMillis, lossless=$lossless, quality=$quality)"
  }
}

/**
 * A clip, and whether re-encoding it would achieve anything. Codec 136.
 *
 * ─── THE CODEC DECIDES, NOT THE SIZE ─────────────────────────────────────────
 *
 * H.264 to HEVC reliably clears 40 percent because H.264 is a twenty year old
 * codec every phone still records in for compatibility. HEVC to HEVC clears
 * almost nothing and costs ten minutes of full rate encoding and a noticeable
 * bite out of the battery.
 *
 * A cleaner that offers every video by size will therefore spend the longest
 * it has ever made someone wait, on the file with the least to gain. So the
 * verdict is read from the track header before anything is offered, and the
 * ones that cannot be helped are shown saying so rather than hidden.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class VideoCandidate (
  val fileId: String,
  val name: String,
  val sizeBytes: Long,
  val widthPx: Long,
  val heightPx: Long,
  val durationMillis: Long,
  /**
   * "h264" | "hevc" | "vp9" | "av1" | "mpeg4" | "unknown"
   *
   * Normalised from the track mime type, so the screen can print something a
   * person recognises rather than "video/x-vnd.on2.vp9".
   */
  val codec: String,
  /** Bits per second, from the container. Zero when it does not say. */
  val bitrate: Long,
  val eligible: Boolean,
  /**
   * Why not, in words, and null when it is. "Already HEVC", "Too short".
   *
   * A sentence rather than a code, because unlike the server probe there is no
   * button attached to any of these: they are all the same outcome, which is
   * that nothing will be done to this file.
   */
  val reason: String? = null,
  val dateMillis: Long? = null,
  val folder: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): VideoCandidate {
      val fileId = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val sizeBytes = pigeonVar_list[2] as Long
      val widthPx = pigeonVar_list[3] as Long
      val heightPx = pigeonVar_list[4] as Long
      val durationMillis = pigeonVar_list[5] as Long
      val codec = pigeonVar_list[6] as String
      val bitrate = pigeonVar_list[7] as Long
      val eligible = pigeonVar_list[8] as Boolean
      val reason = pigeonVar_list[9] as String?
      val dateMillis = pigeonVar_list[10] as Long?
      val folder = pigeonVar_list[11] as String?
      return VideoCandidate(fileId, name, sizeBytes, widthPx, heightPx, durationMillis, codec, bitrate, eligible, reason, dateMillis, folder)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      name,
      sizeBytes,
      widthPx,
      heightPx,
      durationMillis,
      codec,
      bitrate,
      eligible,
      reason,
      dateMillis,
      folder,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as VideoCandidate
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.name, other.name) && CompressApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes) && CompressApiPigeonUtils.deepEquals(this.widthPx, other.widthPx) && CompressApiPigeonUtils.deepEquals(this.heightPx, other.heightPx) && CompressApiPigeonUtils.deepEquals(this.durationMillis, other.durationMillis) && CompressApiPigeonUtils.deepEquals(this.codec, other.codec) && CompressApiPigeonUtils.deepEquals(this.bitrate, other.bitrate) && CompressApiPigeonUtils.deepEquals(this.eligible, other.eligible) && CompressApiPigeonUtils.deepEquals(this.reason, other.reason) && CompressApiPigeonUtils.deepEquals(this.dateMillis, other.dateMillis) && CompressApiPigeonUtils.deepEquals(this.folder, other.folder)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.name)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.sizeBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.widthPx)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.heightPx)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.durationMillis)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.codec)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.bitrate)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.eligible)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.reason)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.dateMillis)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.folder)
    return result
  }
  override fun toString(): String {
    return "VideoCandidate(fileId=$fileId, name=$name, sizeBytes=$sizeBytes, widthPx=$widthPx, heightPx=$heightPx, durationMillis=$durationMillis, codec=$codec, bitrate=$bitrate, eligible=$eligible, reason=$reason, dateMillis=$dateMillis, folder=$folder)"
  }
}

/**
 * What a real sample of one clip suggests. Codec 137.
 *
 * ─── AN ESTIMATE, AND THE FIRST ONE IN THIS API ──────────────────────────────
 *
 * Every figure elsewhere here is measured by encoding the whole file, which is
 * possible at four megabytes and absurd at two gigabytes, where measuring IS
 * the job. So a short slice is really encoded and the rest is extrapolated
 * from it.
 *
 * That is a forecast, not a measurement, and it must never wear the same word
 * on screen as the photo figures. It is also honest in a way a bitrate formula
 * is not: it is this encoder, on this phone, on these pixels.
 *
 * ─── AND IT CAN BE WRONG IN A KNOWN DIRECTION ────────────────────────────────
 *
 * A clip that opens on a still shot and later pans across a market will
 * compress better in its first seconds than across its length, so the estimate
 * runs high. [sampledMillis] is reported so the screen can say what it was
 * based on rather than presenting a number from nowhere.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class VideoEstimate (
  val fileId: String,
  val originalBytes: Long,
  /**
   * Extrapolated. Can exceed [originalBytes] on a clip already well encoded,
   * and the UI drops those rather than offering to make a file bigger.
   */
  val estimatedBytes: Long,
  /** How much was actually encoded to reach it. */
  val sampledMillis: Long,
  /** "same" or "smaller". */
  val preset: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): VideoEstimate {
      val fileId = pigeonVar_list[0] as String
      val originalBytes = pigeonVar_list[1] as Long
      val estimatedBytes = pigeonVar_list[2] as Long
      val sampledMillis = pigeonVar_list[3] as Long
      val preset = pigeonVar_list[4] as String
      return VideoEstimate(fileId, originalBytes, estimatedBytes, sampledMillis, preset)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      originalBytes,
      estimatedBytes,
      sampledMillis,
      preset,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as VideoEstimate
    return CompressApiPigeonUtils.deepEquals(this.fileId, other.fileId) && CompressApiPigeonUtils.deepEquals(this.originalBytes, other.originalBytes) && CompressApiPigeonUtils.deepEquals(this.estimatedBytes, other.estimatedBytes) && CompressApiPigeonUtils.deepEquals(this.sampledMillis, other.sampledMillis) && CompressApiPigeonUtils.deepEquals(this.preset, other.preset)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.originalBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.estimatedBytes)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.sampledMillis)
    result = 31 * result + CompressApiPigeonUtils.deepHash(this.preset)
    return result
  }
  override fun toString(): String {
    return "VideoEstimate(fileId=$fileId, originalBytes=$originalBytes, estimatedBytes=$estimatedBytes, sampledMillis=$sampledMillis, preset=$preset)"
  }
}
private open class CompressApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressCandidate.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressPreview.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressOutcome.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressProgress.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressSummary.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressComparison.fromList(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CompressedEntry.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          VideoCandidate.fromList(it)
        }
      }
      137.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          VideoEstimate.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is CompressCandidate -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is CompressPreview -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is CompressOutcome -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is CompressProgress -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is CompressSummary -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is CompressComparison -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      is CompressedEntry -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is VideoCandidate -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      is VideoEstimate -> {
        stream.write(137)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface CompressHostApi {
  /**
   * Counts and total bytes per category. No encoding, so it is safe to call
   * on a screen the user did not ask to scan.
   */
  fun summary(minBytes: Long, callback: (Result<CompressSummary>) -> Unit)
  /**
   * Images large enough to be worth re-encoding, largest first.
   *
   * [kind] is "screenshot", "photo", or "all". Filtered in the query rather
   * than in Dart, because the alternative is fetching three thousand rows to
   * display twelve.
   */
  fun candidates(kind: String, minBytes: Long, limit: Long, callback: (Result<List<CompressCandidate>>) -> Unit)
  /**
   * Re-encodes into memory and reports the real size.
   *
   * Slow: a full decode and encode per file. The caller previews only what is
   * on screen, never the whole list.
   * [quality] applies to photos only. A screenshot is encoded losslessly and
   * ignores it, which is why the output format is reported back rather than
   * assumed from what was asked for.
   */
  fun preview(fileIds: List<String>, quality: Long, callback: (Result<List<CompressPreview>>) -> Unit)
  /**
   * Replaces the originals, keeping them in the trash.
   *
   * ─── THE ORIGINAL IS TRASHED, NOT DELETED ────────────────────────────────
   *
   * A re-encode is lossy and irreversible. The user gets thirty days to notice
   * that a picture they cared about now has artefacts in the sky, which is the
   * only safe way to offer this at all.
   */
  fun compress(fileIds: List<String>, quality: Long, callback: (Result<List<CompressOutcome>>) -> Unit)
  /**
   * Remembers files that were measured and cannot be improved.
   *
   * ─── THE OTHER HALF OF THE LOOP ──────────────────────────────────────────
   *
   * Excluding this app's own output stopped it offering to re-compress what it
   * wrote. It did nothing about the far larger group: photographs that were
   * already saved efficiently and gain nothing at all. Those are measured,
   * shown as "no gain", left alone by the run, and then measured again from
   * scratch the next time the screen opens, forever.
   *
   * After one run the list is made up entirely of those files, which is
   * indistinguishable from the run having done nothing.
   *
   * Not recorded during [preview], which is speculative and runs on every
   * scroll. Only when the user has actually run a compression and the answer
   * has been acted on.
   */
  fun markNoGain(fileIds: List<String>, callback: (Result<Unit>) -> Unit)
  /**
   * Forgets every no-gain verdict.
   *
   * Called when the quality changes, because the verdict was reached at one
   * setting and does not hold at another: a file that gains nothing at 85 may
   * well gain at 65. Simpler and more obviously correct than storing a quality
   * per file and comparing.
   */
  fun clearNoGain(callback: (Result<Unit>) -> Unit)
  /**
   * What this app has already compressed, newest first.
   *
   * Kept in native rather than in Dart prefs because the candidate query needs
   * it to exclude its own output, and a filter that lived on the far side of
   * the bridge would mean shipping every row across just to drop most of them.
   */
  fun history(limit: Long, callback: (Result<List<CompressedEntry>>) -> Unit)
  /**
   * Every clip, with the ineligible ones included and marked.
   *
   * Included rather than filtered, because a user whose largest video is
   * missing from a list about making files smaller will assume the app failed
   * to see it. Saying "already HEVC" costs one row and answers the question.
   */
  fun videoCandidates(minBytes: Long, limit: Long, callback: (Result<List<VideoCandidate>>) -> Unit)
  /**
   * Encodes a slice of one clip and extrapolates from what it produced.
   *
   * Slow: seconds rather than milliseconds, because it really runs the
   * encoder. The caller estimates what is on screen, one at a time, never the
   * whole list.
   *
   * [preset] is "same" for a codec swap at matched quality, or "smaller" for a
   * reduced bitrate as well. Resolution is never touched by either.
   */
  fun estimateVideo(fileId: String, preset: String, callback: (Result<VideoEstimate?>) -> Unit)
  /**
   * Re-encodes clips and replaces them, keeping the originals in the trash.
   *
   * ─── LONGER THAN ANYTHING ELSE IN THIS APP ───────────────────────────────
   *
   * Minutes per file rather than milliseconds, so it runs behind a foreground
   * service with a notification and can be stopped between files or during
   * one. Progress arrives through the same [progress] call the image path
   * uses, and [cancel] stops both.
   */
  fun compressVideo(fileIds: List<String>, preset: String, callback: (Result<List<CompressOutcome>>) -> Unit)
  /**
   * One file, decoded once, returned as both versions.
   *
   * Deliberately not part of [preview]. That measures a whole selection and
   * must stay cheap per file; this carries megabytes across the bridge and is
   * only ever called for the single picture on screen.
   */
  fun comparison(fileId: String, quality: Long, callback: (Result<CompressComparison?>) -> Unit)
  fun cancel(callback: (Result<Unit>) -> Unit)
  fun progress(callback: (Result<CompressProgress>) -> Unit)

  companion object {
    /** The codec used by CompressHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      CompressApiPigeonCodec()
    }
    /** Sets up an instance of `CompressHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: CompressHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.summary$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val minBytesArg = args[0] as Long
            api.summary(minBytesArg) { result: Result<CompressSummary> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.candidates$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val kindArg = args[0] as String
            val minBytesArg = args[1] as Long
            val limitArg = args[2] as Long
            api.candidates(kindArg, minBytesArg, limitArg) { result: Result<List<CompressCandidate>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.preview$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdsArg = args[0] as List<String>
            val qualityArg = args[1] as Long
            api.preview(fileIdsArg, qualityArg) { result: Result<List<CompressPreview>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.compress$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdsArg = args[0] as List<String>
            val qualityArg = args[1] as Long
            api.compress(fileIdsArg, qualityArg) { result: Result<List<CompressOutcome>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.markNoGain$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdsArg = args[0] as List<String>
            api.markNoGain(fileIdsArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(CompressApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.clearNoGain$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.clearNoGain{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(CompressApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.history$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val limitArg = args[0] as Long
            api.history(limitArg) { result: Result<List<CompressedEntry>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.videoCandidates$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val minBytesArg = args[0] as Long
            val limitArg = args[1] as Long
            api.videoCandidates(minBytesArg, limitArg) { result: Result<List<VideoCandidate>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.estimateVideo$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdArg = args[0] as String
            val presetArg = args[1] as String
            api.estimateVideo(fileIdArg, presetArg) { result: Result<VideoEstimate?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.compressVideo$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdsArg = args[0] as List<String>
            val presetArg = args[1] as String
            api.compressVideo(fileIdsArg, presetArg) { result: Result<List<CompressOutcome>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.comparison$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdArg = args[0] as String
            val qualityArg = args[1] as Long
            api.comparison(fileIdArg, qualityArg) { result: Result<CompressComparison?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.cancel$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cancel{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(CompressApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.CompressHostApi.progress$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.progress{ result: Result<CompressProgress> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(CompressApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(CompressApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
