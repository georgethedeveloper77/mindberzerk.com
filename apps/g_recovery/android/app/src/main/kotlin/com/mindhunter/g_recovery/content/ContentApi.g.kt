// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.content

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object ContentApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * One content pack, as the client sees it. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ContentPackInfo (
  /** "trashmap", "learn-en", "oem-guide". */
  val packId: String,
  /**
   * "registry" for the trashmap, "article" for Learn. A String, because an
   * enum would renumber the schema.
   */
  val packType: String,
  /** What the signed index advertises. 0 when the index has not been read. */
  val version: Long,
  /**
   * What is on disk and verified. 0 when nothing is installed and the app is
   * running on its bundled copy.
   */
  val installedVersion: Long,
  val sizeBytes: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ContentPackInfo {
      val packId = pigeonVar_list[0] as String
      val packType = pigeonVar_list[1] as String
      val version = pigeonVar_list[2] as Long
      val installedVersion = pigeonVar_list[3] as Long
      val sizeBytes = pigeonVar_list[4] as Long
      return ContentPackInfo(packId, packType, version, installedVersion, sizeBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      packId,
      packType,
      version,
      installedVersion,
      sizeBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ContentPackInfo
    return ContentApiPigeonUtils.deepEquals(this.packId, other.packId) && ContentApiPigeonUtils.deepEquals(this.packType, other.packType) && ContentApiPigeonUtils.deepEquals(this.version, other.version) && ContentApiPigeonUtils.deepEquals(this.installedVersion, other.installedVersion) && ContentApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.packId)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.packType)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.version)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.installedVersion)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.sizeBytes)
    return result
  }
  override fun toString(): String {
    return "ContentPackInfo(packId=$packId, packType=$packType, version=$version, installedVersion=$installedVersion, sizeBytes=$sizeBytes)"
  }
}

/**
 * The outcome of one sync. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ContentSyncResult (
  /**
   * "updated" | "upToDate" | "offline" | "rejected" | "failed"
   *
   * A RESULT OBJECT RATHER THAN A THROWN ERROR, and the statuses are not
   * interchangeable. `offline` is the ordinary case on a phone in a lift and
   * deserves silence. `rejected` means a signature failed, which should be
   * reported and NEVER retried, because retrying a bad signature just fetches
   * the same bad bytes again. Only `failed` is worth a retry button.
   */
  val status: String,
  val detail: String,
  /** True when something on disk changed and callers should re-read. */
  val changed: Boolean,
  val packs: List<ContentPackInfo>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ContentSyncResult {
      val status = pigeonVar_list[0] as String
      val detail = pigeonVar_list[1] as String
      val changed = pigeonVar_list[2] as Boolean
      val packs = pigeonVar_list[3] as List<ContentPackInfo>
      return ContentSyncResult(status, detail, changed, packs)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      status,
      detail,
      changed,
      packs,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ContentSyncResult
    return ContentApiPigeonUtils.deepEquals(this.status, other.status) && ContentApiPigeonUtils.deepEquals(this.detail, other.detail) && ContentApiPigeonUtils.deepEquals(this.changed, other.changed) && ContentApiPigeonUtils.deepEquals(this.packs, other.packs)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.status)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.detail)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.changed)
    result = 31 * result + ContentApiPigeonUtils.deepHash(this.packs)
    return result
  }
  override fun toString(): String {
    return "ContentSyncResult(status=$status, detail=$detail, changed=$changed, packs=$packs)"
  }
}
private open class ContentApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ContentPackInfo.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ContentSyncResult.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is ContentPackInfo -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is ContentSyncResult -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface ContentHostApi {
  /**
   * The CDN root, for example the g-recovery folder under the Mindberzerk CDN.
   *
   * Pushed from Dart so the host can move without a Kotlin change, exactly as
   * the launcher does it. Validated natively before use: https only, no path
   * traversal. A base URL that arrives from anywhere other than a constant is
   * still a value to check, not a value to trust.
   */
  fun setBaseUrl(url: String, callback: (Result<Unit>) -> Unit)
  /**
   * Fetch the signed index, verify it, and install any pack that is newer.
   *
   * Safe to call on every launch: the index request carries an ETag, so the
   * common case is a 304 with no body.
   */
  fun sync(callback: (Result<ContentSyncResult>) -> Unit)
  /**
   * The verified JSON for a content id, or null when nothing is installed.
   *
   * Null is the ORDINARY answer, not an error. It means the app should use its
   * bundled copy, which is what happens on first launch and whenever the CDN
   * is unreachable.
   */
  fun readContent(packId: String, callback: (Result<String?>) -> Unit)
  /** What is installed right now, without touching the network. */
  fun packs(callback: (Result<List<ContentPackInfo>>) -> Unit)

  companion object {
    /** The codec used by ContentHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      ContentApiPigeonCodec()
    }
    /** Sets up an instance of `ContentHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: ContentHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ContentHostApi.setBaseUrl$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val urlArg = args[0] as String
            api.setBaseUrl(urlArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ContentApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ContentApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ContentHostApi.sync$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.sync{ result: Result<ContentSyncResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ContentApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ContentApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ContentHostApi.readContent$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val packIdArg = args[0] as String
            api.readContent(packIdArg) { result: Result<String?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ContentApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ContentApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ContentHostApi.packs$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.packs{ result: Result<List<ContentPackInfo>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ContentApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ContentApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
