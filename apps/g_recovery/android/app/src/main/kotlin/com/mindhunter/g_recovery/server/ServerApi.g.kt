// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.server

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object ServerApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * A server the user has told us about. Codec 129.
 *
 * ─── NO PASSWORD FIELD, AND THAT IS DELIBERATE ───────────────────────────────
 *
 * The password crosses this bridge exactly once, as an argument to [save], and
 * is never handed back. Native keeps it in EncryptedSharedPreferences and Dart
 * has no way to read it again.
 *
 * If it were a field here it would be in every state read, sitting in Dart heap
 * for the life of the app and printable by any careless log line. There is no
 * screen that needs to display it: a password field shows dots, and re-entry is
 * safer than recall.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ServerConfig (
  val id: String,
  /**
   * "smb" | "sftp" | "webdav"
   *
   * A String rather than an enum, matching every other schema here: an enum
   * numbers before classes and adding a fourth protocol would renumber all
   * five.
   */
  val protocol: String,
  /**
   * What the user calls it. Defaults to the host, because a person with two
   * servers needs to tell them apart and an IP address does not help.
   */
  val label: String,
  val host: String,
  val port: Long,
  /** SMB only. The share name, which SFTP and WebDAV do not have. */
  val share: String? = null,
  val username: String,
  /** Folder on the server. Everything is written under here. */
  val remotePath: String,
  /**
   * OFF BY DEFAULT, and free rather than sold.
   *
   * The point of a home server is that it is yours: readable photos on your own
   * NAS can be opened by your desktop, served by Plex, and backed up again by
   * whatever you already run. Encrypting turns your machine into a blob store
   * only this app can read, which is the property people dislike about cloud
   * services.
   *
   * It earns its place for a server you do not fully control, such as a VPS,
   * which is why it is a switch with that reason attached rather than a
   * default.
   */
  val encrypt: Boolean,
  val wifiOnly: Boolean,
  val whileCharging: Boolean,
  /**
   * Unattended nightly runs. The Pro half of this feature, because it sells
   * labour rather than capability: connecting, copying and reclaiming all work
   * free, by hand.
   */
  val scheduled: Boolean,
  /**
   * HTTPS rather than plain HTTP. WebDAV only.
   *
   * Null means HTTPS, because null is what an SMB record and every server
   * saved before this field existed will report, and the safe reading of "not
   * stated" is the encrypted one. Only an explicit false sends credentials in
   * the clear.
   */
  val secure: Boolean? = null,
  /**
   * The DAV root, taken from the address the user pasted. WebDAV only.
   *
   * Separate from [remotePath] because they answer different questions.
   * This is where the server's WebDAV endpoint lives and the user copies it
   * from their server settings; remotePath is the folder they chose inside it
   * and is theirs to name. Nextcloud gives out
   * /remote.php/dav/files/<user>, which nobody would type by hand and nobody
   * should have to keep re-typing when they rename their backup folder.
   */
  val basePath: String? = null,
  /**
   * SHA-256 of a certificate the user chose to trust, in hex. WebDAV only.
   *
   * ─── PINNED, NOT TRUSTED BLINDLY ─────────────────────────────────────────
   *
   * A self-hosted server usually presents its own certificate, so refusing
   * outright would rule out most of the servers this feature exists for. The
   * alternative most apps reach for is a "trust all certificates" switch,
   * which is one tap and turns HTTPS into an unauthenticated channel for
   * everything afterwards.
   *
   * This holds ONE certificate the user checked once, by fingerprint. Anything
   * else is still refused, so a network attacker's certificate fails even
   * though the real server's own certificate passes.
   */
  val certPin: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ServerConfig {
      val id = pigeonVar_list[0] as String
      val protocol = pigeonVar_list[1] as String
      val label = pigeonVar_list[2] as String
      val host = pigeonVar_list[3] as String
      val port = pigeonVar_list[4] as Long
      val share = pigeonVar_list[5] as String?
      val username = pigeonVar_list[6] as String
      val remotePath = pigeonVar_list[7] as String
      val encrypt = pigeonVar_list[8] as Boolean
      val wifiOnly = pigeonVar_list[9] as Boolean
      val whileCharging = pigeonVar_list[10] as Boolean
      val scheduled = pigeonVar_list[11] as Boolean
      val secure = pigeonVar_list[12] as Boolean?
      val basePath = pigeonVar_list[13] as String?
      val certPin = pigeonVar_list[14] as String?
      return ServerConfig(id, protocol, label, host, port, share, username, remotePath, encrypt, wifiOnly, whileCharging, scheduled, secure, basePath, certPin)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      protocol,
      label,
      host,
      port,
      share,
      username,
      remotePath,
      encrypt,
      wifiOnly,
      whileCharging,
      scheduled,
      secure,
      basePath,
      certPin,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ServerConfig
    return ServerApiPigeonUtils.deepEquals(this.id, other.id) && ServerApiPigeonUtils.deepEquals(this.protocol, other.protocol) && ServerApiPigeonUtils.deepEquals(this.label, other.label) && ServerApiPigeonUtils.deepEquals(this.host, other.host) && ServerApiPigeonUtils.deepEquals(this.port, other.port) && ServerApiPigeonUtils.deepEquals(this.share, other.share) && ServerApiPigeonUtils.deepEquals(this.username, other.username) && ServerApiPigeonUtils.deepEquals(this.remotePath, other.remotePath) && ServerApiPigeonUtils.deepEquals(this.encrypt, other.encrypt) && ServerApiPigeonUtils.deepEquals(this.wifiOnly, other.wifiOnly) && ServerApiPigeonUtils.deepEquals(this.whileCharging, other.whileCharging) && ServerApiPigeonUtils.deepEquals(this.scheduled, other.scheduled) && ServerApiPigeonUtils.deepEquals(this.secure, other.secure) && ServerApiPigeonUtils.deepEquals(this.basePath, other.basePath) && ServerApiPigeonUtils.deepEquals(this.certPin, other.certPin)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.id)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.protocol)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.label)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.host)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.port)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.share)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.username)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.remotePath)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.encrypt)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.wifiOnly)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.whileCharging)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.scheduled)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.secure)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.basePath)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.certPin)
    return result
  }
  override fun toString(): String {
    return "ServerConfig(id=$id, protocol=$protocol, label=$label, host=$host, port=$port, share=$share, username=$username, remotePath=$remotePath, encrypt=$encrypt, wifiOnly=$wifiOnly, whileCharging=$whileCharging, scheduled=$scheduled, secure=$secure, basePath=$basePath, certPin=$certPin)"
  }
}

/**
 * The result of trying to reach a server. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ServerProbe (
  val reachable: Boolean,
  /**
   * Reachable and writable are separate on purpose.
   *
   * A share that accepts the login and refuses a write is the commonest
   * misconfiguration there is, and discovering it at 2am during the first
   * scheduled run is far too late. The test writes a small file and removes it.
   */
  val writable: Boolean,
  /**
   * Why it failed, in words a person can act on. Empty when it worked.
   *
   * Never a raw exception. "Connection refused" tells someone nothing; "nothing
   * is listening on port 445, check the server is on" tells them what to do.
   */
  val detail: String,
  /** Free space on the share, or null where the protocol does not report it. */
  val freeBytes: Long? = null,
  /**
   * Which outcome this is, for the UI to branch on. Null from older code.
   *
   * ─── WHY [detail] IS NOT ENOUGH ──────────────────────────────────────────
   *
   * detail is a sentence for a person and must stay that way. But one of these
   * outcomes needs a button the others do not: an untrusted certificate is the
   * only failure the user can resolve from inside the app, by checking a
   * fingerprint and pinning it. Deciding that from the text would mean
   * matching on English, which breaks the moment the string is translated.
   *
   * One of: "ok", "auth", "cert", "not_dav", "path", "space", "network".
   * Anything unrecognised must be treated as a plain failure and shown as
   * [detail], so a newer native layer can add a code without breaking an
   * older screen.
   */
  val code: String? = null,
  /**
   * SHA-256 of the certificate that was refused, in hex. Set only with "cert".
   *
   * Shown so the user can compare it against their own server before pinning
   * it. Displaying it is the entire point: a fingerprint nobody checks is a
   * trust-all switch with extra steps.
   */
  val certFingerprint: String? = null,
  /** What the server calls itself, when it says. Absent rather than guessed. */
  val serverName: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ServerProbe {
      val reachable = pigeonVar_list[0] as Boolean
      val writable = pigeonVar_list[1] as Boolean
      val detail = pigeonVar_list[2] as String
      val freeBytes = pigeonVar_list[3] as Long?
      val code = pigeonVar_list[4] as String?
      val certFingerprint = pigeonVar_list[5] as String?
      val serverName = pigeonVar_list[6] as String?
      return ServerProbe(reachable, writable, detail, freeBytes, code, certFingerprint, serverName)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      reachable,
      writable,
      detail,
      freeBytes,
      code,
      certFingerprint,
      serverName,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ServerProbe
    return ServerApiPigeonUtils.deepEquals(this.reachable, other.reachable) && ServerApiPigeonUtils.deepEquals(this.writable, other.writable) && ServerApiPigeonUtils.deepEquals(this.detail, other.detail) && ServerApiPigeonUtils.deepEquals(this.freeBytes, other.freeBytes) && ServerApiPigeonUtils.deepEquals(this.code, other.code) && ServerApiPigeonUtils.deepEquals(this.certFingerprint, other.certFingerprint) && ServerApiPigeonUtils.deepEquals(this.serverName, other.serverName)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.reachable)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.writable)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.detail)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.freeBytes)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.code)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.certFingerprint)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.serverName)
    return result
  }
  override fun toString(): String {
    return "ServerProbe(reachable=$reachable, writable=$writable, detail=$detail, freeBytes=$freeBytes, code=$code, certFingerprint=$certFingerprint, serverName=$serverName)"
  }
}

/**
 * How a copy is going. Codec 131.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class TransferState (
  val running: Boolean,
  val sent: Long,
  val total: Long,
  val bytesSent: Long,
  val bytesTotal: Long,
  /** What is being copied right now, for the progress line. Null between files. */
  val currentName: String? = null,
  /**
   * Files that could not be copied. Reported rather than retried forever: a
   * file that fails twice is usually one the server will never accept, and a
   * silent infinite retry is how a backup appears to run all night and achieve
   * nothing.
   */
  val failed: Long,
  val lastRunMillis: Long? = null,
  /** Null when the last run finished cleanly. */
  val lastError: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): TransferState {
      val running = pigeonVar_list[0] as Boolean
      val sent = pigeonVar_list[1] as Long
      val total = pigeonVar_list[2] as Long
      val bytesSent = pigeonVar_list[3] as Long
      val bytesTotal = pigeonVar_list[4] as Long
      val currentName = pigeonVar_list[5] as String?
      val failed = pigeonVar_list[6] as Long
      val lastRunMillis = pigeonVar_list[7] as Long?
      val lastError = pigeonVar_list[8] as String?
      return TransferState(running, sent, total, bytesSent, bytesTotal, currentName, failed, lastRunMillis, lastError)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      running,
      sent,
      total,
      bytesSent,
      bytesTotal,
      currentName,
      failed,
      lastRunMillis,
      lastError,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as TransferState
    return ServerApiPigeonUtils.deepEquals(this.running, other.running) && ServerApiPigeonUtils.deepEquals(this.sent, other.sent) && ServerApiPigeonUtils.deepEquals(this.total, other.total) && ServerApiPigeonUtils.deepEquals(this.bytesSent, other.bytesSent) && ServerApiPigeonUtils.deepEquals(this.bytesTotal, other.bytesTotal) && ServerApiPigeonUtils.deepEquals(this.currentName, other.currentName) && ServerApiPigeonUtils.deepEquals(this.failed, other.failed) && ServerApiPigeonUtils.deepEquals(this.lastRunMillis, other.lastRunMillis) && ServerApiPigeonUtils.deepEquals(this.lastError, other.lastError)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.running)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.sent)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.total)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.bytesSent)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.bytesTotal)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.currentName)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.failed)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.lastRunMillis)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.lastError)
    return result
  }
  override fun toString(): String {
    return "TransferState(running=$running, sent=$sent, total=$total, bytesSent=$bytesSent, bytesTotal=$bytesTotal, currentName=$currentName, failed=$failed, lastRunMillis=$lastRunMillis, lastError=$lastError)"
  }
}

/**
 * Something already on the server. Codec 132.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RemoteFile (
  val remotePath: String,
  val name: String,
  val sizeBytes: Long,
  val modifiedMillis: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RemoteFile {
      val remotePath = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val sizeBytes = pigeonVar_list[2] as Long
      val modifiedMillis = pigeonVar_list[3] as Long
      return RemoteFile(remotePath, name, sizeBytes, modifiedMillis)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      remotePath,
      name,
      sizeBytes,
      modifiedMillis,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as RemoteFile
    return ServerApiPigeonUtils.deepEquals(this.remotePath, other.remotePath) && ServerApiPigeonUtils.deepEquals(this.name, other.name) && ServerApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes) && ServerApiPigeonUtils.deepEquals(this.modifiedMillis, other.modifiedMillis)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.remotePath)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.name)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.sizeBytes)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.modifiedMillis)
    return result
  }
  override fun toString(): String {
    return "RemoteFile(remotePath=$remotePath, name=$name, sizeBytes=$sizeBytes, modifiedMillis=$modifiedMillis)"
  }
}

/**
 * A local file that could be removed because the server has it. Codec 133.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ReclaimCandidate (
  val fileId: String,
  val name: String,
  val sizeBytes: Long,
  /**
   * Whether the copy on the server was checked and matched.
   *
   * FALSE MEANS DO NOT TOUCH IT. The reclaim flow removes originals, so a
   * candidate that has not been verified by size and checksum is offered to
   * nobody. This is the one place in the app where being wrong destroys
   * something.
   */
  val verified: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ReclaimCandidate {
      val fileId = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val sizeBytes = pigeonVar_list[2] as Long
      val verified = pigeonVar_list[3] as Boolean
      return ReclaimCandidate(fileId, name, sizeBytes, verified)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fileId,
      name,
      sizeBytes,
      verified,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ReclaimCandidate
    return ServerApiPigeonUtils.deepEquals(this.fileId, other.fileId) && ServerApiPigeonUtils.deepEquals(this.name, other.name) && ServerApiPigeonUtils.deepEquals(this.sizeBytes, other.sizeBytes) && ServerApiPigeonUtils.deepEquals(this.verified, other.verified)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.fileId)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.name)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.sizeBytes)
    result = 31 * result + ServerApiPigeonUtils.deepHash(this.verified)
    return result
  }
  override fun toString(): String {
    return "ReclaimCandidate(fileId=$fileId, name=$name, sizeBytes=$sizeBytes, verified=$verified)"
  }
}
private open class ServerApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ServerConfig.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ServerProbe.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          TransferState.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RemoteFile.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ReclaimCandidate.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is ServerConfig -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is ServerProbe -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is TransferState -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is RemoteFile -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is ReclaimCandidate -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface ServerHostApi {
  /** The saved server, or null when none is set up. One at a time, for now. */
  fun current(callback: (Result<ServerConfig?>) -> Unit)
  /**
   * Tries to reach a server without saving anything.
   *
   * [password] is passed for the test and not retained. A person changing a
   * setting on a saved server should not have to retype it, so an empty
   * password here means use the stored one.
   */
  fun test(config: ServerConfig, password: String, callback: (Result<ServerProbe>) -> Unit)
  /** Saves, after a successful test. An empty [password] keeps the stored one. */
  fun save(config: ServerConfig, password: String, callback: (Result<Unit>) -> Unit)
  /** Forgets the server and its password. Nothing on the server is touched. */
  fun forget(callback: (Result<Unit>) -> Unit)
  /**
   * Turns the nightly run on or off.
   *
   * ─── SEPARATE FROM save, DELIBERATELY ────────────────────────────────────
   *
   * Saving a config writes settings. This schedules or cancels actual work in
   * the system, and folding it into save would mean every edit to a port
   * number silently re-queued a job.
   */
  fun setSchedule(enabled: Boolean, callback: (Result<Unit>) -> Unit)
  /**
   * When the next run is due, or null when nothing is scheduled.
   *
   * From WorkManager rather than computed, so a phone that has deferred the
   * job for battery reasons reports what will actually happen rather than what
   * was asked for.
   */
  fun nextRunMillis(callback: (Result<Long?>) -> Unit)
  /** Starts a copy. Returns at once; watch [transferState]. */
  fun startBackup(callback: (Result<Unit>) -> Unit)
  fun cancelBackup(callback: (Result<Unit>) -> Unit)
  fun transferState(callback: (Result<TransferState>) -> Unit)
  /**
   * Checks the chosen files byte for byte before they are removed.
   *
   * ─── SEPARATE FROM [reclaimable], AND THAT IS THE POINT ──────────────────
   *
   * The listing compares sizes, which is cheap enough to run over a whole
   * library and catches a file that was truncated or replaced. It does not
   * catch a file of the same length whose contents differ, which is exactly
   * the case where deleting the local copy loses something.
   *
   * So the hash runs only on what the user actually selected: reading both
   * sides of four thousand files to answer a question about the twelve they
   * chose would take minutes and achieve nothing.
   *
   * Returns the ids that MATCHED. Anything missing from the result failed and
   * must not be touched.
   */
  fun verify(fileIds: List<String>, callback: (Result<List<String>>) -> Unit)
  /**
   * Local files whose copy on the server has been verified.
   *
   * Verification is size and checksum, done here rather than trusted from the
   * upload, because the interesting failure is a file that uploaded months ago
   * and has since been moved or truncated.
   */
  fun reclaimable(limit: Long, callback: (Result<List<ReclaimCandidate>>) -> Unit)

  companion object {
    /** The codec used by ServerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      ServerApiPigeonCodec()
    }
    /** Sets up an instance of `ServerHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: ServerHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.current$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.current{ result: Result<ServerConfig?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.test$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val configArg = args[0] as ServerConfig
            val passwordArg = args[1] as String
            api.test(configArg, passwordArg) { result: Result<ServerProbe> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.save$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val configArg = args[0] as ServerConfig
            val passwordArg = args[1] as String
            api.save(configArg, passwordArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ServerApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.forget$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.forget{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ServerApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.setSchedule$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val enabledArg = args[0] as Boolean
            api.setSchedule(enabledArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ServerApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.nextRunMillis$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.nextRunMillis{ result: Result<Long?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.startBackup$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.startBackup{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ServerApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.cancelBackup$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cancelBackup{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(ServerApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.transferState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.transferState{ result: Result<TransferState> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.verify$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fileIdsArg = args[0] as List<String>
            api.verify(fileIdsArg) { result: Result<List<String>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.ServerHostApi.reclaimable$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val limitArg = args[0] as Long
            api.reclaimable(limitArg) { result: Result<List<ReclaimCandidate>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(ServerApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(ServerApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
