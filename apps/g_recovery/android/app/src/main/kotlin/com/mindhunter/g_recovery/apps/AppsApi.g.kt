// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.apps

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object AppsApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * One installed app and what it occupies. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class AppEntry (
  val packageName: String,
  val label: String,
  /** The APK and its libraries. */
  val appBytes: Long,
  /**
   * Everything the app saved: accounts, messages, downloaded media.
   *
   * SEPARATE FROM CACHE, and the distinction is the whole point of this
   * screen. Cache can be thrown away with no loss. Data is the app's actual
   * content, and clearing it signs you out and deletes what you had.
   */
  val dataBytes: Long,
  /** Rebuildable. This is the number a person is looking for. */
  val cacheBytes: Long,
  val system: Boolean,
  /** Null when the app has never been opened, or when usage access is off. */
  val lastUsedMillis: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): AppEntry {
      val packageName = pigeonVar_list[0] as String
      val label = pigeonVar_list[1] as String
      val appBytes = pigeonVar_list[2] as Long
      val dataBytes = pigeonVar_list[3] as Long
      val cacheBytes = pigeonVar_list[4] as Long
      val system = pigeonVar_list[5] as Boolean
      val lastUsedMillis = pigeonVar_list[6] as Long?
      return AppEntry(packageName, label, appBytes, dataBytes, cacheBytes, system, lastUsedMillis)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      packageName,
      label,
      appBytes,
      dataBytes,
      cacheBytes,
      system,
      lastUsedMillis,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as AppEntry
    return AppsApiPigeonUtils.deepEquals(this.packageName, other.packageName) && AppsApiPigeonUtils.deepEquals(this.label, other.label) && AppsApiPigeonUtils.deepEquals(this.appBytes, other.appBytes) && AppsApiPigeonUtils.deepEquals(this.dataBytes, other.dataBytes) && AppsApiPigeonUtils.deepEquals(this.cacheBytes, other.cacheBytes) && AppsApiPigeonUtils.deepEquals(this.system, other.system) && AppsApiPigeonUtils.deepEquals(this.lastUsedMillis, other.lastUsedMillis)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.packageName)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.label)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.appBytes)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.dataBytes)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.cacheBytes)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.system)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.lastUsedMillis)
    return result
  }
  override fun toString(): String {
    return "AppEntry(packageName=$packageName, label=$label, appBytes=$appBytes, dataBytes=$dataBytes, cacheBytes=$cacheBytes, system=$system, lastUsedMillis=$lastUsedMillis)"
  }
}

/**
 * Whether this can work at all. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class AppsState (
  /**
   * Usage Access, granted on a settings screen. Without it no size can be read
   * at all, so the UI asks before it shows an empty list.
   */
  val usageAccess: Boolean,
  val totalBytes: Long,
  val cacheBytes: Long,
  val count: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): AppsState {
      val usageAccess = pigeonVar_list[0] as Boolean
      val totalBytes = pigeonVar_list[1] as Long
      val cacheBytes = pigeonVar_list[2] as Long
      val count = pigeonVar_list[3] as Long
      return AppsState(usageAccess, totalBytes, cacheBytes, count)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      usageAccess,
      totalBytes,
      cacheBytes,
      count,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as AppsState
    return AppsApiPigeonUtils.deepEquals(this.usageAccess, other.usageAccess) && AppsApiPigeonUtils.deepEquals(this.totalBytes, other.totalBytes) && AppsApiPigeonUtils.deepEquals(this.cacheBytes, other.cacheBytes) && AppsApiPigeonUtils.deepEquals(this.count, other.count)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.usageAccess)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.totalBytes)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.cacheBytes)
    result = 31 * result + AppsApiPigeonUtils.deepHash(this.count)
    return result
  }
  override fun toString(): String {
    return "AppsState(usageAccess=$usageAccess, totalBytes=$totalBytes, cacheBytes=$cacheBytes, count=$count)"
  }
}
private open class AppsApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          AppEntry.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          AppsState.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is AppEntry -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is AppsState -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface AppsHostApi {
  fun state(callback: (Result<AppsState>) -> Unit)
  /** Opens the Usage Access settings screen. */
  fun requestUsageAccess(callback: (Result<Boolean>) -> Unit)
  /**
   * Every app this phone will admit to, largest first.
   *
   * Slow. StorageStatsManager reads each package separately and a phone with
   * two hundred apps takes a few seconds, which is why the caller shows a
   * loading state rather than blocking.
   */
  fun apps(callback: (Result<List<AppEntry>>) -> Unit)
  /** The system storage screen for one app, where Clear cache actually works. */
  fun openAppSettings(packageName: String, callback: (Result<Boolean>) -> Unit)

  companion object {
    /** The codec used by AppsHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      AppsApiPigeonCodec()
    }
    /** Sets up an instance of `AppsHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: AppsHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.AppsHostApi.state$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.state{ result: Result<AppsState> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(AppsApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(AppsApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.AppsHostApi.requestUsageAccess$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestUsageAccess{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(AppsApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(AppsApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.AppsHostApi.apps$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.apps{ result: Result<List<AppEntry>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(AppsApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(AppsApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.AppsHostApi.openAppSettings$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val packageNameArg = args[0] as String
            api.openAppSettings(packageNameArg) { result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(AppsApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(AppsApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
