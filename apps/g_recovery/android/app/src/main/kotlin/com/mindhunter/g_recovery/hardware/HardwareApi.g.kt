// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindhunter.g_recovery.hardware

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object HardwareApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * The screen. Codec 129.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DisplayInfo (
  val widthPx: Long,
  val heightPx: Long,
  val densityDpi: Long,
  val refreshHz: Double,
  /**
   * Every mode the panel will run at, largest first. One entry on a fixed
   * 60 Hz screen, which is not an error and needs no note.
   */
  val supportedHz: List<Double>,
  val hdr: Boolean,
  val wideColour: Boolean,
  /** "HDR10", "HLG", "HDR10+", "Dolby Vision". */
  val hdrTypes: List<String>,
  /**
   * Candelas per square metre, and null on a panel that does not declare them,
   * which is most non HDR screens.
   */
  val maxLuminance: Double? = null,
  val minLuminance: Double? = null,
  val averageLuminance: Double? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DisplayInfo {
      val widthPx = pigeonVar_list[0] as Long
      val heightPx = pigeonVar_list[1] as Long
      val densityDpi = pigeonVar_list[2] as Long
      val refreshHz = pigeonVar_list[3] as Double
      val supportedHz = pigeonVar_list[4] as List<Double>
      val hdr = pigeonVar_list[5] as Boolean
      val wideColour = pigeonVar_list[6] as Boolean
      val hdrTypes = pigeonVar_list[7] as List<String>
      val maxLuminance = pigeonVar_list[8] as Double?
      val minLuminance = pigeonVar_list[9] as Double?
      val averageLuminance = pigeonVar_list[10] as Double?
      return DisplayInfo(widthPx, heightPx, densityDpi, refreshHz, supportedHz, hdr, wideColour, hdrTypes, maxLuminance, minLuminance, averageLuminance)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      widthPx,
      heightPx,
      densityDpi,
      refreshHz,
      supportedHz,
      hdr,
      wideColour,
      hdrTypes,
      maxLuminance,
      minLuminance,
      averageLuminance,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as DisplayInfo
    return HardwareApiPigeonUtils.deepEquals(this.widthPx, other.widthPx) && HardwareApiPigeonUtils.deepEquals(this.heightPx, other.heightPx) && HardwareApiPigeonUtils.deepEquals(this.densityDpi, other.densityDpi) && HardwareApiPigeonUtils.deepEquals(this.refreshHz, other.refreshHz) && HardwareApiPigeonUtils.deepEquals(this.supportedHz, other.supportedHz) && HardwareApiPigeonUtils.deepEquals(this.hdr, other.hdr) && HardwareApiPigeonUtils.deepEquals(this.wideColour, other.wideColour) && HardwareApiPigeonUtils.deepEquals(this.hdrTypes, other.hdrTypes) && HardwareApiPigeonUtils.deepEquals(this.maxLuminance, other.maxLuminance) && HardwareApiPigeonUtils.deepEquals(this.minLuminance, other.minLuminance) && HardwareApiPigeonUtils.deepEquals(this.averageLuminance, other.averageLuminance)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.widthPx)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.heightPx)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.densityDpi)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.refreshHz)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.supportedHz)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.hdr)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.wideColour)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.hdrTypes)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.maxLuminance)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.minLuminance)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.averageLuminance)
    return result
  }
  override fun toString(): String {
    return "DisplayInfo(widthPx=$widthPx, heightPx=$heightPx, densityDpi=$densityDpi, refreshHz=$refreshHz, supportedHz=$supportedHz, hdr=$hdr, wideColour=$wideColour, hdrTypes=$hdrTypes, maxLuminance=$maxLuminance, minLuminance=$minLuminance, averageLuminance=$averageLuminance)"
  }
}

/**
 * The active connection. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class NetworkInfo (
  /** "wifi" | "cellular" | "ethernet" | "vpn" | "none" */
  val type: String,
  val connected: Boolean,
  val metered: Boolean,
  val ipv4: String? = null,
  val ipv6: String? = null,
  /**
   * Since boot, across every interface. TrafficStats needs no permission for
   * device totals, unlike the per app figures.
   */
  val rxBytesTotal: Long,
  val txBytesTotal: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): NetworkInfo {
      val type = pigeonVar_list[0] as String
      val connected = pigeonVar_list[1] as Boolean
      val metered = pigeonVar_list[2] as Boolean
      val ipv4 = pigeonVar_list[3] as String?
      val ipv6 = pigeonVar_list[4] as String?
      val rxBytesTotal = pigeonVar_list[5] as Long
      val txBytesTotal = pigeonVar_list[6] as Long
      return NetworkInfo(type, connected, metered, ipv4, ipv6, rxBytesTotal, txBytesTotal)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      type,
      connected,
      metered,
      ipv4,
      ipv6,
      rxBytesTotal,
      txBytesTotal,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as NetworkInfo
    return HardwareApiPigeonUtils.deepEquals(this.type, other.type) && HardwareApiPigeonUtils.deepEquals(this.connected, other.connected) && HardwareApiPigeonUtils.deepEquals(this.metered, other.metered) && HardwareApiPigeonUtils.deepEquals(this.ipv4, other.ipv4) && HardwareApiPigeonUtils.deepEquals(this.ipv6, other.ipv6) && HardwareApiPigeonUtils.deepEquals(this.rxBytesTotal, other.rxBytesTotal) && HardwareApiPigeonUtils.deepEquals(this.txBytesTotal, other.txBytesTotal)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.type)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.connected)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.metered)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.ipv4)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.ipv6)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.rxBytesTotal)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.txBytesTotal)
    return result
  }
  override fun toString(): String {
    return "NetworkInfo(type=$type, connected=$connected, metered=$metered, ipv4=$ipv4, ipv6=$ipv6, rxBytesTotal=$rxBytesTotal, txBytesTotal=$txBytesTotal)"
  }
}

/**
 * The Wi-Fi link. Codec 131.
 *
 * ─── HALF OF THIS NEEDS LOCATION ─────────────────────────────────────────────
 *
 * From Android 10, SSID, BSSID and MAC are only real with ACCESS_FINE_LOCATION.
 * Without it the system hands back "<unknown ssid>" and 02:00:00:00:00:00, and
 * an app that prints those is printing a refusal as though it were data.
 *
 * So they are NULLABLE and native returns null rather than the placeholder. The
 * UI shows the row missing and names the permission once.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class WifiInfo (
  val connected: Boolean,
  val ssid: String? = null,
  val bssid: String? = null,
  val macAddress: String? = null,
  /** "802.11n", "802.11ac", "802.11ax". */
  val standard: String? = null,
  val frequencyMhz: Long? = null,
  val linkSpeedMbps: Long? = null,
  val rxLinkSpeedMbps: Long? = null,
  val signalPercent: Long? = null,
  val security: String? = null,
  val supports5Ghz: Boolean,
  val supports6Ghz: Boolean,
  /**
   * So the UI can say which permission would fill the gaps, rather than
   * guessing from a null that might just be a disconnected radio.
   */
  val hasLocationPermission: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): WifiInfo {
      val connected = pigeonVar_list[0] as Boolean
      val ssid = pigeonVar_list[1] as String?
      val bssid = pigeonVar_list[2] as String?
      val macAddress = pigeonVar_list[3] as String?
      val standard = pigeonVar_list[4] as String?
      val frequencyMhz = pigeonVar_list[5] as Long?
      val linkSpeedMbps = pigeonVar_list[6] as Long?
      val rxLinkSpeedMbps = pigeonVar_list[7] as Long?
      val signalPercent = pigeonVar_list[8] as Long?
      val security = pigeonVar_list[9] as String?
      val supports5Ghz = pigeonVar_list[10] as Boolean
      val supports6Ghz = pigeonVar_list[11] as Boolean
      val hasLocationPermission = pigeonVar_list[12] as Boolean
      return WifiInfo(connected, ssid, bssid, macAddress, standard, frequencyMhz, linkSpeedMbps, rxLinkSpeedMbps, signalPercent, security, supports5Ghz, supports6Ghz, hasLocationPermission)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      connected,
      ssid,
      bssid,
      macAddress,
      standard,
      frequencyMhz,
      linkSpeedMbps,
      rxLinkSpeedMbps,
      signalPercent,
      security,
      supports5Ghz,
      supports6Ghz,
      hasLocationPermission,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as WifiInfo
    return HardwareApiPigeonUtils.deepEquals(this.connected, other.connected) && HardwareApiPigeonUtils.deepEquals(this.ssid, other.ssid) && HardwareApiPigeonUtils.deepEquals(this.bssid, other.bssid) && HardwareApiPigeonUtils.deepEquals(this.macAddress, other.macAddress) && HardwareApiPigeonUtils.deepEquals(this.standard, other.standard) && HardwareApiPigeonUtils.deepEquals(this.frequencyMhz, other.frequencyMhz) && HardwareApiPigeonUtils.deepEquals(this.linkSpeedMbps, other.linkSpeedMbps) && HardwareApiPigeonUtils.deepEquals(this.rxLinkSpeedMbps, other.rxLinkSpeedMbps) && HardwareApiPigeonUtils.deepEquals(this.signalPercent, other.signalPercent) && HardwareApiPigeonUtils.deepEquals(this.security, other.security) && HardwareApiPigeonUtils.deepEquals(this.supports5Ghz, other.supports5Ghz) && HardwareApiPigeonUtils.deepEquals(this.supports6Ghz, other.supports6Ghz) && HardwareApiPigeonUtils.deepEquals(this.hasLocationPermission, other.hasLocationPermission)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.connected)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.ssid)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.bssid)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.macAddress)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.standard)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.frequencyMhz)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.linkSpeedMbps)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.rxLinkSpeedMbps)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.signalPercent)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.security)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.supports5Ghz)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.supports6Ghz)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.hasLocationPermission)
    return result
  }
  override fun toString(): String {
    return "WifiInfo(connected=$connected, ssid=$ssid, bssid=$bssid, macAddress=$macAddress, standard=$standard, frequencyMhz=$frequencyMhz, linkSpeedMbps=$linkSpeedMbps, rxLinkSpeedMbps=$rxLinkSpeedMbps, signalPercent=$signalPercent, security=$security, supports5Ghz=$supports5Ghz, supports6Ghz=$supports6Ghz, hasLocationPermission=$hasLocationPermission)"
  }
}

/**
 * One camera. Codec 132.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CameraInfo (
  val id: String,
  /** "front" | "back" | "external" */
  val facing: String,
  val megapixels: Double,
  val widthPx: Long,
  val heightPx: Long,
  val focalLengthsMm: List<Double>,
  val apertures: List<Double>,
  val isoMin: Long? = null,
  val isoMax: Long? = null,
  val supportsRaw: Boolean,
  val hasFlash: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CameraInfo {
      val id = pigeonVar_list[0] as String
      val facing = pigeonVar_list[1] as String
      val megapixels = pigeonVar_list[2] as Double
      val widthPx = pigeonVar_list[3] as Long
      val heightPx = pigeonVar_list[4] as Long
      val focalLengthsMm = pigeonVar_list[5] as List<Double>
      val apertures = pigeonVar_list[6] as List<Double>
      val isoMin = pigeonVar_list[7] as Long?
      val isoMax = pigeonVar_list[8] as Long?
      val supportsRaw = pigeonVar_list[9] as Boolean
      val hasFlash = pigeonVar_list[10] as Boolean
      return CameraInfo(id, facing, megapixels, widthPx, heightPx, focalLengthsMm, apertures, isoMin, isoMax, supportsRaw, hasFlash)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      facing,
      megapixels,
      widthPx,
      heightPx,
      focalLengthsMm,
      apertures,
      isoMin,
      isoMax,
      supportsRaw,
      hasFlash,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CameraInfo
    return HardwareApiPigeonUtils.deepEquals(this.id, other.id) && HardwareApiPigeonUtils.deepEquals(this.facing, other.facing) && HardwareApiPigeonUtils.deepEquals(this.megapixels, other.megapixels) && HardwareApiPigeonUtils.deepEquals(this.widthPx, other.widthPx) && HardwareApiPigeonUtils.deepEquals(this.heightPx, other.heightPx) && HardwareApiPigeonUtils.deepEquals(this.focalLengthsMm, other.focalLengthsMm) && HardwareApiPigeonUtils.deepEquals(this.apertures, other.apertures) && HardwareApiPigeonUtils.deepEquals(this.isoMin, other.isoMin) && HardwareApiPigeonUtils.deepEquals(this.isoMax, other.isoMax) && HardwareApiPigeonUtils.deepEquals(this.supportsRaw, other.supportsRaw) && HardwareApiPigeonUtils.deepEquals(this.hasFlash, other.hasFlash)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.id)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.facing)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.megapixels)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.widthPx)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.heightPx)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.focalLengthsMm)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.apertures)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.isoMin)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.isoMax)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.supportsRaw)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.hasFlash)
    return result
  }
  override fun toString(): String {
    return "CameraInfo(id=$id, facing=$facing, megapixels=$megapixels, widthPx=$widthPx, heightPx=$heightPx, focalLengthsMm=$focalLengthsMm, apertures=$apertures, isoMin=$isoMin, isoMax=$isoMax, supportsRaw=$supportsRaw, hasFlash=$hasFlash)"
  }
}

/**
 * What the hardware has. Codec 133.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class FeatureFlags (
  val nfc: Boolean,
  val gps: Boolean,
  val uwb: Boolean,
  val usbHost: Boolean,
  val fingerprint: Boolean,
  val bluetooth: Boolean,
  val bluetoothLe: Boolean,
  val telephony: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): FeatureFlags {
      val nfc = pigeonVar_list[0] as Boolean
      val gps = pigeonVar_list[1] as Boolean
      val uwb = pigeonVar_list[2] as Boolean
      val usbHost = pigeonVar_list[3] as Boolean
      val fingerprint = pigeonVar_list[4] as Boolean
      val bluetooth = pigeonVar_list[5] as Boolean
      val bluetoothLe = pigeonVar_list[6] as Boolean
      val telephony = pigeonVar_list[7] as Boolean
      return FeatureFlags(nfc, gps, uwb, usbHost, fingerprint, bluetooth, bluetoothLe, telephony)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      nfc,
      gps,
      uwb,
      usbHost,
      fingerprint,
      bluetooth,
      bluetoothLe,
      telephony,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as FeatureFlags
    return HardwareApiPigeonUtils.deepEquals(this.nfc, other.nfc) && HardwareApiPigeonUtils.deepEquals(this.gps, other.gps) && HardwareApiPigeonUtils.deepEquals(this.uwb, other.uwb) && HardwareApiPigeonUtils.deepEquals(this.usbHost, other.usbHost) && HardwareApiPigeonUtils.deepEquals(this.fingerprint, other.fingerprint) && HardwareApiPigeonUtils.deepEquals(this.bluetooth, other.bluetooth) && HardwareApiPigeonUtils.deepEquals(this.bluetoothLe, other.bluetoothLe) && HardwareApiPigeonUtils.deepEquals(this.telephony, other.telephony)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.nfc)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.gps)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.uwb)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.usbHost)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.fingerprint)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.bluetooth)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.bluetoothLe)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.telephony)
    return result
  }
  override fun toString(): String {
    return "FeatureFlags(nfc=$nfc, gps=$gps, uwb=$uwb, usbHost=$usbHost, fingerprint=$fingerprint, bluetooth=$bluetooth, bluetoothLe=$bluetoothLe, telephony=$telephony)"
  }
}

/**
 * One throughput reading. Codec 134.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ThroughputSample (
  /**
   * A RATE, computed natively from two counter reads.
   *
   * Native owns the previous counter and the elapsed time, because doing the
   * subtraction in Dart would make every rate wrong by however long the UI
   * thread was busy.
   */
  val rxBytesPerSecond: Long,
  val txBytesPerSecond: Long,
  val rxBytesTotal: Long,
  val txBytesTotal: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ThroughputSample {
      val rxBytesPerSecond = pigeonVar_list[0] as Long
      val txBytesPerSecond = pigeonVar_list[1] as Long
      val rxBytesTotal = pigeonVar_list[2] as Long
      val txBytesTotal = pigeonVar_list[3] as Long
      return ThroughputSample(rxBytesPerSecond, txBytesPerSecond, rxBytesTotal, txBytesTotal)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      rxBytesPerSecond,
      txBytesPerSecond,
      rxBytesTotal,
      txBytesTotal,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ThroughputSample
    return HardwareApiPigeonUtils.deepEquals(this.rxBytesPerSecond, other.rxBytesPerSecond) && HardwareApiPigeonUtils.deepEquals(this.txBytesPerSecond, other.txBytesPerSecond) && HardwareApiPigeonUtils.deepEquals(this.rxBytesTotal, other.rxBytesTotal) && HardwareApiPigeonUtils.deepEquals(this.txBytesTotal, other.txBytesTotal)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.rxBytesPerSecond)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.txBytesPerSecond)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.rxBytesTotal)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.txBytesTotal)
    return result
  }
  override fun toString(): String {
    return "ThroughputSample(rxBytesPerSecond=$rxBytesPerSecond, txBytesPerSecond=$txBytesPerSecond, rxBytesTotal=$rxBytesTotal, txBytesTotal=$txBytesTotal)"
  }
}

/**
 * One sensor, reading now. Codec 135.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class SensorReading (
  /**
   * The Android sensor type constant, as a string. Numbers would be an enum,
   * and an enum here would renumber every class above it.
   */
  val type: String,
  val name: String,
  /**
   * One to three axes. Light and pressure send one, the accelerometer sends
   * three, and the caller renders whatever arrives rather than assuming.
   */
  val values: List<Double>,
  /** "m/s2", "lux", "uT", "rad/s", "hPa", "cm". */
  val unit: String,
  /**
   * The largest value this sensor can report, for scaling a bar. Null where
   * the sensor does not declare one.
   */
  val maxRange: Double? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): SensorReading {
      val type = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String
      val values = pigeonVar_list[2] as List<Double>
      val unit = pigeonVar_list[3] as String
      val maxRange = pigeonVar_list[4] as Double?
      return SensorReading(type, name, values, unit, maxRange)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      type,
      name,
      values,
      unit,
      maxRange,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as SensorReading
    return HardwareApiPigeonUtils.deepEquals(this.type, other.type) && HardwareApiPigeonUtils.deepEquals(this.name, other.name) && HardwareApiPigeonUtils.deepEquals(this.values, other.values) && HardwareApiPigeonUtils.deepEquals(this.unit, other.unit) && HardwareApiPigeonUtils.deepEquals(this.maxRange, other.maxRange)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.type)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.name)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.values)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.unit)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.maxRange)
    return result
  }
  override fun toString(): String {
    return "SensorReading(type=$type, name=$name, values=$values, unit=$unit, maxRange=$maxRange)"
  }
}

/**
 * One SIM. Codec 136.
 *
 * ─── WHAT IS FREE AND WHAT IS NOT ────────────────────────────────────────────
 *
 * Carrier name, country, MCC and MNC come from SubscriptionManager and need
 * READ_PHONE_STATE from Android 11. The number and the ICCID need more than
 * that and are not read at all: a recovery app does not need the subscriber's
 * phone number, and asking for it would be the single most alarming permission
 * in the app.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class SimInfo (
  val slot: Long,
  val carrier: String? = null,
  /**
   * Two letters, uppercased for display. Null where the SIM does not report a
   * country, which happens on some MVNOs.
   */
  val countryIso: String? = null,
  val mcc: String? = null,
  val mnc: String? = null,
  /** An eSIM rather than a physical card. */
  val embedded: Boolean,
  val roaming: Boolean,
  /** The SIM carrying mobile data. False on both when data is off. */
  val dataDefault: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): SimInfo {
      val slot = pigeonVar_list[0] as Long
      val carrier = pigeonVar_list[1] as String?
      val countryIso = pigeonVar_list[2] as String?
      val mcc = pigeonVar_list[3] as String?
      val mnc = pigeonVar_list[4] as String?
      val embedded = pigeonVar_list[5] as Boolean
      val roaming = pigeonVar_list[6] as Boolean
      val dataDefault = pigeonVar_list[7] as Boolean
      return SimInfo(slot, carrier, countryIso, mcc, mnc, embedded, roaming, dataDefault)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      slot,
      carrier,
      countryIso,
      mcc,
      mnc,
      embedded,
      roaming,
      dataDefault,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as SimInfo
    return HardwareApiPigeonUtils.deepEquals(this.slot, other.slot) && HardwareApiPigeonUtils.deepEquals(this.carrier, other.carrier) && HardwareApiPigeonUtils.deepEquals(this.countryIso, other.countryIso) && HardwareApiPigeonUtils.deepEquals(this.mcc, other.mcc) && HardwareApiPigeonUtils.deepEquals(this.mnc, other.mnc) && HardwareApiPigeonUtils.deepEquals(this.embedded, other.embedded) && HardwareApiPigeonUtils.deepEquals(this.roaming, other.roaming) && HardwareApiPigeonUtils.deepEquals(this.dataDefault, other.dataDefault)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.slot)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.carrier)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.countryIso)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.mcc)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.mnc)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.embedded)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.roaming)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.dataDefault)
    return result
  }
  override fun toString(): String {
    return "SimInfo(slot=$slot, carrier=$carrier, countryIso=$countryIso, mcc=$mcc, mnc=$mnc, embedded=$embedded, roaming=$roaming, dataDefault=$dataDefault)"
  }
}

/**
 * One paired device. Codec 137.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PairedDevice (
  val name: String,
  /**
   * Masked to the last four characters. A full MAC on screen is a tracking
   * identifier for a device the user may be about to photograph the screen of.
   */
  val address: String,
  /** "audio" | "phone" | "computer" | "wearable" | "input" | "other" */
  val type: String,
  val connected: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PairedDevice {
      val name = pigeonVar_list[0] as String
      val address = pigeonVar_list[1] as String
      val type = pigeonVar_list[2] as String
      val connected = pigeonVar_list[3] as Boolean
      return PairedDevice(name, address, type, connected)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      name,
      address,
      type,
      connected,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PairedDevice
    return HardwareApiPigeonUtils.deepEquals(this.name, other.name) && HardwareApiPigeonUtils.deepEquals(this.address, other.address) && HardwareApiPigeonUtils.deepEquals(this.type, other.type) && HardwareApiPigeonUtils.deepEquals(this.connected, other.connected)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.name)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.address)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.type)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.connected)
    return result
  }
  override fun toString(): String {
    return "PairedDevice(name=$name, address=$address, type=$type, connected=$connected)"
  }
}

/**
 * The Bluetooth radio. Codec 138.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class BluetoothInfo (
  /** The device has a radio at all. */
  val available: Boolean,
  val enabled: Boolean,
  /**
   * Null without BLUETOOTH_CONNECT, which is where the adapter name lives from
   * Android 12.
   */
  val name: String? = null,
  val hasPermission: Boolean,
  /**
   * Empty without the permission, which is not the same as no paired devices.
   * [hasPermission] tells the two apart.
   */
  val paired: List<PairedDevice>,
  val leSupported: Boolean,
  val le2mSupported: Boolean,
  val leCodedSupported: Boolean,
  val leAudioSupported: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): BluetoothInfo {
      val available = pigeonVar_list[0] as Boolean
      val enabled = pigeonVar_list[1] as Boolean
      val name = pigeonVar_list[2] as String?
      val hasPermission = pigeonVar_list[3] as Boolean
      val paired = pigeonVar_list[4] as List<PairedDevice>
      val leSupported = pigeonVar_list[5] as Boolean
      val le2mSupported = pigeonVar_list[6] as Boolean
      val leCodedSupported = pigeonVar_list[7] as Boolean
      val leAudioSupported = pigeonVar_list[8] as Boolean
      return BluetoothInfo(available, enabled, name, hasPermission, paired, leSupported, le2mSupported, leCodedSupported, leAudioSupported)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      available,
      enabled,
      name,
      hasPermission,
      paired,
      leSupported,
      le2mSupported,
      leCodedSupported,
      leAudioSupported,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as BluetoothInfo
    return HardwareApiPigeonUtils.deepEquals(this.available, other.available) && HardwareApiPigeonUtils.deepEquals(this.enabled, other.enabled) && HardwareApiPigeonUtils.deepEquals(this.name, other.name) && HardwareApiPigeonUtils.deepEquals(this.hasPermission, other.hasPermission) && HardwareApiPigeonUtils.deepEquals(this.paired, other.paired) && HardwareApiPigeonUtils.deepEquals(this.leSupported, other.leSupported) && HardwareApiPigeonUtils.deepEquals(this.le2mSupported, other.le2mSupported) && HardwareApiPigeonUtils.deepEquals(this.leCodedSupported, other.leCodedSupported) && HardwareApiPigeonUtils.deepEquals(this.leAudioSupported, other.leAudioSupported)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.available)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.enabled)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.name)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.hasPermission)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.paired)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.leSupported)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.le2mSupported)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.leCodedSupported)
    result = 31 * result + HardwareApiPigeonUtils.deepHash(this.leAudioSupported)
    return result
  }
  override fun toString(): String {
    return "BluetoothInfo(available=$available, enabled=$enabled, name=$name, hasPermission=$hasPermission, paired=$paired, leSupported=$leSupported, le2mSupported=$le2mSupported, leCodedSupported=$leCodedSupported, leAudioSupported=$leAudioSupported)"
  }
}
private open class HardwareApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DisplayInfo.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          NetworkInfo.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          WifiInfo.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CameraInfo.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          FeatureFlags.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ThroughputSample.fromList(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          SensorReading.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          SimInfo.fromList(it)
        }
      }
      137.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PairedDevice.fromList(it)
        }
      }
      138.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          BluetoothInfo.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is DisplayInfo -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is NetworkInfo -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is WifiInfo -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is CameraInfo -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is FeatureFlags -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is ThroughputSample -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      is SensorReading -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is SimInfo -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      is PairedDevice -> {
        stream.write(137)
        writeValue(stream, value.toList())
      }
      is BluetoothInfo -> {
        stream.write(138)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface HardwareHostApi {
  fun display(callback: (Result<DisplayInfo>) -> Unit)
  fun network(callback: (Result<NetworkInfo>) -> Unit)
  fun wifi(callback: (Result<WifiInfo>) -> Unit)
  fun cameras(callback: (Result<List<CameraInfo>>) -> Unit)
  fun features(callback: (Result<FeatureFlags>) -> Unit)
  /**
   * One reading, taken against the previous call.
   *
   * The first call after a cold start returns zero rates and real totals, since
   * there is nothing to subtract from yet.
   */
  fun throughput(callback: (Result<ThroughputSample>) -> Unit)
  /**
   * Starts listening to a small set of sensors.
   *
   * ─── EXPLICIT START AND STOP, NOT A STANDING SUBSCRIPTION ────────────────
   *
   * A registered SensorEventListener wakes the CPU at whatever rate it asked
   * for, whether or not anything is looking. Tying registration to a screen
   * being open is the difference between a page that costs nothing when closed
   * and a background drain nobody attributes to this app.
   */
  fun startSensors(types: List<String>, callback: (Result<Unit>) -> Unit)
  fun stopSensors(callback: (Result<Unit>) -> Unit)
  /**
   * The latest value from each started sensor.
   *
   * Polled rather than pushed. A sensor at its default rate fires far more
   * often than a screen refreshes, and forwarding every event over a channel
   * would spend more time in serialisation than in the reading.
   */
  fun sensorValues(callback: (Result<List<SensorReading>>) -> Unit)
  /** Every SIM the phone can see, by slot. */
  fun sims(callback: (Result<List<SimInfo>>) -> Unit)
  fun bluetooth(callback: (Result<BluetoothInfo>) -> Unit)
  /**
   * Plays a steady tone through one or both speakers.
   *
   * ─── GENERATED, NOT A BUNDLED FILE ───────────────────────────────────────
   *
   * A sine wave written straight into an AudioTrack. An asset would be a few
   * hundred kilobytes in the APK for a sound the phone can compute, and a
   * plugin would be a dependency for sixty lines of Kotlin.
   *
   * [channel] is "left", "right" or "both". Separate channels are the whole
   * point: a phone with one dead speaker sounds fine on "both".
   */
  fun playTone(hertz: Double, milliseconds: Long, channel: String, callback: (Result<Unit>) -> Unit)
  fun stopTone(callback: (Result<Unit>) -> Unit)
  /** [pattern] is "short", "long" or "double". */
  fun vibrate(pattern: String, callback: (Result<Boolean>) -> Unit)
  /** Opens the system dialog for nearby Bluetooth devices. */
  fun requestBluetooth(callback: (Result<Boolean>) -> Unit)
  /** Opens the system dialog for phone state, which SIM details need. */
  fun requestPhoneState(callback: (Result<Boolean>) -> Unit)
  /** Opens the system dialog for fine location. */
  fun requestLocation(callback: (Result<Boolean>) -> Unit)

  companion object {
    /** The codec used by HardwareHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      HardwareApiPigeonCodec()
    }
    /** Sets up an instance of `HardwareHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: HardwareHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.display$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.display{ result: Result<DisplayInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.network$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.network{ result: Result<NetworkInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.wifi$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.wifi{ result: Result<WifiInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.cameras$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cameras{ result: Result<List<CameraInfo>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.features$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.features{ result: Result<FeatureFlags> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.throughput$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.throughput{ result: Result<ThroughputSample> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.startSensors$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val typesArg = args[0] as List<String>
            api.startSensors(typesArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(HardwareApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.stopSensors$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopSensors{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(HardwareApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.sensorValues$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.sensorValues{ result: Result<List<SensorReading>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.sims$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.sims{ result: Result<List<SimInfo>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.bluetooth$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.bluetooth{ result: Result<BluetoothInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.playTone$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val hertzArg = args[0] as Double
            val millisecondsArg = args[1] as Long
            val channelArg = args[2] as String
            api.playTone(hertzArg, millisecondsArg, channelArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(HardwareApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.stopTone$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopTone{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                reply.reply(HardwareApiPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.vibrate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val patternArg = args[0] as String
            api.vibrate(patternArg) { result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.requestBluetooth$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestBluetooth{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.requestPhoneState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestPhoneState{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.g_recovery.HardwareHostApi.requestLocation$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestLocation{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(HardwareApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(HardwareApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
