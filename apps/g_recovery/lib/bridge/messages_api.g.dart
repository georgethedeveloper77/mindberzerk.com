// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: unused_import, unused_shown_name
// ignore_for_file: type=lint

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List;

import 'package:flutter/services.dart';
import 'package:meta/meta.dart' show immutable, protected, visibleForTesting;

Object? _extractReplyValueOrThrow(
  List<Object?>? replyList,
  String channelName, {
  required bool isNullValid,
}) {
  if (replyList == null) {
    throw PlatformException(
      code: 'channel-error',
      message: 'Unable to establish connection on channel: "$channelName".',
    );
  } else if (replyList.length > 1) {
    throw PlatformException(
      code: replyList[0]! as String,
      message: replyList[1] as String?,
      details: replyList[2],
    );
  } else if (!isNullValid && (replyList.isNotEmpty && replyList[0] == null)) {
    throw PlatformException(
      code: 'null-error',
      message: 'Host platform returned null value for non-null return value.',
    );
  }
  return replyList.firstOrNull;
}

bool _deepEquals(Object? a, Object? b) {
  if (identical(a, b)) {
    return true;
  }
  if (a is double && b is double) {
    if (a.isNaN && b.isNaN) {
      return true;
    }
    return a == b;
  }
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed.every(
          ((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]),
        );
  }
  if (a is Map && b is Map) {
    if (a.length != b.length) {
      return false;
    }
    for (final MapEntry<Object?, Object?> entryA in a.entries) {
      bool found = false;
      for (final MapEntry<Object?, Object?> entryB in b.entries) {
        if (_deepEquals(entryA.key, entryB.key)) {
          if (_deepEquals(entryA.value, entryB.value)) {
            found = true;
            break;
          } else {
            return false;
          }
        }
      }
      if (!found) {
        return false;
      }
    }
    return true;
  }
  return a == b;
}

int _deepHash(Object? value) {
  if (value is List) {
    return Object.hashAll(value.map(_deepHash));
  }
  if (value is Map) {
    int result = 0;
    for (final MapEntry<Object?, Object?> entry in value.entries) {
      result += (_deepHash(entry.key) * 31) ^ _deepHash(entry.value);
    }
    return result;
  }
  if (value is double && value.isNaN) {
    // Normalize NaN to a consistent hash.
    return 0x7FF8000000000000.hashCode;
  }
  if (value is double && value == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return 0.0.hashCode;
  }
  return value.hashCode;
}

/// Whether capture is possible and running. Codec 129.
class MessageCapture {
  MessageCapture({
    required this.listenerEnabled,
    required this.capturing,
    required this.messageCount,
    required this.conversationCount,
    this.since,
  });

  /// Whether notification access has been granted in system settings.
  ///
  /// The only thing standing between off and on. There is no runtime dialog for
  /// this permission: it is a settings screen the user has to visit, which is
  /// why the app has to check it on resume rather than await a result.
  bool listenerEnabled;

  /// Whether the user has also switched capture on inside this app.
  ///
  /// Separate from [listenerEnabled] on purpose. Granting the system permission
  /// and choosing to archive are two decisions, and collapsing them would mean
  /// a user who granted access for one reason silently starts recording every
  /// message they receive.
  bool capturing;

  int messageCount;

  int conversationCount;

  /// When capture began, epoch milliseconds, or null if it never has.
  ///
  /// The honest edge of the archive. Everything older than this is not missing,
  /// it never existed as far as this app is concerned, and the UI says so
  /// instead of showing an empty list.
  int? since;

  List<Object?> _toList() {
    return <Object?>[
      listenerEnabled,
      capturing,
      messageCount,
      conversationCount,
      since,
    ];
  }

  Object encode() {
    return _toList();
  }

  static MessageCapture decode(Object result) {
    result as List<Object?>;
    return MessageCapture(
      listenerEnabled: result[0]! as bool,
      capturing: result[1]! as bool,
      messageCount: result[2]! as int,
      conversationCount: result[3]! as int,
      since: result[4] as int?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! MessageCapture || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(listenerEnabled, other.listenerEnabled) &&
        _deepEquals(capturing, other.capturing) &&
        _deepEquals(messageCount, other.messageCount) &&
        _deepEquals(conversationCount, other.conversationCount) &&
        _deepEquals(since, other.since);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'MessageCapture(listenerEnabled: $listenerEnabled, capturing: $capturing, messageCount: $messageCount, conversationCount: $conversationCount, since: $since)';
  }
}

/// One captured message. Codec 130.
class ArchivedMessage {
  ArchivedMessage({
    required this.messageId,
    required this.packageName,
    required this.appLabel,
    this.conversation,
    this.sender,
    required this.text,
    required this.postedAtMillis,
    this.removedAtMillis,
    required this.edited,
  });

  String messageId;

  /// The app that posted it, such as com.whatsapp. Kept raw so a future filter
  /// can group by app without re-deriving it from a display name.
  String packageName;

  String appLabel;

  /// The chat or group title as the notification carried it. Null when the
  /// posting app gave no title, which happens on a first message from an
  /// unsaved number.
  String? conversation;

  String? sender;

  String text;

  int postedAtMillis;

  /// When the notification was taken away, epoch milliseconds, or null while it
  /// is still showing.
  ///
  /// NOT proof of deletion. A notification is removed when the user opens the
  /// chat, swipes it away, or when the sender deletes the message, and the
  /// system does not say which. The UI must not label this "deleted".
  int? removedAtMillis;

  /// True when a later notification for the same conversation replaced this
  /// text with different text.
  ///
  /// The closest thing to real evidence of a deletion this approach can offer:
  /// WhatsApp replaces the message with "This message was deleted", which
  /// arrives as an update to the same notification, so the ORIGINAL text is
  /// still here and the replacement is what everyone else sees.
  bool edited;

  List<Object?> _toList() {
    return <Object?>[
      messageId,
      packageName,
      appLabel,
      conversation,
      sender,
      text,
      postedAtMillis,
      removedAtMillis,
      edited,
    ];
  }

  Object encode() {
    return _toList();
  }

  static ArchivedMessage decode(Object result) {
    result as List<Object?>;
    return ArchivedMessage(
      messageId: result[0]! as String,
      packageName: result[1]! as String,
      appLabel: result[2]! as String,
      conversation: result[3] as String?,
      sender: result[4] as String?,
      text: result[5]! as String,
      postedAtMillis: result[6]! as int,
      removedAtMillis: result[7] as int?,
      edited: result[8]! as bool,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! ArchivedMessage || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(messageId, other.messageId) &&
        _deepEquals(packageName, other.packageName) &&
        _deepEquals(appLabel, other.appLabel) &&
        _deepEquals(conversation, other.conversation) &&
        _deepEquals(sender, other.sender) &&
        _deepEquals(text, other.text) &&
        _deepEquals(postedAtMillis, other.postedAtMillis) &&
        _deepEquals(removedAtMillis, other.removedAtMillis) &&
        _deepEquals(edited, other.edited);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'ArchivedMessage(messageId: $messageId, packageName: $packageName, appLabel: $appLabel, conversation: $conversation, sender: $sender, text: $text, postedAtMillis: $postedAtMillis, removedAtMillis: $removedAtMillis, edited: $edited)';
  }
}

class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();

  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    } else if (value is MessageCapture) {
      buffer.putUint8(129);
      writeValue(buffer, value.encode());
    } else if (value is ArchivedMessage) {
      buffer.putUint8(130);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129:
        return MessageCapture.decode(readValue(buffer)!);
      case 130:
        return ArchivedMessage.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

class MessagesHostApi {
  /// Constructor for [MessagesHostApi]. The [binaryMessenger] named argument is
  /// available for dependency injection. If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  MessagesHostApi({
    BinaryMessenger? binaryMessenger,
    String messageChannelSuffix = '',
  }) : pigeonVar_binaryMessenger = binaryMessenger,
       pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty
           ? '.$messageChannelSuffix'
           : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// Whether capture is possible, and how much has been kept.
  Future<MessageCapture> captureState() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.captureState$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return pigeonVar_replyValue! as MessageCapture;
  }

  /// Opens the system notification access screen.
  ///
  /// Returns whether a screen could be opened at all. Some heavily modified ROMs
  /// hide it, and an app that silently does nothing on those is worse than one
  /// that says it could not get there.
  Future<bool> openListenerSettings() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.openListenerSettings$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return pigeonVar_replyValue! as bool;
  }

  /// Turns archiving on or off inside this app.
  ///
  /// Off does not delete what has been kept. Clearing is [clear], deliberately a
  /// separate and explicit act.
  Future<void> setCapturing(bool value) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.setCapturing$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[value],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  /// Newest first. [conversation] null means every chat.
  Future<List<ArchivedMessage>> messages(
    String? conversation,
    int limit,
  ) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.messages$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[conversation, limit],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return (pigeonVar_replyValue! as List<Object?>).cast<ArchivedMessage>();
  }

  /// Every conversation seen, newest activity first.
  Future<List<String>> conversations() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.conversations$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return (pigeonVar_replyValue! as List<Object?>).cast<String>();
  }

  /// Deletes the archive.
  Future<void> clear() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.MessagesHostApi.clear$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }
}
