// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: unused_import, unused_shown_name
// ignore_for_file: type=lint

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List;

import 'package:flutter/services.dart';
import 'package:meta/meta.dart' show immutable, protected, visibleForTesting;

Object? _extractReplyValueOrThrow(
  List<Object?>? replyList,
  String channelName, {
  required bool isNullValid,
}) {
  if (replyList == null) {
    throw PlatformException(
      code: 'channel-error',
      message: 'Unable to establish connection on channel: "$channelName".',
    );
  } else if (replyList.length > 1) {
    throw PlatformException(
      code: replyList[0]! as String,
      message: replyList[1] as String?,
      details: replyList[2],
    );
  } else if (!isNullValid && (replyList.isNotEmpty && replyList[0] == null)) {
    throw PlatformException(
      code: 'null-error',
      message: 'Host platform returned null value for non-null return value.',
    );
  }
  return replyList.firstOrNull;
}

bool _deepEquals(Object? a, Object? b) {
  if (identical(a, b)) {
    return true;
  }
  if (a is double && b is double) {
    if (a.isNaN && b.isNaN) {
      return true;
    }
    return a == b;
  }
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed.every(
          ((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]),
        );
  }
  if (a is Map && b is Map) {
    if (a.length != b.length) {
      return false;
    }
    for (final MapEntry<Object?, Object?> entryA in a.entries) {
      bool found = false;
      for (final MapEntry<Object?, Object?> entryB in b.entries) {
        if (_deepEquals(entryA.key, entryB.key)) {
          if (_deepEquals(entryA.value, entryB.value)) {
            found = true;
            break;
          } else {
            return false;
          }
        }
      }
      if (!found) {
        return false;
      }
    }
    return true;
  }
  return a == b;
}

int _deepHash(Object? value) {
  if (value is List) {
    return Object.hashAll(value.map(_deepHash));
  }
  if (value is Map) {
    int result = 0;
    for (final MapEntry<Object?, Object?> entry in value.entries) {
      result += (_deepHash(entry.key) * 31) ^ _deepHash(entry.value);
    }
    return result;
  }
  if (value is double && value.isNaN) {
    // Normalize NaN to a consistent hash.
    return 0x7FF8000000000000.hashCode;
  }
  if (value is double && value == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return 0.0.hashCode;
  }
  return value.hashCode;
}

/// A server the user has told us about. Codec 129.
///
/// ─── NO PASSWORD FIELD, AND THAT IS DELIBERATE ───────────────────────────────
///
/// The password crosses this bridge exactly once, as an argument to [save], and
/// is never handed back. Native keeps it in EncryptedSharedPreferences and Dart
/// has no way to read it again.
///
/// If it were a field here it would be in every state read, sitting in Dart heap
/// for the life of the app and printable by any careless log line. There is no
/// screen that needs to display it: a password field shows dots, and re-entry is
/// safer than recall.
class ServerConfig {
  ServerConfig({
    required this.id,
    required this.protocol,
    required this.label,
    required this.host,
    required this.port,
    this.share,
    required this.username,
    required this.remotePath,
    required this.encrypt,
    required this.wifiOnly,
    required this.whileCharging,
    required this.scheduled,
    this.secure,
    this.basePath,
    this.certPin,
  });

  String id;

  /// "smb" | "sftp" | "webdav"
  ///
  /// A String rather than an enum, matching every other schema here: an enum
  /// numbers before classes and adding a fourth protocol would renumber all
  /// five.
  String protocol;

  /// What the user calls it. Defaults to the host, because a person with two
  /// servers needs to tell them apart and an IP address does not help.
  String label;

  String host;

  int port;

  /// SMB only. The share name, which SFTP and WebDAV do not have.
  String? share;

  String username;

  /// Folder on the server. Everything is written under here.
  String remotePath;

  /// OFF BY DEFAULT, and free rather than sold.
  ///
  /// The point of a home server is that it is yours: readable photos on your own
  /// NAS can be opened by your desktop, served by Plex, and backed up again by
  /// whatever you already run. Encrypting turns your machine into a blob store
  /// only this app can read, which is the property people dislike about cloud
  /// services.
  ///
  /// It earns its place for a server you do not fully control, such as a VPS,
  /// which is why it is a switch with that reason attached rather than a
  /// default.
  bool encrypt;

  bool wifiOnly;

  bool whileCharging;

  /// Unattended nightly runs. The Pro half of this feature, because it sells
  /// labour rather than capability: connecting, copying and reclaiming all work
  /// free, by hand.
  bool scheduled;

  /// HTTPS rather than plain HTTP. WebDAV only.
  ///
  /// Null means HTTPS, because null is what an SMB record and every server
  /// saved before this field existed will report, and the safe reading of "not
  /// stated" is the encrypted one. Only an explicit false sends credentials in
  /// the clear.
  bool? secure;

  /// The DAV root, taken from the address the user pasted. WebDAV only.
  ///
  /// Separate from [remotePath] because they answer different questions.
  /// This is where the server's WebDAV endpoint lives and the user copies it
  /// from their server settings; remotePath is the folder they chose inside it
  /// and is theirs to name. Nextcloud gives out
  /// /remote.php/dav/files/<user>, which nobody would type by hand and nobody
  /// should have to keep re-typing when they rename their backup folder.
  String? basePath;

  /// SHA-256 of a certificate the user chose to trust, in hex. WebDAV only.
  ///
  /// ─── PINNED, NOT TRUSTED BLINDLY ─────────────────────────────────────────
  ///
  /// A self-hosted server usually presents its own certificate, so refusing
  /// outright would rule out most of the servers this feature exists for. The
  /// alternative most apps reach for is a "trust all certificates" switch,
  /// which is one tap and turns HTTPS into an unauthenticated channel for
  /// everything afterwards.
  ///
  /// This holds ONE certificate the user checked once, by fingerprint. Anything
  /// else is still refused, so a network attacker's certificate fails even
  /// though the real server's own certificate passes.
  String? certPin;

  List<Object?> _toList() {
    return <Object?>[
      id,
      protocol,
      label,
      host,
      port,
      share,
      username,
      remotePath,
      encrypt,
      wifiOnly,
      whileCharging,
      scheduled,
      secure,
      basePath,
      certPin,
    ];
  }

  Object encode() {
    return _toList();
  }

  static ServerConfig decode(Object result) {
    result as List<Object?>;
    return ServerConfig(
      id: result[0]! as String,
      protocol: result[1]! as String,
      label: result[2]! as String,
      host: result[3]! as String,
      port: result[4]! as int,
      share: result[5] as String?,
      username: result[6]! as String,
      remotePath: result[7]! as String,
      encrypt: result[8]! as bool,
      wifiOnly: result[9]! as bool,
      whileCharging: result[10]! as bool,
      scheduled: result[11]! as bool,
      secure: result[12] as bool?,
      basePath: result[13] as String?,
      certPin: result[14] as String?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! ServerConfig || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(id, other.id) &&
        _deepEquals(protocol, other.protocol) &&
        _deepEquals(label, other.label) &&
        _deepEquals(host, other.host) &&
        _deepEquals(port, other.port) &&
        _deepEquals(share, other.share) &&
        _deepEquals(username, other.username) &&
        _deepEquals(remotePath, other.remotePath) &&
        _deepEquals(encrypt, other.encrypt) &&
        _deepEquals(wifiOnly, other.wifiOnly) &&
        _deepEquals(whileCharging, other.whileCharging) &&
        _deepEquals(scheduled, other.scheduled) &&
        _deepEquals(secure, other.secure) &&
        _deepEquals(basePath, other.basePath) &&
        _deepEquals(certPin, other.certPin);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'ServerConfig(id: $id, protocol: $protocol, label: $label, host: $host, port: $port, share: $share, username: $username, remotePath: $remotePath, encrypt: $encrypt, wifiOnly: $wifiOnly, whileCharging: $whileCharging, scheduled: $scheduled, secure: $secure, basePath: $basePath, certPin: $certPin)';
  }
}

/// The result of trying to reach a server. Codec 130.
class ServerProbe {
  ServerProbe({
    required this.reachable,
    required this.writable,
    required this.detail,
    this.freeBytes,
    this.code,
    this.certFingerprint,
    this.serverName,
  });

  bool reachable;

  /// Reachable and writable are separate on purpose.
  ///
  /// A share that accepts the login and refuses a write is the commonest
  /// misconfiguration there is, and discovering it at 2am during the first
  /// scheduled run is far too late. The test writes a small file and removes it.
  bool writable;

  /// Why it failed, in words a person can act on. Empty when it worked.
  ///
  /// Never a raw exception. "Connection refused" tells someone nothing; "nothing
  /// is listening on port 445, check the server is on" tells them what to do.
  String detail;

  /// Free space on the share, or null where the protocol does not report it.
  int? freeBytes;

  /// Which outcome this is, for the UI to branch on. Null from older code.
  ///
  /// ─── WHY [detail] IS NOT ENOUGH ──────────────────────────────────────────
  ///
  /// detail is a sentence for a person and must stay that way. But one of these
  /// outcomes needs a button the others do not: an untrusted certificate is the
  /// only failure the user can resolve from inside the app, by checking a
  /// fingerprint and pinning it. Deciding that from the text would mean
  /// matching on English, which breaks the moment the string is translated.
  ///
  /// One of: "ok", "auth", "cert", "not_dav", "path", "space", "network".
  /// Anything unrecognised must be treated as a plain failure and shown as
  /// [detail], so a newer native layer can add a code without breaking an
  /// older screen.
  String? code;

  /// SHA-256 of the certificate that was refused, in hex. Set only with "cert".
  ///
  /// Shown so the user can compare it against their own server before pinning
  /// it. Displaying it is the entire point: a fingerprint nobody checks is a
  /// trust-all switch with extra steps.
  String? certFingerprint;

  /// What the server calls itself, when it says. Absent rather than guessed.
  String? serverName;

  List<Object?> _toList() {
    return <Object?>[
      reachable,
      writable,
      detail,
      freeBytes,
      code,
      certFingerprint,
      serverName,
    ];
  }

  Object encode() {
    return _toList();
  }

  static ServerProbe decode(Object result) {
    result as List<Object?>;
    return ServerProbe(
      reachable: result[0]! as bool,
      writable: result[1]! as bool,
      detail: result[2]! as String,
      freeBytes: result[3] as int?,
      code: result[4] as String?,
      certFingerprint: result[5] as String?,
      serverName: result[6] as String?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! ServerProbe || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(reachable, other.reachable) &&
        _deepEquals(writable, other.writable) &&
        _deepEquals(detail, other.detail) &&
        _deepEquals(freeBytes, other.freeBytes) &&
        _deepEquals(code, other.code) &&
        _deepEquals(certFingerprint, other.certFingerprint) &&
        _deepEquals(serverName, other.serverName);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'ServerProbe(reachable: $reachable, writable: $writable, detail: $detail, freeBytes: $freeBytes, code: $code, certFingerprint: $certFingerprint, serverName: $serverName)';
  }
}

/// How a copy is going. Codec 131.
class TransferState {
  TransferState({
    required this.running,
    required this.sent,
    required this.total,
    required this.bytesSent,
    required this.bytesTotal,
    this.currentName,
    required this.failed,
    this.lastRunMillis,
    this.lastError,
  });

  bool running;

  int sent;

  int total;

  int bytesSent;

  int bytesTotal;

  /// What is being copied right now, for the progress line. Null between files.
  String? currentName;

  /// Files that could not be copied. Reported rather than retried forever: a
  /// file that fails twice is usually one the server will never accept, and a
  /// silent infinite retry is how a backup appears to run all night and achieve
  /// nothing.
  int failed;

  int? lastRunMillis;

  /// Null when the last run finished cleanly.
  String? lastError;

  List<Object?> _toList() {
    return <Object?>[
      running,
      sent,
      total,
      bytesSent,
      bytesTotal,
      currentName,
      failed,
      lastRunMillis,
      lastError,
    ];
  }

  Object encode() {
    return _toList();
  }

  static TransferState decode(Object result) {
    result as List<Object?>;
    return TransferState(
      running: result[0]! as bool,
      sent: result[1]! as int,
      total: result[2]! as int,
      bytesSent: result[3]! as int,
      bytesTotal: result[4]! as int,
      currentName: result[5] as String?,
      failed: result[6]! as int,
      lastRunMillis: result[7] as int?,
      lastError: result[8] as String?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! TransferState || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(running, other.running) &&
        _deepEquals(sent, other.sent) &&
        _deepEquals(total, other.total) &&
        _deepEquals(bytesSent, other.bytesSent) &&
        _deepEquals(bytesTotal, other.bytesTotal) &&
        _deepEquals(currentName, other.currentName) &&
        _deepEquals(failed, other.failed) &&
        _deepEquals(lastRunMillis, other.lastRunMillis) &&
        _deepEquals(lastError, other.lastError);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'TransferState(running: $running, sent: $sent, total: $total, bytesSent: $bytesSent, bytesTotal: $bytesTotal, currentName: $currentName, failed: $failed, lastRunMillis: $lastRunMillis, lastError: $lastError)';
  }
}

/// Something already on the server. Codec 132.
class RemoteFile {
  RemoteFile({
    required this.remotePath,
    required this.name,
    required this.sizeBytes,
    required this.modifiedMillis,
  });

  String remotePath;

  String name;

  int sizeBytes;

  int modifiedMillis;

  List<Object?> _toList() {
    return <Object?>[remotePath, name, sizeBytes, modifiedMillis];
  }

  Object encode() {
    return _toList();
  }

  static RemoteFile decode(Object result) {
    result as List<Object?>;
    return RemoteFile(
      remotePath: result[0]! as String,
      name: result[1]! as String,
      sizeBytes: result[2]! as int,
      modifiedMillis: result[3]! as int,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! RemoteFile || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(remotePath, other.remotePath) &&
        _deepEquals(name, other.name) &&
        _deepEquals(sizeBytes, other.sizeBytes) &&
        _deepEquals(modifiedMillis, other.modifiedMillis);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'RemoteFile(remotePath: $remotePath, name: $name, sizeBytes: $sizeBytes, modifiedMillis: $modifiedMillis)';
  }
}

/// A local file that could be removed because the server has it. Codec 133.
class ReclaimCandidate {
  ReclaimCandidate({
    required this.fileId,
    required this.name,
    required this.sizeBytes,
    required this.verified,
  });

  String fileId;

  String name;

  int sizeBytes;

  /// Whether the copy on the server was checked and matched.
  ///
  /// FALSE MEANS DO NOT TOUCH IT. The reclaim flow removes originals, so a
  /// candidate that has not been verified by size and checksum is offered to
  /// nobody. This is the one place in the app where being wrong destroys
  /// something.
  bool verified;

  List<Object?> _toList() {
    return <Object?>[fileId, name, sizeBytes, verified];
  }

  Object encode() {
    return _toList();
  }

  static ReclaimCandidate decode(Object result) {
    result as List<Object?>;
    return ReclaimCandidate(
      fileId: result[0]! as String,
      name: result[1]! as String,
      sizeBytes: result[2]! as int,
      verified: result[3]! as bool,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! ReclaimCandidate || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(fileId, other.fileId) &&
        _deepEquals(name, other.name) &&
        _deepEquals(sizeBytes, other.sizeBytes) &&
        _deepEquals(verified, other.verified);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'ReclaimCandidate(fileId: $fileId, name: $name, sizeBytes: $sizeBytes, verified: $verified)';
  }
}

class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();

  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    } else if (value is ServerConfig) {
      buffer.putUint8(129);
      writeValue(buffer, value.encode());
    } else if (value is ServerProbe) {
      buffer.putUint8(130);
      writeValue(buffer, value.encode());
    } else if (value is TransferState) {
      buffer.putUint8(131);
      writeValue(buffer, value.encode());
    } else if (value is RemoteFile) {
      buffer.putUint8(132);
      writeValue(buffer, value.encode());
    } else if (value is ReclaimCandidate) {
      buffer.putUint8(133);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129:
        return ServerConfig.decode(readValue(buffer)!);
      case 130:
        return ServerProbe.decode(readValue(buffer)!);
      case 131:
        return TransferState.decode(readValue(buffer)!);
      case 132:
        return RemoteFile.decode(readValue(buffer)!);
      case 133:
        return ReclaimCandidate.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

class ServerHostApi {
  /// Constructor for [ServerHostApi]. The [binaryMessenger] named argument is
  /// available for dependency injection. If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  ServerHostApi({
    BinaryMessenger? binaryMessenger,
    String messageChannelSuffix = '',
  }) : pigeonVar_binaryMessenger = binaryMessenger,
       pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty
           ? '.$messageChannelSuffix'
           : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// The saved server, or null when none is set up. One at a time, for now.
  Future<ServerConfig?> current() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.current$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
    return pigeonVar_replyValue as ServerConfig?;
  }

  /// Tries to reach a server without saving anything.
  ///
  /// [password] is passed for the test and not retained. A person changing a
  /// setting on a saved server should not have to retype it, so an empty
  /// password here means use the stored one.
  Future<ServerProbe> test(ServerConfig config, String password) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.test$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[config, password],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return pigeonVar_replyValue! as ServerProbe;
  }

  /// Saves, after a successful test. An empty [password] keeps the stored one.
  Future<void> save(ServerConfig config, String password) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.save$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[config, password],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  /// Forgets the server and its password. Nothing on the server is touched.
  Future<void> forget() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.forget$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  /// Turns the nightly run on or off.
  ///
  /// ─── SEPARATE FROM save, DELIBERATELY ────────────────────────────────────
  ///
  /// Saving a config writes settings. This schedules or cancels actual work in
  /// the system, and folding it into save would mean every edit to a port
  /// number silently re-queued a job.
  Future<void> setSchedule(bool enabled) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.setSchedule$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[enabled],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  /// When the next run is due, or null when nothing is scheduled.
  ///
  /// From WorkManager rather than computed, so a phone that has deferred the
  /// job for battery reasons reports what will actually happen rather than what
  /// was asked for.
  Future<int?> nextRunMillis() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.nextRunMillis$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
    return pigeonVar_replyValue as int?;
  }

  /// Starts a copy. Returns at once; watch [transferState].
  Future<void> startBackup() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.startBackup$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  Future<void> cancelBackup() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.cancelBackup$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }

  Future<TransferState> transferState() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.transferState$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return pigeonVar_replyValue! as TransferState;
  }

  /// Checks the chosen files byte for byte before they are removed.
  ///
  /// ─── SEPARATE FROM [reclaimable], AND THAT IS THE POINT ──────────────────
  ///
  /// The listing compares sizes, which is cheap enough to run over a whole
  /// library and catches a file that was truncated or replaced. It does not
  /// catch a file of the same length whose contents differ, which is exactly
  /// the case where deleting the local copy loses something.
  ///
  /// So the hash runs only on what the user actually selected: reading both
  /// sides of four thousand files to answer a question about the twelve they
  /// chose would take minutes and achieve nothing.
  ///
  /// Returns the ids that MATCHED. Anything missing from the result failed and
  /// must not be touched.
  Future<List<String>> verify(List<String> fileIds) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.verify$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[fileIds],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return (pigeonVar_replyValue! as List<Object?>).cast<String>();
  }

  /// Local files whose copy on the server has been verified.
  ///
  /// Verification is size and checksum, done here rather than trusted from the
  /// upload, because the interesting failure is a file that uploaded months ago
  /// and has since been moved or truncated.
  Future<List<ReclaimCandidate>> reclaimable(int limit) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.ServerHostApi.reclaimable$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[limit],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return (pigeonVar_replyValue! as List<Object?>).cast<ReclaimCandidate>();
  }
}
