// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: unused_import, unused_shown_name
// ignore_for_file: type=lint

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List;

import 'package:flutter/services.dart';
import 'package:meta/meta.dart' show immutable, protected, visibleForTesting;

Object? _extractReplyValueOrThrow(
  List<Object?>? replyList,
  String channelName, {
  required bool isNullValid,
}) {
  if (replyList == null) {
    throw PlatformException(
      code: 'channel-error',
      message: 'Unable to establish connection on channel: "$channelName".',
    );
  } else if (replyList.length > 1) {
    throw PlatformException(
      code: replyList[0]! as String,
      message: replyList[1] as String?,
      details: replyList[2],
    );
  } else if (!isNullValid && (replyList.isNotEmpty && replyList[0] == null)) {
    throw PlatformException(
      code: 'null-error',
      message: 'Host platform returned null value for non-null return value.',
    );
  }
  return replyList.firstOrNull;
}

List<Object?> wrapResponse({
  Object? result,
  PlatformException? error,
  bool empty = false,
}) {
  if (empty) {
    return <Object?>[];
  }
  if (error == null) {
    return <Object?>[result];
  }
  return <Object?>[error.code, error.message, error.details];
}

bool _deepEquals(Object? a, Object? b) {
  if (identical(a, b)) {
    return true;
  }
  if (a is double && b is double) {
    if (a.isNaN && b.isNaN) {
      return true;
    }
    return a == b;
  }
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed.every(
          ((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]),
        );
  }
  if (a is Map && b is Map) {
    if (a.length != b.length) {
      return false;
    }
    for (final MapEntry<Object?, Object?> entryA in a.entries) {
      bool found = false;
      for (final MapEntry<Object?, Object?> entryB in b.entries) {
        if (_deepEquals(entryA.key, entryB.key)) {
          if (_deepEquals(entryA.value, entryB.value)) {
            found = true;
            break;
          } else {
            return false;
          }
        }
      }
      if (!found) {
        return false;
      }
    }
    return true;
  }
  return a == b;
}

int _deepHash(Object? value) {
  if (value is List) {
    return Object.hashAll(value.map(_deepHash));
  }
  if (value is Map) {
    int result = 0;
    for (final MapEntry<Object?, Object?> entry in value.entries) {
      result += (_deepHash(entry.key) * 31) ^ _deepHash(entry.value);
    }
    return result;
  }
  if (value is double && value.isNaN) {
    // Normalize NaN to a consistent hash.
    return 0x7FF8000000000000.hashCode;
  }
  if (value is double && value == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return 0.0.hashCode;
  }
  return value.hashCode;
}

/// A set of files that belong together. Codec 129.
class CompareGroup {
  CompareGroup({
    required this.groupId,
    required this.kind,
    required this.fileIds,
    required this.totalBytes,
    required this.wastedBytes,
    required this.keepFileId,
    required this.sizes,
  });

  String groupId;

  /// "exact" for byte identical copies, "similar" for near duplicates.
  ///
  /// A String rather than an enum, matching every other schema in this app: an
  /// enum numbers before classes and adding a third kind would renumber
  /// everything after it.
  String kind;

  /// Every member, largest first, so the head of the list is the one worth
  /// keeping on size alone.
  List<String> fileIds;

  int totalBytes;

  /// What removing everything except the keeper would free.
  ///
  /// Reported separately from [totalBytes] because they are wildly different
  /// numbers and confusing them is how a cleaner app claims to free forty
  /// gigabytes and frees four.
  int wastedBytes;

  /// The suggestion, never the decision.
  ///
  /// Largest file in the group, since for two encodings of one photo the bigger
  /// one carries more detail. The UI preselects the others and lets the user
  /// change it; nothing is ever removed on this recommendation alone.
  String keepFileId;

  /// Size per file, in the same order as [fileIds].
  ///
  /// APPENDED LAST, so nothing before it renumbers.
  ///
  /// Without it the UI divided the group total evenly, which is exact for byte
  /// identical copies and wrong for near duplicates, where a shared copy can be
  /// a fifth the size of the original. Once a user can keep more than one file
  /// in a set, that approximation stops being cosmetic: the figure on the button
  /// is what they are deciding on.
  List<int> sizes;

  List<Object?> _toList() {
    return <Object?>[
      groupId,
      kind,
      fileIds,
      totalBytes,
      wastedBytes,
      keepFileId,
      sizes,
    ];
  }

  Object encode() {
    return _toList();
  }

  static CompareGroup decode(Object result) {
    result as List<Object?>;
    return CompareGroup(
      groupId: result[0]! as String,
      kind: result[1]! as String,
      fileIds: (result[2]! as List<Object?>).cast<String>(),
      totalBytes: result[3]! as int,
      wastedBytes: result[4]! as int,
      keepFileId: result[5]! as String,
      sizes: (result[6]! as List<Object?>).cast<int>(),
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! CompareGroup || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(groupId, other.groupId) &&
        _deepEquals(kind, other.kind) &&
        _deepEquals(fileIds, other.fileIds) &&
        _deepEquals(totalBytes, other.totalBytes) &&
        _deepEquals(wastedBytes, other.wastedBytes) &&
        _deepEquals(keepFileId, other.keepFileId) &&
        _deepEquals(sizes, other.sizes);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'CompareGroup(groupId: $groupId, kind: $kind, fileIds: $fileIds, totalBytes: $totalBytes, wastedBytes: $wastedBytes, keepFileId: $keepFileId, sizes: $sizes)';
  }
}

/// One image judged soft. Codec 130.
class BlurredImage {
  BlurredImage({
    required this.fileId,
    required this.sharpness,
    required this.sizeBytes,
  });

  String fileId;

  /// Variance of the Laplacian. Higher is sharper.
  ///
  /// Reported raw rather than as a verdict so the UI can sort by it and a
  /// threshold can move without a native change. It is scale dependent, which
  /// is why every image is measured at the same working size.
  double sharpness;

  int sizeBytes;

  List<Object?> _toList() {
    return <Object?>[fileId, sharpness, sizeBytes];
  }

  Object encode() {
    return _toList();
  }

  static BlurredImage decode(Object result) {
    result as List<Object?>;
    return BlurredImage(
      fileId: result[0]! as String,
      sharpness: result[1]! as double,
      sizeBytes: result[2]! as int,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! BlurredImage || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(fileId, other.fileId) &&
        _deepEquals(sharpness, other.sharpness) &&
        _deepEquals(sizeBytes, other.sizeBytes);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'BlurredImage(fileId: $fileId, sharpness: $sharpness, sizeBytes: $sizeBytes)';
  }
}

/// What a scan found. Codec 131.
class CompareResult {
  CompareResult({
    required this.groups,
    required this.blurred,
    required this.scanned,
    required this.cancelled,
  });

  List<CompareGroup> groups;

  List<BlurredImage> blurred;

  int scanned;

  /// True when the user stopped it. The findings so far are real and are kept;
  /// what is not true is that they are complete, and the UI says so.
  bool cancelled;

  List<Object?> _toList() {
    return <Object?>[groups, blurred, scanned, cancelled];
  }

  Object encode() {
    return _toList();
  }

  static CompareResult decode(Object result) {
    result as List<Object?>;
    return CompareResult(
      groups: (result[0]! as List<Object?>).cast<CompareGroup>(),
      blurred: (result[1]! as List<Object?>).cast<BlurredImage>(),
      scanned: result[2]! as int,
      cancelled: result[3]! as bool,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! CompareResult || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(groups, other.groups) &&
        _deepEquals(blurred, other.blurred) &&
        _deepEquals(scanned, other.scanned) &&
        _deepEquals(cancelled, other.cancelled);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'CompareResult(groups: $groups, blurred: $blurred, scanned: $scanned, cancelled: $cancelled)';
  }
}

/// How far along. Codec 132.
class CompareProgress {
  CompareProgress({
    required this.scanned,
    required this.total,
    required this.found,
    required this.done,
  });

  int scanned;

  int total;

  int found;

  bool done;

  List<Object?> _toList() {
    return <Object?>[scanned, total, found, done];
  }

  Object encode() {
    return _toList();
  }

  static CompareProgress decode(Object result) {
    result as List<Object?>;
    return CompareProgress(
      scanned: result[0]! as int,
      total: result[1]! as int,
      found: result[2]! as int,
      done: result[3]! as bool,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! CompareProgress || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(scanned, other.scanned) &&
        _deepEquals(total, other.total) &&
        _deepEquals(found, other.found) &&
        _deepEquals(done, other.done);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'CompareProgress(scanned: $scanned, total: $total, found: $found, done: $done)';
  }
}

class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();

  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    } else if (value is CompareGroup) {
      buffer.putUint8(129);
      writeValue(buffer, value.encode());
    } else if (value is BlurredImage) {
      buffer.putUint8(130);
      writeValue(buffer, value.encode());
    } else if (value is CompareResult) {
      buffer.putUint8(131);
      writeValue(buffer, value.encode());
    } else if (value is CompareProgress) {
      buffer.putUint8(132);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129:
        return CompareGroup.decode(readValue(buffer)!);
      case 130:
        return BlurredImage.decode(readValue(buffer)!);
      case 131:
        return CompareResult.decode(readValue(buffer)!);
      case 132:
        return CompareProgress.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

class CompareHostApi {
  /// Constructor for [CompareHostApi]. The [binaryMessenger] named argument is
  /// available for dependency injection. If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  CompareHostApi({
    BinaryMessenger? binaryMessenger,
    String messageChannelSuffix = '',
  }) : pigeonVar_binaryMessenger = binaryMessenger,
       pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty
           ? '.$messageChannelSuffix'
           : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// Decodes every image and computes all three answers in one pass.
  ///
  /// [blurThreshold] is passed in so the line between soft and sharp can move
  /// without a native change. Around 100 is a reasonable start on a 256 pixel
  /// working size; below 50 catches only the truly ruined.
  Future<CompareResult> scan(int maxImages, double blurThreshold) async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.CompareHostApi.scan$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(
      <Object?>[maxImages, blurThreshold],
    );
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: false,
    );
    return pigeonVar_replyValue! as CompareResult;
  }

  /// Stops the scan. Whatever has been compared so far is returned by the call
  /// still in flight, with cancelled true.
  Future<void> cancel() async {
    final pigeonVar_channelName =
        'dev.flutter.pigeon.g_recovery.CompareHostApi.cancel$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
      pigeonVar_replyList,
      pigeonVar_channelName,
      isNullValid: true,
    );
  }
}

/// Progress, pushed while a scan runs.
abstract class CompareFlutterApi {
  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  void onCompareProgress(CompareProgress progress);

  static void setUp(
    CompareFlutterApi? api, {
    BinaryMessenger? binaryMessenger,
    String messageChannelSuffix = '',
  }) {
    messageChannelSuffix = messageChannelSuffix.isNotEmpty
        ? '.$messageChannelSuffix'
        : '';
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
        'dev.flutter.pigeon.g_recovery.CompareFlutterApi.onCompareProgress$messageChannelSuffix',
        pigeonChannelCodec,
        binaryMessenger: binaryMessenger,
      );
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final CompareProgress arg_progress = args[0]! as CompareProgress;
          try {
            api.onCompareProgress(arg_progress);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          } catch (e) {
            return wrapResponse(
              error: PlatformException(code: 'error', message: e.toString()),
            );
          }
        });
      }
    }
  }
}
