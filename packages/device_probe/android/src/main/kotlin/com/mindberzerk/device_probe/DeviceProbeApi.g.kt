// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mindberzerk.device_probe

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object DeviceProbeApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * What THIS DEVICE will actually answer. Codec 129.
 *
 * A SEPARATE ANSWER FROM A SNAPSHOT OF NULLS. A null in [DeviceSnapshot] is
 * ambiguous: it can mean "this kernel will never tell us" or "not sampled yet",
 * and those need different UI. Permanently absent is a sentence explaining why;
 * pending is a shimmer.
 *
 * Probed ONCE natively and cached for the process lifetime. Every field here is
 * the result of an actual attempted read, not a version check, because SELinux
 * policy is per-ROM: the same API level reads core frequencies on a Tecno and
 * refuses on a hardened Samsung.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ProbeCapabilities (
  /** `scaling_cur_freq` per core. False on most hardened ROMs. */
  val coreFrequencies: Boolean,
  /**
   * Whether the kernel published its frequency domains under
   * `cpufreq/policyN`, in the `related_cpus` file. When false, clusters were
   * inferred from distinct max frequencies instead, which is a good guess and
   * not a fact.
   *
   * NO SHELL GLOBS IN THIS FILE. See the header: a `*` before a `/` closes the
   * generated KDoc block early and the rest of the sentence lands inside a
   * Kotlin constructor.
   */
  val cpuClusters: Boolean,
  val cpuGovernor: Boolean,
  /**
   * Aggregate `/proc/stat`. Almost always false on Android 12+. Kept as an
   * honest field so the answer is "this device says no" rather than "we never
   * asked".
   */
  val cpuJiffies: Boolean,
  val thermalZones: Boolean,
  /** `PowerManager.getCurrentThermalStatus()`, API 29+. */
  val thermalStatus: Boolean,
  /** Level and charging state. True on effectively every device. */
  val battery: Boolean,
  /**
   * Temperature, current draw, voltage. Separate from [battery] because the
   * sticky broadcast can carry a level while `CURRENT_NOW` returns nothing.
   */
  val batteryDetail: Boolean,
  /** `BATTERY_PROPERTY_CYCLE_COUNT`, API 34+ and OEM-dependent even there. */
  val batteryCycleCount: Boolean,
  val memory: Boolean,
  /**
   * zram and swap from `/proc/meminfo`. Usually readable where `/proc/stat`
   * is not, because meminfo has no per-process information to leak.
   */
  val swap: Boolean,
  /** 0 means the sensor list came back empty, which happens on emulators. */
  val sensorCount: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ProbeCapabilities {
      val coreFrequencies = pigeonVar_list[0] as Boolean
      val cpuClusters = pigeonVar_list[1] as Boolean
      val cpuGovernor = pigeonVar_list[2] as Boolean
      val cpuJiffies = pigeonVar_list[3] as Boolean
      val thermalZones = pigeonVar_list[4] as Boolean
      val thermalStatus = pigeonVar_list[5] as Boolean
      val battery = pigeonVar_list[6] as Boolean
      val batteryDetail = pigeonVar_list[7] as Boolean
      val batteryCycleCount = pigeonVar_list[8] as Boolean
      val memory = pigeonVar_list[9] as Boolean
      val swap = pigeonVar_list[10] as Boolean
      val sensorCount = pigeonVar_list[11] as Long
      return ProbeCapabilities(coreFrequencies, cpuClusters, cpuGovernor, cpuJiffies, thermalZones, thermalStatus, battery, batteryDetail, batteryCycleCount, memory, swap, sensorCount)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      coreFrequencies,
      cpuClusters,
      cpuGovernor,
      cpuJiffies,
      thermalZones,
      thermalStatus,
      battery,
      batteryDetail,
      batteryCycleCount,
      memory,
      swap,
      sensorCount,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ProbeCapabilities
    return DeviceProbeApiPigeonUtils.deepEquals(this.coreFrequencies, other.coreFrequencies) && DeviceProbeApiPigeonUtils.deepEquals(this.cpuClusters, other.cpuClusters) && DeviceProbeApiPigeonUtils.deepEquals(this.cpuGovernor, other.cpuGovernor) && DeviceProbeApiPigeonUtils.deepEquals(this.cpuJiffies, other.cpuJiffies) && DeviceProbeApiPigeonUtils.deepEquals(this.thermalZones, other.thermalZones) && DeviceProbeApiPigeonUtils.deepEquals(this.thermalStatus, other.thermalStatus) && DeviceProbeApiPigeonUtils.deepEquals(this.battery, other.battery) && DeviceProbeApiPigeonUtils.deepEquals(this.batteryDetail, other.batteryDetail) && DeviceProbeApiPigeonUtils.deepEquals(this.batteryCycleCount, other.batteryCycleCount) && DeviceProbeApiPigeonUtils.deepEquals(this.memory, other.memory) && DeviceProbeApiPigeonUtils.deepEquals(this.swap, other.swap) && DeviceProbeApiPigeonUtils.deepEquals(this.sensorCount, other.sensorCount)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.coreFrequencies)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.cpuClusters)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.cpuGovernor)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.cpuJiffies)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.thermalZones)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.thermalStatus)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.battery)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.batteryDetail)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.batteryCycleCount)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.memory)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.swap)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.sensorCount)
    return result
  }
  override fun toString(): String {
    return "ProbeCapabilities(coreFrequencies=$coreFrequencies, cpuClusters=$cpuClusters, cpuGovernor=$cpuGovernor, cpuJiffies=$cpuJiffies, thermalZones=$thermalZones, thermalStatus=$thermalStatus, battery=$battery, batteryDetail=$batteryDetail, batteryCycleCount=$batteryCycleCount, memory=$memory, swap=$swap, sensorCount=$sensorCount)"
  }
}

/**
 * One frequency domain. Codec 130.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CpuCluster (
  /**
   * "Prime", "Gold", "Silver", "Big", "Mid", "Little", or "CPU".
   *
   * A STRING, NOT AN ENUM, for the reason in the file header. Also because the
   * naming depends on the topology: a 1 + 3 + 4 layout gets Prime/Gold/Silver,
   * a 4 + 4 gets Big/Little, and a uniform part gets one cluster called CPU.
   */
  val label: String,
  /** Zero-based core indices in this domain, ascending. */
  val coreIds: List<Long>,
  val minKhz: Long? = null,
  val maxKhz: Long? = null,
  val governor: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CpuCluster {
      val label = pigeonVar_list[0] as String
      val coreIds = pigeonVar_list[1] as List<Long>
      val minKhz = pigeonVar_list[2] as Long?
      val maxKhz = pigeonVar_list[3] as Long?
      val governor = pigeonVar_list[4] as String?
      return CpuCluster(label, coreIds, minKhz, maxKhz, governor)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      label,
      coreIds,
      minKhz,
      maxKhz,
      governor,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CpuCluster
    return DeviceProbeApiPigeonUtils.deepEquals(this.label, other.label) && DeviceProbeApiPigeonUtils.deepEquals(this.coreIds, other.coreIds) && DeviceProbeApiPigeonUtils.deepEquals(this.minKhz, other.minKhz) && DeviceProbeApiPigeonUtils.deepEquals(this.maxKhz, other.maxKhz) && DeviceProbeApiPigeonUtils.deepEquals(this.governor, other.governor)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.label)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.coreIds)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.minKhz)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.maxKhz)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.governor)
    return result
  }
  override fun toString(): String {
    return "CpuCluster(label=$label, coreIds=$coreIds, minKhz=$minKhz, maxKhz=$maxKhz, governor=$governor)"
  }
}

/**
 * Static CPU description, read once. Codec 131.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CpuInfo (
  val coreCount: Long,
  /**
   * Ordered fastest first. Empty when nothing under `/sys/devices/system/cpu`
   * could be read, which is not the same as a single-cluster device.
   */
  val clusters: List<CpuCluster>,
  /**
   * `Build.SOC_MODEL`, API 31+. Null below that or when the OEM leaves it
   * unknown, which several Transsion builds do.
   */
  val socModel: String? = null,
  val hardware: String? = null,
  val abi: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CpuInfo {
      val coreCount = pigeonVar_list[0] as Long
      val clusters = pigeonVar_list[1] as List<CpuCluster>
      val socModel = pigeonVar_list[2] as String?
      val hardware = pigeonVar_list[3] as String?
      val abi = pigeonVar_list[4] as String?
      return CpuInfo(coreCount, clusters, socModel, hardware, abi)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      coreCount,
      clusters,
      socModel,
      hardware,
      abi,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CpuInfo
    return DeviceProbeApiPigeonUtils.deepEquals(this.coreCount, other.coreCount) && DeviceProbeApiPigeonUtils.deepEquals(this.clusters, other.clusters) && DeviceProbeApiPigeonUtils.deepEquals(this.socModel, other.socModel) && DeviceProbeApiPigeonUtils.deepEquals(this.hardware, other.hardware) && DeviceProbeApiPigeonUtils.deepEquals(this.abi, other.abi)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.coreCount)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.clusters)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.socModel)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.hardware)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.abi)
    return result
  }
  override fun toString(): String {
    return "CpuInfo(coreCount=$coreCount, clusters=$clusters, socModel=$socModel, hardware=$hardware, abi=$abi)"
  }
}

/**
 * Live CPU sample. Codec 132.
 *
 * CUMULATIVE COUNTERS, NOT RATES. A rate needs two samples and an interval, and
 * the ticker that owns the interval lives in Dart. Native reads and returns.
 * The delta arithmetic stays in one tested place, and it stays correct when a
 * frame is late because [DeviceSnapshot.elapsedRealtimeMillis] is the divisor.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CpuSample (
  /**
   * Per core, index-aligned with [CpuCluster.coreIds]. An entry is null when
   * the core is offline OR unreadable, which is why [coreOnline] exists
   * alongside rather than being inferred from a null here.
   */
  val coreKhz: List<Long?>,
  /**
   * From each core's `online` file. Null where that file itself is
   * unreadable. Core 0 has no
   * `online` file on many kernels because it cannot be offlined, and is
   * reported as true.
   */
  val coreOnline: List<Boolean?>,
  /**
   * Aggregate `cpu` line of `/proc/stat`: idle+iowait, and the grand total.
   * Both null together or neither.
   */
  val idleJiffies: Long? = null,
  val totalJiffies: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CpuSample {
      val coreKhz = pigeonVar_list[0] as List<Long?>
      val coreOnline = pigeonVar_list[1] as List<Boolean?>
      val idleJiffies = pigeonVar_list[2] as Long?
      val totalJiffies = pigeonVar_list[3] as Long?
      return CpuSample(coreKhz, coreOnline, idleJiffies, totalJiffies)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      coreKhz,
      coreOnline,
      idleJiffies,
      totalJiffies,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CpuSample
    return DeviceProbeApiPigeonUtils.deepEquals(this.coreKhz, other.coreKhz) && DeviceProbeApiPigeonUtils.deepEquals(this.coreOnline, other.coreOnline) && DeviceProbeApiPigeonUtils.deepEquals(this.idleJiffies, other.idleJiffies) && DeviceProbeApiPigeonUtils.deepEquals(this.totalJiffies, other.totalJiffies)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.coreKhz)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.coreOnline)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.idleJiffies)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.totalJiffies)
    return result
  }
  override fun toString(): String {
    return "CpuSample(coreKhz=$coreKhz, coreOnline=$coreOnline, idleJiffies=$idleJiffies, totalJiffies=$totalJiffies)"
  }
}

/**
 * One thermal sensor. Codec 133.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ThermalZone (
  val zoneId: Long,
  /**
   * The raw `type` file contents, for example "battery" or "mtktsbattery" or
   * "VIRTUAL-SKIN". Shown verbatim in the detail list because OEM naming is
   * the only ground truth about what is being measured.
   */
  val label: String,
  /**
   * "battery" | "cpu" | "gpu" | "skin" | "modem" | "ambient" | "other".
   * Derived from [label] by substring match, String for the header's reason.
   * "other" is common and correct, not a failure.
   */
  val category: String,
  /**
   * MILLIDEGREES CELSIUS, normalised here and only here.
   *
   * Kernels report this zone in millidegrees, decidegrees or whole degrees with
   * no way to ask which. Native applies a magnitude heuristic once so every
   * consumer sees one unit. Dart divides by 1000 at the point of display.
   */
  val milliCelsius: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ThermalZone {
      val zoneId = pigeonVar_list[0] as Long
      val label = pigeonVar_list[1] as String
      val category = pigeonVar_list[2] as String
      val milliCelsius = pigeonVar_list[3] as Long
      return ThermalZone(zoneId, label, category, milliCelsius)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      zoneId,
      label,
      category,
      milliCelsius,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ThermalZone
    return DeviceProbeApiPigeonUtils.deepEquals(this.zoneId, other.zoneId) && DeviceProbeApiPigeonUtils.deepEquals(this.label, other.label) && DeviceProbeApiPigeonUtils.deepEquals(this.category, other.category) && DeviceProbeApiPigeonUtils.deepEquals(this.milliCelsius, other.milliCelsius)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.zoneId)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.label)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.category)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.milliCelsius)
    return result
  }
  override fun toString(): String {
    return "ThermalZone(zoneId=$zoneId, label=$label, category=$category, milliCelsius=$milliCelsius)"
  }
}

/**
 * Codec 134.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ThermalSample (
  /**
   * Empty when `/sys/class/thermal` is not readable, which is the common case
   * on recent Samsung and Pixel builds.
   */
  val zones: List<ThermalZone>,
  /**
   * `PowerManager.getCurrentThermalStatus()`, 0 (none) to 6 (shutdown).
   * Available even when [zones] is empty, and often the only thermal signal
   * a device will give up.
   */
  val status: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ThermalSample {
      val zones = pigeonVar_list[0] as List<ThermalZone>
      val status = pigeonVar_list[1] as Long?
      return ThermalSample(zones, status)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      zones,
      status,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as ThermalSample
    return DeviceProbeApiPigeonUtils.deepEquals(this.zones, other.zones) && DeviceProbeApiPigeonUtils.deepEquals(this.status, other.status)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.zones)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.status)
    return result
  }
  override fun toString(): String {
    return "ThermalSample(zones=$zones, status=$status)"
  }
}

/**
 * Codec 135.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class BatterySnapshot (
  /** 0-100, computed from level and scale rather than assuming scale is 100. */
  val percent: Long? = null,
  val charging: Boolean? = null,
  /** "charging" | "discharging" | "full" | "notCharging" | "unknown". */
  val status: String? = null,
  /**
   * "good" | "overheat" | "dead" | "overVoltage" | "cold" | "failure" |
   * "unknown". Note that "good" is what almost every device reports right up
   * until it does not, so this is informational and never a health score.
   */
  val health: String? = null,
  val technology: String? = null,
  /**
   * TENTHS of a degree Celsius, exactly as Android reports it. Kept in the
   * platform's own unit so the conversion happens once, at display, rather
   * than rounding twice.
   */
  val tempDeciC: Long? = null,
  /**
   * MICROAMPS from `BATTERY_PROPERTY_CURRENT_NOW`.
   *
   * THE SIGN IS NOT PORTABLE. Most OEMs report negative while discharging;
   * several Samsung and Xiaomi builds report positive. Display the MAGNITUDE
   * and take direction from [charging], which is the one signal that is
   * consistent everywhere. Do not "fix" this by trusting the sign.
   */
  val currentMicroA: Long? = null,
  val voltageMilliV: Long? = null,
  /** API 34+, and null on plenty of devices above it. */
  val cycleCount: Long? = null,
  val chargeCounterMicroAh: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): BatterySnapshot {
      val percent = pigeonVar_list[0] as Long?
      val charging = pigeonVar_list[1] as Boolean?
      val status = pigeonVar_list[2] as String?
      val health = pigeonVar_list[3] as String?
      val technology = pigeonVar_list[4] as String?
      val tempDeciC = pigeonVar_list[5] as Long?
      val currentMicroA = pigeonVar_list[6] as Long?
      val voltageMilliV = pigeonVar_list[7] as Long?
      val cycleCount = pigeonVar_list[8] as Long?
      val chargeCounterMicroAh = pigeonVar_list[9] as Long?
      return BatterySnapshot(percent, charging, status, health, technology, tempDeciC, currentMicroA, voltageMilliV, cycleCount, chargeCounterMicroAh)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      percent,
      charging,
      status,
      health,
      technology,
      tempDeciC,
      currentMicroA,
      voltageMilliV,
      cycleCount,
      chargeCounterMicroAh,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as BatterySnapshot
    return DeviceProbeApiPigeonUtils.deepEquals(this.percent, other.percent) && DeviceProbeApiPigeonUtils.deepEquals(this.charging, other.charging) && DeviceProbeApiPigeonUtils.deepEquals(this.status, other.status) && DeviceProbeApiPigeonUtils.deepEquals(this.health, other.health) && DeviceProbeApiPigeonUtils.deepEquals(this.technology, other.technology) && DeviceProbeApiPigeonUtils.deepEquals(this.tempDeciC, other.tempDeciC) && DeviceProbeApiPigeonUtils.deepEquals(this.currentMicroA, other.currentMicroA) && DeviceProbeApiPigeonUtils.deepEquals(this.voltageMilliV, other.voltageMilliV) && DeviceProbeApiPigeonUtils.deepEquals(this.cycleCount, other.cycleCount) && DeviceProbeApiPigeonUtils.deepEquals(this.chargeCounterMicroAh, other.chargeCounterMicroAh)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.percent)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.charging)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.status)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.health)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.technology)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.tempDeciC)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.currentMicroA)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.voltageMilliV)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.cycleCount)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.chargeCounterMicroAh)
    return result
  }
  override fun toString(): String {
    return "BatterySnapshot(percent=$percent, charging=$charging, status=$status, health=$health, technology=$technology, tempDeciC=$tempDeciC, currentMicroA=$currentMicroA, voltageMilliV=$voltageMilliV, cycleCount=$cycleCount, chargeCounterMicroAh=$chargeCounterMicroAh)"
  }
}

/**
 * Codec 136.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MemorySnapshot (
  /**
   * `ActivityManager.MemoryInfo`, NOT `/proc/meminfo`. Same restriction story
   * as `/proc/stat`, and this one needs no file read at all.
   */
  val totalBytes: Long? = null,
  val availBytes: Long? = null,
  /**
   * The level at which Android starts killing background processes. Worth
   * showing because "2 GB free" means nothing without it.
   */
  val thresholdBytes: Long? = null,
  val lowMemory: Boolean? = null,
  /**
   * zram, on nearly every budget device. From `/proc/meminfo`, which is
   * readable where `/proc/stat` is not.
   */
  val swapTotalBytes: Long? = null,
  val swapFreeBytes: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MemorySnapshot {
      val totalBytes = pigeonVar_list[0] as Long?
      val availBytes = pigeonVar_list[1] as Long?
      val thresholdBytes = pigeonVar_list[2] as Long?
      val lowMemory = pigeonVar_list[3] as Boolean?
      val swapTotalBytes = pigeonVar_list[4] as Long?
      val swapFreeBytes = pigeonVar_list[5] as Long?
      return MemorySnapshot(totalBytes, availBytes, thresholdBytes, lowMemory, swapTotalBytes, swapFreeBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      totalBytes,
      availBytes,
      thresholdBytes,
      lowMemory,
      swapTotalBytes,
      swapFreeBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MemorySnapshot
    return DeviceProbeApiPigeonUtils.deepEquals(this.totalBytes, other.totalBytes) && DeviceProbeApiPigeonUtils.deepEquals(this.availBytes, other.availBytes) && DeviceProbeApiPigeonUtils.deepEquals(this.thresholdBytes, other.thresholdBytes) && DeviceProbeApiPigeonUtils.deepEquals(this.lowMemory, other.lowMemory) && DeviceProbeApiPigeonUtils.deepEquals(this.swapTotalBytes, other.swapTotalBytes) && DeviceProbeApiPigeonUtils.deepEquals(this.swapFreeBytes, other.swapFreeBytes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.totalBytes)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.availBytes)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.thresholdBytes)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.lowMemory)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.swapTotalBytes)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.swapFreeBytes)
    return result
  }
  override fun toString(): String {
    return "MemorySnapshot(totalBytes=$totalBytes, availBytes=$availBytes, thresholdBytes=$thresholdBytes, lowMemory=$lowMemory, swapTotalBytes=$swapTotalBytes, swapFreeBytes=$swapFreeBytes)"
  }
}

/**
 * One sensor as the device advertises it. Codec 137.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class SensorInfo (
  /**
   * Index into the device's sensor list. Stable for the process only, which is
   * all a UI session needs, and unlike `Sensor.getId()` it is never 0 for
   * everything.
   */
  val handle: Long,
  /**
   * The raw Android type constant. Kept alongside [category] so an unknown
   * OEM sensor can still be identified in a bug report.
   */
  val type: Long,
  val name: String,
  /** "motion" | "position" | "environment" | "body" | "composite" | "other". */
  val category: String,
  /**
   * 1 for a scalar such as light, 3 for a vector such as the accelerometer.
   * Drives whether the UI draws a number or a three-axis sparkline.
   */
  val valueCount: Long,
  val wakeUp: Boolean,
  /**
   * False when the sensor is present but the app may not register for it,
   * which is the case for heart rate and other BODY_SENSORS types.
   *
   * Reported rather than filtered out. A user looking for their heart rate
   * sensor needs to see "present, blocked by the system" instead of an
   * unexplained absence.
   */
  val readable: Boolean,
  val vendor: String? = null,
  val stringType: String? = null,
  val maxRange: Double? = null,
  val resolution: Double? = null,
  /** Milliamps while active, as declared by the driver. Frequently optimistic. */
  val powerMilliAmp: Double? = null,
  val minDelayMicros: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): SensorInfo {
      val handle = pigeonVar_list[0] as Long
      val type = pigeonVar_list[1] as Long
      val name = pigeonVar_list[2] as String
      val category = pigeonVar_list[3] as String
      val valueCount = pigeonVar_list[4] as Long
      val wakeUp = pigeonVar_list[5] as Boolean
      val readable = pigeonVar_list[6] as Boolean
      val vendor = pigeonVar_list[7] as String?
      val stringType = pigeonVar_list[8] as String?
      val maxRange = pigeonVar_list[9] as Double?
      val resolution = pigeonVar_list[10] as Double?
      val powerMilliAmp = pigeonVar_list[11] as Double?
      val minDelayMicros = pigeonVar_list[12] as Long?
      return SensorInfo(handle, type, name, category, valueCount, wakeUp, readable, vendor, stringType, maxRange, resolution, powerMilliAmp, minDelayMicros)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      handle,
      type,
      name,
      category,
      valueCount,
      wakeUp,
      readable,
      vendor,
      stringType,
      maxRange,
      resolution,
      powerMilliAmp,
      minDelayMicros,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as SensorInfo
    return DeviceProbeApiPigeonUtils.deepEquals(this.handle, other.handle) && DeviceProbeApiPigeonUtils.deepEquals(this.type, other.type) && DeviceProbeApiPigeonUtils.deepEquals(this.name, other.name) && DeviceProbeApiPigeonUtils.deepEquals(this.category, other.category) && DeviceProbeApiPigeonUtils.deepEquals(this.valueCount, other.valueCount) && DeviceProbeApiPigeonUtils.deepEquals(this.wakeUp, other.wakeUp) && DeviceProbeApiPigeonUtils.deepEquals(this.readable, other.readable) && DeviceProbeApiPigeonUtils.deepEquals(this.vendor, other.vendor) && DeviceProbeApiPigeonUtils.deepEquals(this.stringType, other.stringType) && DeviceProbeApiPigeonUtils.deepEquals(this.maxRange, other.maxRange) && DeviceProbeApiPigeonUtils.deepEquals(this.resolution, other.resolution) && DeviceProbeApiPigeonUtils.deepEquals(this.powerMilliAmp, other.powerMilliAmp) && DeviceProbeApiPigeonUtils.deepEquals(this.minDelayMicros, other.minDelayMicros)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.handle)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.type)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.name)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.category)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.valueCount)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.wakeUp)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.readable)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.vendor)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.stringType)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.maxRange)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.resolution)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.powerMilliAmp)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.minDelayMicros)
    return result
  }
  override fun toString(): String {
    return "SensorInfo(handle=$handle, type=$type, name=$name, category=$category, valueCount=$valueCount, wakeUp=$wakeUp, readable=$readable, vendor=$vendor, stringType=$stringType, maxRange=$maxRange, resolution=$resolution, powerMilliAmp=$powerMilliAmp, minDelayMicros=$minDelayMicros)"
  }
}

/**
 * One tick. Codec 138.
 *
 * A COMPOSITE RATHER THAN FOUR CALLS, because at 2 Hz four channel round trips
 * per tick is four times the platform-thread wakeups for the same data, and
 * the four readings would no longer share a single sample clock.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DeviceSnapshot (
  /**
   * `SystemClock.elapsedRealtime()`. Milliseconds since boot INCLUDING deep
   * sleep, which is what makes it usable across a screen-off gap.
   *
   * Never null: if this is unavailable the process is not running. It is both
   * the uptime row AND the divisor every rate is computed against.
   */
  val elapsedRealtimeMillis: Long,
  val cpu: CpuSample? = null,
  val thermal: ThermalSample? = null,
  val battery: BatterySnapshot? = null,
  val memory: MemorySnapshot? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DeviceSnapshot {
      val elapsedRealtimeMillis = pigeonVar_list[0] as Long
      val cpu = pigeonVar_list[1] as CpuSample?
      val thermal = pigeonVar_list[2] as ThermalSample?
      val battery = pigeonVar_list[3] as BatterySnapshot?
      val memory = pigeonVar_list[4] as MemorySnapshot?
      return DeviceSnapshot(elapsedRealtimeMillis, cpu, thermal, battery, memory)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      elapsedRealtimeMillis,
      cpu,
      thermal,
      battery,
      memory,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as DeviceSnapshot
    return DeviceProbeApiPigeonUtils.deepEquals(this.elapsedRealtimeMillis, other.elapsedRealtimeMillis) && DeviceProbeApiPigeonUtils.deepEquals(this.cpu, other.cpu) && DeviceProbeApiPigeonUtils.deepEquals(this.thermal, other.thermal) && DeviceProbeApiPigeonUtils.deepEquals(this.battery, other.battery) && DeviceProbeApiPigeonUtils.deepEquals(this.memory, other.memory)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.elapsedRealtimeMillis)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.cpu)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.thermal)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.battery)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.memory)
    return result
  }
  override fun toString(): String {
    return "DeviceSnapshot(elapsedRealtimeMillis=$elapsedRealtimeMillis, cpu=$cpu, thermal=$thermal, battery=$battery, memory=$memory)"
  }
}

/**
 * WHAT THIS DEVICE WILL LET US TOUCH. Codec 139.
 *
 * ─── WHY THIS IS NOT DERIVED FROM A VERSION NUMBER IN DART ──────────────────
 *
 * It could be, and it would be wrong about half the time. The API level says
 * which permissions EXIST; it says nothing about which were granted, and a
 * screen that decides what to offer from the level alone offers a restore that
 * then fails at the last step. Both halves are read natively and reported
 * together.
 *
 * ─── THE THREE TIERS ARE NOT BETTER AND WORSE ───────────────────────────────
 *
 * This app runs from API 24, and the oldest devices have the MOST file access,
 * not the least. Android 11 is what closed per app storage to outside readers.
 * A 2017 phone can read another app's trash folder with a plain storage
 * permission; a 2024 phone cannot at any price.
 *
 *   legacy   24 to 28, whole volume readable, no platform trash bin
 *   scoped   29 exactly, neither legacy access nor all files access exists
 *   managed  30 and up, all files access and the trash bin, per app storage
 *            closed to direct reads
 *
 * 29 is genuinely thin and nothing here can fix it: the platform removed the
 * old route one release before it added the new one. The UI's job is to say so
 * rather than to offer a scan that finds nothing.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class StorageAccess (
  /**
   * `Build.VERSION.SDK_INT`. Reported alongside [tier] rather than instead of
   * it, because a bug report needs the number and a screen needs the tier.
   */
  val sdkInt: Long,
  /**
   * "legacy" | "scoped" | "managed". A String for the reason in the file
   * header, and because a fourth tier is a matter of when rather than if.
   */
  val tier: String,
  /**
   * `MediaStore.IS_TRASHED` and the system bin, API 30 and up. The only
   * recovery route the platform itself blesses.
   */
  val osTrashBin: Boolean,
  /**
   * The All Files Access settings screen exists AND resolves. False below 30,
   * and false on the handful of ROMs that ship without the screen, where
   * asking would send the user somewhere that does not open.
   */
  val allFilesAccessPossible: Boolean,
  /**
   * `Environment.isExternalStorageManager()`. THE GRANT, not the possibility.
   * [allFilesAccessPossible] true with this false is the state that deserves a
   * button; both false deserves an explanation.
   */
  val allFilesAccessGranted: Boolean,
  /**
   * Read and write to shared storage held on API 24 to 28. Always false above
   * that, where the permission is either capped in the manifest or no longer
   * grants what its name suggests.
   */
  val legacyStorageGranted: Boolean,
  /**
   * Whether per app storage can be walked directly. True on the legacy tier
   * with the grant, false everywhere from API 29. This single field is the
   * difference between a scan that reaches another app's trash folder and one
   * that cannot see it at any permission level.
   */
  val appDataReadable: Boolean,
  /**
   * Nothing but MediaStore and whatever a document tree grant provides. True
   * on the scoped tier, and on the managed tier until the grant is given.
   */
  val mediaStoreOnly: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): StorageAccess {
      val sdkInt = pigeonVar_list[0] as Long
      val tier = pigeonVar_list[1] as String
      val osTrashBin = pigeonVar_list[2] as Boolean
      val allFilesAccessPossible = pigeonVar_list[3] as Boolean
      val allFilesAccessGranted = pigeonVar_list[4] as Boolean
      val legacyStorageGranted = pigeonVar_list[5] as Boolean
      val appDataReadable = pigeonVar_list[6] as Boolean
      val mediaStoreOnly = pigeonVar_list[7] as Boolean
      return StorageAccess(sdkInt, tier, osTrashBin, allFilesAccessPossible, allFilesAccessGranted, legacyStorageGranted, appDataReadable, mediaStoreOnly)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      sdkInt,
      tier,
      osTrashBin,
      allFilesAccessPossible,
      allFilesAccessGranted,
      legacyStorageGranted,
      appDataReadable,
      mediaStoreOnly,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as StorageAccess
    return DeviceProbeApiPigeonUtils.deepEquals(this.sdkInt, other.sdkInt) && DeviceProbeApiPigeonUtils.deepEquals(this.tier, other.tier) && DeviceProbeApiPigeonUtils.deepEquals(this.osTrashBin, other.osTrashBin) && DeviceProbeApiPigeonUtils.deepEquals(this.allFilesAccessPossible, other.allFilesAccessPossible) && DeviceProbeApiPigeonUtils.deepEquals(this.allFilesAccessGranted, other.allFilesAccessGranted) && DeviceProbeApiPigeonUtils.deepEquals(this.legacyStorageGranted, other.legacyStorageGranted) && DeviceProbeApiPigeonUtils.deepEquals(this.appDataReadable, other.appDataReadable) && DeviceProbeApiPigeonUtils.deepEquals(this.mediaStoreOnly, other.mediaStoreOnly)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.sdkInt)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.tier)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.osTrashBin)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.allFilesAccessPossible)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.allFilesAccessGranted)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.legacyStorageGranted)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.appDataReadable)
    result = 31 * result + DeviceProbeApiPigeonUtils.deepHash(this.mediaStoreOnly)
    return result
  }
  override fun toString(): String {
    return "StorageAccess(sdkInt=$sdkInt, tier=$tier, osTrashBin=$osTrashBin, allFilesAccessPossible=$allFilesAccessPossible, allFilesAccessGranted=$allFilesAccessGranted, legacyStorageGranted=$legacyStorageGranted, appDataReadable=$appDataReadable, mediaStoreOnly=$mediaStoreOnly)"
  }
}
private open class DeviceProbeApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ProbeCapabilities.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CpuCluster.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CpuInfo.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CpuSample.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ThermalZone.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ThermalSample.fromList(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          BatterySnapshot.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MemorySnapshot.fromList(it)
        }
      }
      137.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          SensorInfo.fromList(it)
        }
      }
      138.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DeviceSnapshot.fromList(it)
        }
      }
      139.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          StorageAccess.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is ProbeCapabilities -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is CpuCluster -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is CpuInfo -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is CpuSample -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is ThermalZone -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is ThermalSample -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      is BatterySnapshot -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is MemorySnapshot -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      is SensorInfo -> {
        stream.write(137)
        writeValue(stream, value.toList())
      }
      is DeviceSnapshot -> {
        stream.write(138)
        writeValue(stream, value.toList())
      }
      is StorageAccess -> {
        stream.write(139)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface DeviceProbeHostApi {
  /**
   * Probed once natively, cached for the process. Call this before rendering
   * anything: it decides which sections exist at all.
   */
  fun capabilities(callback: (Result<ProbeCapabilities>) -> Unit)
  /**
   * Static description. Read once per app launch; core topology does not
   * change while the process is alive.
   */
  fun cpuInfo(callback: (Result<CpuInfo>) -> Unit)
  /**
   * The full sensor list including entries this app may not register for.
   * `@async` because it walks the whole list and resolves permissions.
   */
  fun sensors(callback: (Result<List<SensorInfo>>) -> Unit)
  /**
   * One tick of everything live. Called on the sampler's interval.
   *
   * Sections that this device refuses come back null rather than as an object
   * full of nulls, so the UI can drop an entire card instead of drawing an
   * empty one.
   */
  fun readSnapshot(callback: (Result<DeviceSnapshot>) -> Unit)
  /**
   * Which storage tier this device is on and what was actually granted.
   *
   * NOT CACHED, unlike [capabilities]. All Files Access can be granted and
   * revoked from Settings while the process is alive, so a cached answer would
   * have the app offering a scan the system will refuse, or refusing one it
   * would now allow. Call it when a screen that depends on it appears, and
   * again on resume.
   */
  fun storageAccess(callback: (Result<StorageAccess>) -> Unit)

  companion object {
    /** The codec used by DeviceProbeHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      DeviceProbeApiPigeonCodec()
    }
    /** Sets up an instance of `DeviceProbeHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: DeviceProbeHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.device_probe.DeviceProbeHostApi.capabilities$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.capabilities{ result: Result<ProbeCapabilities> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DeviceProbeApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DeviceProbeApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.device_probe.DeviceProbeHostApi.cpuInfo$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.cpuInfo{ result: Result<CpuInfo> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DeviceProbeApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DeviceProbeApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.device_probe.DeviceProbeHostApi.sensors$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.sensors{ result: Result<List<SensorInfo>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DeviceProbeApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DeviceProbeApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.device_probe.DeviceProbeHostApi.readSnapshot$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.readSnapshot{ result: Result<DeviceSnapshot> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DeviceProbeApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DeviceProbeApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.device_probe.DeviceProbeHostApi.storageAccess$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.storageAccess{ result: Result<StorageAccess> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DeviceProbeApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DeviceProbeApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
